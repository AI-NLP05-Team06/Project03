# KDIC 검색평가 v3.5 — 업무 필터 없음 Colab 패키지

## 고정 조건

- 평가 데이터셋: `Evaluation_DataSet_v3.5.xlsx`
- 평가 시트: `평가데이터셋 v3`
- 유효 문항: 빈 행을 제외한 132개
- 검색 범위: 업무 라벨 필터를 적용하지 않고 전체 427개 청크
- 원본 평가 데이터셋은 수정하지 않음
- 중첩 JSON 배열은 실행 중에만 1차원 청크 ID 배열로 해석하며 셀 값은 변경하지 않음
- 원본·패키지 사본 SHA-256: `ef70f5a591ad580f1979053adb37f6c1bd3425454f8feda0a506770aaed32cd5`

## 평가 방식

1. BM25 Nori Discard
2. BGE-M3 Dense content
3. BGE-M3 Dense structured — 제목·소제목·브레드크럼·본문
4. BGE-M3 Sparse
5. Hybrid A — Dense 0.85 + BM25 Nori 0.15, Weighted RRF
6. Hybrid A + BGE Reranker

## 실행 방법

1. 원하는 방식의 폴더에서 Colab 노트북과 평가기 ZIP을 준비합니다.
2. `07_공통입력_원본그대로`의 XLSX와 `KDIC_output.zip`을 함께 업로드합니다.
3. 노트북 셀을 위에서 아래로 실행합니다.
4. Dry-run에서 `question_count=132`, `domain_filter=false`를 확인합니다.
5. 마지막 셀에서 결과 ZIP을 내려받습니다.

## API 및 GPU

- BM25 Nori: HCX API 키 불필요
- BGE-M3 Dense content: HCX의 BGE-M3 임베딩 API 키 필요
- BGE-M3 Dense structured: HCX의 BGE-M3 임베딩 API 키 필요. 최초 실행 시 427개 구조화 문서를 임베딩하며 결과 폴더에 캐시
- BGE-M3 Sparse: Colab GPU에서 로컬 실행, HCX API 키 불필요
- Hybrid A: Dense 질의 임베딩 때문에 HCX API 키 필요
- Hybrid Reranker: HCX API 키 + Colab GPU 필요

## 비교 주의사항

업무 필터 적용 버전과 비교할 때는 같은 v3.5 데이터셋과 같은 검색 파라미터를 사용해야 합니다.
결과 `summary.json`의 `domain_filter`가 `false`인지 확인하세요.
