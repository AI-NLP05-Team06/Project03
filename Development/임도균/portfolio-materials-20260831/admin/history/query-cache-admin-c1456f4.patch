diff --git a/Development/ec2_deploy/2026-08-28-kdic-managed-suggestion-admin-migration.sql b/Development/ec2_deploy/2026-08-28-kdic-managed-suggestion-admin-migration.sql
new file mode 100644
index 0000000..ff815e9
--- /dev/null
+++ b/Development/ec2_deploy/2026-08-28-kdic-managed-suggestion-admin-migration.sql
@@ -0,0 +1,33 @@
+BEGIN;
+
+-- The original migration fixed the catalog at exactly 26 rows.  The manager UI
+-- keeps those defaults, but administrators may add or retire keyword buttons.
+ALTER TABLE suggestion_catalog
+    ADD COLUMN IF NOT EXISTS business_key TEXT;
+
+UPDATE suggestion_catalog
+SET business_key = CASE
+    WHEN business LIKE '%착오송금%' THEN '착오송금'
+    WHEN business LIKE '%예금보험금%' THEN '예금보험금'
+    WHEN business LIKE '%예금자보호%' THEN '예금자보호'
+    WHEN business LIKE '%미수령금%' THEN '미수령금'
+    WHEN business LIKE '%채무조정%' THEN '채무조정'
+    WHEN business LIKE '%은닉재산%' THEN '은닉재산'
+    ELSE business
+END
+WHERE business_key IS NULL OR btrim(business_key) = '';
+
+ALTER TABLE suggestion_catalog
+    ALTER COLUMN business_key SET NOT NULL;
+
+ALTER TABLE suggestion_catalog
+    DROP CONSTRAINT IF EXISTS suggestion_catalog_display_order_check;
+
+ALTER TABLE suggestion_catalog
+    ADD CONSTRAINT suggestion_catalog_display_order_check
+        CHECK (display_order BETWEEN 1 AND 32767);
+
+CREATE INDEX IF NOT EXISTS idx_suggestion_catalog_business_key
+    ON suggestion_catalog (business_key, display_order);
+
+COMMIT;
diff --git a/Development/ec2_deploy/2026-08-28-kdic-persistent-suggestion-catalog-migration.sql b/Development/ec2_deploy/2026-08-28-kdic-persistent-suggestion-catalog-migration.sql
index d383e6d..a74f528 100644
--- a/Development/ec2_deploy/2026-08-28-kdic-persistent-suggestion-catalog-migration.sql
+++ b/Development/ec2_deploy/2026-08-28-kdic-persistent-suggestion-catalog-migration.sql
@@ -11,6 +11,7 @@ WHERE validation_status = 'VALIDATED';
 
 CREATE TABLE IF NOT EXISTS suggestion_catalog (
     suggestion_id       VARCHAR(100) PRIMARY KEY,
+    business_key        TEXT NOT NULL,
     business            TEXT NOT NULL,
     keyword             TEXT NOT NULL,
     canonical_question  TEXT NOT NULL,
diff --git a/Development/ec2_deploy/kdic_deploy_assets/2026-08-23-kdic-service-core.py b/Development/ec2_deploy/kdic_deploy_assets/2026-08-23-kdic-service-core.py
index 548a4ec..fb0f7e4 100644
--- a/Development/ec2_deploy/kdic_deploy_assets/2026-08-23-kdic-service-core.py
+++ b/Development/ec2_deploy/kdic_deploy_assets/2026-08-23-kdic-service-core.py
@@ -267,6 +267,7 @@ BASIS_EXPLANATION_SCHEMA_VERSION = "kdic-basis-explanation-v2"
 ANSWER_SUMMARY_SCHEMA_VERSION = "kdic-answer-summary-v1"
 _SUGGESTION_BY_ID: dict[str, dict[str, str]] = {}
 _SUGGESTIONS_BY_BUSINESS_KEY: dict[str, list[dict[str, str]]] = {}
+_SUGGESTION_CATALOG_PROVIDER: Callable[[], Sequence[Mapping[str, Any]]] | None = None
 
 
 def _suggestion_id(business: str, label: str, query: str) -> str:
@@ -299,7 +300,50 @@ def _build_suggestion_registry() -> None:
 _build_suggestion_registry()
 
 
+def set_suggestion_catalog_provider(
+    provider: Callable[[], Sequence[Mapping[str, Any]]] | None,
+) -> None:
+    """Use the administrator-managed catalog when a durable store provides one."""
+
+    global _SUGGESTION_CATALOG_PROVIDER
+    _SUGGESTION_CATALOG_PROVIDER = provider
+
+
+def _managed_suggestion_catalog() -> list[dict[str, str]]:
+    if not callable(_SUGGESTION_CATALOG_PROVIDER):
+        return []
+    try:
+        source = list(_SUGGESTION_CATALOG_PROVIDER() or [])
+    except Exception:
+        return []
+    output: list[dict[str, str]] = []
+    for row in source:
+        if not isinstance(row, Mapping) or row.get("is_enabled") is False:
+            continue
+        suggestion_id = _clean_text(row.get("suggestion_id")).upper()
+        business = _clean_text(row.get("business"))
+        business_key = _clean_text(row.get("business_key")) or _business_scope_key(
+            business
+        )
+        label = _clean_text(row.get("label") or row.get("keyword"))
+        query = _clean_text(row.get("query") or row.get("canonical_question"))
+        if suggestion_id and business and business_key and label and query:
+            output.append(
+                {
+                    "suggestion_id": suggestion_id,
+                    "business_key": business_key,
+                    "business": business,
+                    "label": label,
+                    "query": query,
+                }
+            )
+    return output
+
+
 def suggestion_catalog() -> list[dict[str, str]]:
+    managed = _managed_suggestion_catalog()
+    if managed:
+        return managed
     return [
         copy.deepcopy(row)
         for rows in _SUGGESTIONS_BY_BUSINESS_KEY.values()
@@ -314,31 +358,36 @@ def resolve_registered_suggestion(
     """Accept cache access only when the server ID and canonical query both match."""
 
     clean_id = _clean_text(suggestion_id).upper()
-    record = _SUGGESTION_BY_ID.get(clean_id)
+    record = next(
+        (row for row in suggestion_catalog() if row["suggestion_id"] == clean_id),
+        None,
+    )
     if record is None or record["query"] != _clean_text(question):
         return None
     return copy.deepcopy(record)
 
 
 def suggestion_registry_stats() -> dict[str, Any]:
+    rows = suggestion_catalog()
     return {
         "schema_version": SUGGESTION_CACHE_SCHEMA_VERSION,
-        "registered_questions": len(_SUGGESTION_BY_ID),
-        "business_count": len(_SUGGESTIONS_BY_BUSINESS_KEY),
+        "registered_questions": len(rows),
+        "business_count": len({row["business_key"] for row in rows}),
         "requires_exact_id_and_query": True,
+        "managed": callable(_SUGGESTION_CATALOG_PROVIDER),
     }
 
 
 def _followup_keywords(businesses: Sequence[str]) -> list[dict[str, str]]:
     output: list[dict[str, str]] = []
+    rows = suggestion_catalog()
     for business in businesses:
-        for key, rows in _SUGGESTIONS_BY_BUSINESS_KEY.items():
-            if key not in business:
+        for record in rows:
+            if record["business_key"] not in business:
                 continue
-            for record in rows:
-                item = copy.deepcopy(record)
-                if item not in output:
-                    output.append(item)
+            item = copy.deepcopy(record)
+            if item not in output:
+                output.append(item)
     return output[:5]
 
 
@@ -1694,6 +1743,11 @@ class KDICJobService:
         sync_catalog = getattr(self.suggestion_cache, "sync_catalog", None)
         if callable(sync_catalog):
             sync_catalog(suggestion_catalog())
+        list_catalog = getattr(self.suggestion_cache, "list_catalog", None)
+        if callable(list_catalog):
+            set_suggestion_catalog_provider(
+                lambda: list_catalog(include_disabled=False)
+            )
         self.executor = ThreadPoolExecutor(
             max_workers=max(1, int(max_workers)), thread_name_prefix="kdic-pipeline"
         )
@@ -1881,7 +1935,14 @@ class KDICJobService:
                     save(session)
             public = normalize_public_result(raw)
             if record.cache_key and record.suggestion_id:
-                suggestion = _SUGGESTION_BY_ID[record.suggestion_id]
+                suggestion = resolve_registered_suggestion(
+                    record.question,
+                    record.suggestion_id,
+                )
+                if suggestion is None:
+                    raise RuntimeError(
+                        "답변 생성 중 질의 캐시 항목이 변경되거나 비활성화되었습니다."
+                    )
                 eligible, reason = self._cache_eligibility(
                     public,
                     suggestion["business"],
diff --git a/Development/ec2_deploy/kdic_deploy_assets/kdic-admin-ui.html b/Development/ec2_deploy/kdic_deploy_assets/kdic-admin-ui.html
index 5d1b51c..97e794d 100644
--- a/Development/ec2_deploy/kdic_deploy_assets/kdic-admin-ui.html
+++ b/Development/ec2_deploy/kdic_deploy_assets/kdic-admin-ui.html
@@ -109,6 +109,8 @@ html,body,button,input,select,textarea{font-family:"Pretendard Variable",Pretend
 .admin-app.dark #eval-change-hint[data-tone="info"]{background:#172f4d!important;border-color:#4478b7!important;color:#c7e0ff!important}.admin-app.dark #eval-change-hint[data-tone="info"] b{color:#e5f1ff!important}.admin-app.dark #eval-change-hint[data-tone="warning"]{background:#332b19!important;border-color:#786237!important;color:#ffe0a0!important}.admin-app.dark #eval-change-hint[data-tone="warning"] b{color:#fff1cb!important}
 @media(max-width:1100px){.metric-k-grid{grid-template-columns:repeat(2,1fr)}}
 @media(max-width:720px){.eval-depth-row,.metric-k-layout{grid-template-columns:1fr}.api-hit{grid-template-columns:minmax(120px,.7fr) minmax(180px,1.3fr) 52px}}
+.prompt-preset-manager{padding:18px;margin-bottom:14px}.prompt-preset-toolbar{display:grid;grid-template-columns:minmax(220px,1fr) auto auto;gap:8px;align-items:end}.prompt-preset-toolbar label,.prompt-save-row label{display:grid;gap:6px}.prompt-preset-toolbar label span,.prompt-save-row label span{font-size:8px;color:var(--muted);font-weight:800}.prompt-preset-toolbar select,.prompt-save-row input{width:100%}.prompt-save-row{display:grid;grid-template-columns:minmax(220px,1fr) auto;gap:8px;align-items:end;margin-top:14px;padding-top:14px;border-top:1px solid var(--line)}.prompt-preset-summary{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}.prompt-preset-summary span{padding:5px 8px;border-radius:99px;background:var(--soft);color:var(--blue);font-size:8px;font-weight:800}.prompt-test-preset{display:grid;grid-template-columns:1fr auto;gap:8px;align-items:end;margin:12px 0}.prompt-test-preset label{display:grid;gap:6px}.prompt-test-preset span{font-size:8px;color:var(--muted);font-weight:800}.prompt-preset-note{margin-top:8px;color:var(--muted);font-size:8px;line-height:1.55}.admin-app.dark .prompt-preset-manager,.admin-app.dark .prompt-save-row{border-color:var(--line)}
+@media(max-width:760px){.prompt-preset-toolbar,.prompt-save-row,.prompt-test-preset{grid-template-columns:1fr}.prompt-preset-toolbar button,.prompt-save-row button,.prompt-test-preset button{width:100%}}
 </style>
 </head>
 <body>
@@ -128,18 +130,18 @@ state.pipelineScroll={left:0,top:0};
 state.sidebarScroll=0;
 state.pageScroll=0;
 state.renderedView=state.view;
-const navItems=[['dashboard','▦','대시보드','운영 상태'],['monitoring','◒','운영 모니터링','질의·토큰·지연시간'],['pipeline','⌘','파이프라인','흐름·안전 설정'],['guardrails','◈','가드레일','금칙어·PII 관리'],['prompts','✎','프롬프트','현재안·개선안 비교'],['runtime','◇','파라미터','개선안 설정'],['evaluation','◫','검색 비교 평가','데이터셋 일괄 평가'],['data','▤','청크 관리','조회·추가·제거'],['keys','◆','API 키','검증·교체'],['apply','✓','반영 센터','승인·롤백'],['chat','◉','챗봇 테스트','최종 답변 비교'],['jobs','↻','작업 이력','챗봇 Job']];
+const navItems=[['dashboard','▦','대시보드','운영 상태'],['monitoring','◒','운영 모니터링','질의·토큰·지연시간'],['pipeline','⌘','파이프라인','흐름·안전 설정'],['guardrails','◈','가드레일','금칙어·PII 관리'],['prompts','✎','프롬프트 설정 관리','저장·선택·답변 비교'],['runtime','◇','파라미터 설정 관리','저장·선택·삭제'],['evaluation','◫','검색 비교 평가','데이터셋 일괄 평가'],['data','▤','청크 관리','수집·적재·운영 청크'],['data_ingest','↓','수집·적재 관리','URL 수집·재청킹·JSON 추가'],['data_library','▤','운영 청크 관리','조회·상세·제거'],['keys','◆','API 키','검증·교체'],['apply','✓','반영 센터','승인·롤백'],['chat','◉','챗봇 테스트','최종 답변 비교'],['jobs','↻','작업 이력','챗봇 Job']];
 const navGroups=[
   {id:'dashboard',icon:'▦',label:'대시보드',cap:'운영 상태'},
   {id:'monitoring',icon:'◒',label:'운영 모니터링',cap:'질의·토큰·지연시간'},
   {id:'enhancement',icon:'✦',label:'챗봇 고도화',cap:'검색·답변 설정과 검증',items:['pipeline','prompts','runtime','evaluation','chat']},
-  {id:'data',icon:'▤',label:'청크 관리',cap:'조회·추가·제거'},
+  {id:'data',icon:'▤',label:'청크 관리',cap:'수집·적재·운영 청크',items:['data_ingest','data_library']},
   {id:'security',icon:'◈',label:'보안·연동 관리',cap:'가드레일·API 키',items:['guardrails','keys']},
   {id:'apply',icon:'✓',label:'반영 센터',cap:'승인·롤백'},
   {id:'jobs',icon:'↻',label:'작업 이력',cap:'챗봇 Job'}
 ];
 const navExpansion=(()=>{try{return JSON.parse(localStorage.getItem('kdic-admin-nav-groups')||'{}')||{};}catch(_){return {};}})();
-Object.assign(state,{parameterPresets:[],selectedPresetId:'',pipelineParameterValues:null,parameterPresetName:''});
+Object.assign(state,{parameterPresets:[],selectedPresetId:'',pipelineParameterValues:null,parameterPresetName:'',promptPresets:[],selectedPromptPresetId:'',promptPresetName:''});
 const metricNames={hit:'Hit',recall:'Recall',mrr:'MRR',map:'MAP',complete:'Complete',ndcg:'nDCG',precision:'Precision',f1:'F1'};
 const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
 const attr=v=>esc(v).replace(/`/g,'&#96;');
@@ -148,7 +150,10 @@ const fmtDate=v=>v?new Date(Number(v)*1000).toLocaleString('ko-KR'):'—';
 const pct=v=>v==null?'—':`${(Number(v)*100).toFixed(1)}%`;
 const sleep=ms=>new Promise(r=>setTimeout(r,ms));
 async function api(path,options={}){const headers={...(options.headers||{})};if(options.body&&!headers['Content-Type'])headers['Content-Type']='application/json';const res=await fetch(path,{credentials:'include',...options,headers});const type=res.headers.get('content-type')||'';const out=type.includes('json')?await res.json():await res.text();if(!res.ok){const error=new Error(typeof out==='object'?(out.detail||JSON.stringify(out)):out);error.status=res.status;throw error;}return out;}
-function toast(message,error=false){document.querySelector('.toast')?.remove();const el=document.createElement('div');el.className=`toast${error?' error':''}`;el.innerHTML=`<b>${error?'!':'✓'}</b>${esc(message)}`;document.body.appendChild(el);setTimeout(()=>el.remove(),4200);}
+function parameterTerminology(value){return String(value??'').replaceAll('파라미터 설정 창고','파라미터 설정 관리').replaceAll('파라미터 창고 열기','파라미터 설정 열기').replaceAll('파라미터 창고에서','저장된 파라미터 설정에서').replaceAll('파라미터 창고의 설정을','저장된 파라미터 설정을').replaceAll('파라미터 창고','파라미터 설정 관리').replaceAll('설정 창고에','파라미터 설정 목록에').replaceAll('창고 열기','파라미터 설정 열기').replaceAll('창고에서','설정 목록에서').replaceAll('창고에','설정 목록에').replaceAll('설정 창고','파라미터 설정 관리');}
+function normalizeParameterTerminology(container){if(!container)return;const walker=document.createTreeWalker(container,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);nodes.forEach(node=>{const next=parameterTerminology(node.nodeValue);if(next!==node.nodeValue)node.nodeValue=next;});container.querySelectorAll('[title],[aria-label],[placeholder]').forEach(el=>['title','aria-label','placeholder'].forEach(name=>{if(el.hasAttribute(name))el.setAttribute(name,parameterTerminology(el.getAttribute(name)));}));}
+const nativeConfirm=window.confirm.bind(window);window.confirm=message=>nativeConfirm(parameterTerminology(message));
+function toast(message,error=false){document.querySelector('.toast')?.remove();const el=document.createElement('div');el.className=`toast${error?' error':''}`;el.innerHTML=`<b>${error?'!':'✓'}</b>${esc(parameterTerminology(message))}`;document.body.appendChild(el);setTimeout(()=>el.remove(),4200);}
 function statusBadge(status){const k=String(status||'UNKNOWN').toUpperCase();const map={DONE:['SUCCEEDED','완료'],RUNNING:['RUNNING','처리 중'],ERROR:['FAILED','오류'],FAILED:['FAILED','오류'],ACTIVE:['ACTIVE','정상'],STAGED_WRITE:['RUNNING','초안-승인'],QUEUED:['QUEUED','대기']};const [c,l]=map[k]||['INACTIVE',k];return `<span class="status status-${c.toLowerCase()}"><i></i>${l}</span>`;}
 const heading=(e,t,d,a='')=>`<div class="page-heading"><div><span>${e}</span><h1>${t}</h1><p>${d}</p></div>${a}</div>`;
 const metric=(l,v,d,t='blue')=>`<article class="metric metric-${t}"><div class="metric-top"><span>${l}</span><b>↗</b></div><strong>${esc(v)}</strong><small>${esc(d)}</small></article>`;
@@ -193,7 +198,7 @@ function pipelineLabelEditor(node){return `<div class="pipeline-label-editor"><l
 function pipelineSettingsPanel(){const node=state.pipelineNode;if(!node)return `<aside class="panel pipeline-setting-panel"><span class="section-kicker">PIPELINE BLOCK</span><h2>블록을 선택하세요</h2><p>모든 블록의 표시명을 바꿀 수 있습니다. ✓ 블록에서는 검증된 운영 파라미터도 초안으로 관리할 수 있습니다.</p><div class="pipeline-locked-note">회색 블록은 표시명만 변경할 수 있고 실제 코드는 로컬 개발 환경에서 관리합니다.</div></aside>`;const common=`<span class="section-kicker">${node.configurable?'PARAMETER DRAFT':'DISPLAY CUSTOM'}</span><h2>${esc(node.label)}</h2><p>${esc(node.description)}</p>${pipelineLabelEditor(node)}`;if(node.setting_view==='runtime'){const candidate=state.draft?.candidate_config||state.draft?.active_config||{};return `<aside class="panel pipeline-setting-panel">${common}<div class="setting-fields">${(node.settings||[]).map(key=>pipelineSettingField(key,candidate[key])).join('')}</div><div class="pipeline-setting-actions"><button class="primary-btn" data-action="save-pipeline-settings">이 블록의 파라미터 초안 저장</button><button class="secondary-btn" data-view="evaluation">데이터셋 검색 비교 →</button><button class="secondary-btn" data-view="chat">최종 답변 비교 →</button></div></aside>`;}const target=node.setting_view;if(target==='prompts'||target==='guardrails')return `<aside class="panel pipeline-setting-panel">${common}<div class="pipeline-locked-note">${target==='prompts'?'이 단계의 동작은 버전 관리되는 답변 프롬프트에서 수정합니다.':'이 단계의 동작은 가드레일 규칙에서 수정합니다.'}</div><div class="pipeline-setting-actions"><button class="primary-btn" data-view="${attr(target)}">${target==='prompts'?'프롬프트 초안 열기':'가드레일 열기'} →</button><button class="secondary-btn" data-view="chat">최종 답변 비교 →</button></div></aside>`;return `<aside class="panel pipeline-setting-panel">${common}<div class="pipeline-locked-note">이 블록은 표시명만 변경할 수 있습니다. 동작 변경은 로컬에서 코드 검증 후 배포해야 합니다.</div></aside>`;}
 function pipelineView(){const graph=state.pipelineGraph;if(!graph)return `${heading('PIPELINE STUDIO','동적 파이프라인','현재 챗봇 런타임 구조를 불러오고 있습니다.')}<section class="panel empty-list">파이프라인 정보를 불러오는 중입니다.</section>`;const byId=Object.fromEntries((graph.nodes||[]).map(node=>[node.id,node]));const lanes=pipelineLanes.map(([id,label,ids])=>`<section class="pipeline-lane lane-${id}"><header><span>${label}</span><small>${ids.filter(nodeId=>byId[nodeId]).length}단계</small></header><div>${ids.map(nodeId=>byId[nodeId]?pipelineNodeCard(byId[nodeId]):'').join('')}</div></section>`).join('');return `${heading('PIPELINE STUDIO','운영 파이프라인 설정','코드는 로컬에서 관리하고, 운영 서버에서는 안전하게 허용된 설정과 모든 블록의 표시명만 변경합니다.','<button class="secondary-btn" data-action="refresh-pipeline">↻ 런타임 다시 읽기</button>')}<section class="pipeline-flow-steps"><button data-view="pipeline"><b>1. 블록 선택</b><small>표시명·안전 설정</small></button><button data-view="runtime"><b>2. 개선안 저장</b><small>현재안은 유지</small></button><button data-view="evaluation"><b>3. 검색 비교</b><small>평가데이터셋·차트</small></button><button data-view="chat"><b>4. 최종 답변 비교</b><small>현재안·개선안</small></button><button data-view="apply"><b>5. 반영·롤백</b><small>통합 변경 이력</small></button></section><div class="pipeline-runtime-strip"><span><b>런타임</b>${esc(graph.pipeline_name)}</span><span><b>설정 가능 블록</b>${(graph.nodes||[]).filter(n=>n.configurable).length}개</span><span><b>구조 해시</b><code>${esc(graph.fingerprint)}</code></span><span class="readonly-tag">서버 코드 편집 차단</span></div><section class="pipeline-workspace"><div class="panel pipeline-map-wrap"><div class="pipeline-map" id="pipeline-map"><svg class="pipeline-edges" id="pipeline-edges" aria-hidden="true"></svg>${lanes}</div><div class="pipeline-map-legend"><span><i class="legend-analysis"></i>질의 분석</span><span><i class="legend-retrieval"></i>검색·근거</span><span><i class="legend-answer"></i>답변</span><span>✓ 파라미터 설정 · 회색은 표시명만 변경</span></div></div>${pipelineSettingsPanel()}</section>`;}
 function drawPipelineEdges(){if(state.view!=='pipeline'||!state.pipelineGraph)return;const map=document.getElementById('pipeline-map'),svg=document.getElementById('pipeline-edges');if(!map||!svg)return;const box=map.getBoundingClientRect();svg.setAttribute('viewBox',`0 0 ${box.width} ${box.height}`);svg.innerHTML=`<defs><marker id="pipeline-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="5" markerHeight="5" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z"></path></marker></defs>`;(state.pipelineGraph.edges||[]).forEach(edge=>{const from=map.querySelector(`[data-node-id="${CSS.escape(edge.source)}"]`),to=map.querySelector(`[data-node-id="${CSS.escape(edge.target)}"]`);if(!from||!to)return;const a=from.getBoundingClientRect(),b=to.getBoundingClientRect(),x1=a.right-box.left,y1=a.top+a.height/2-box.top,x2=b.left-box.left,y2=b.top+b.height/2-box.top,dx=Math.max(26,(x2-x1)*.45);const path=document.createElementNS('http://www.w3.org/2000/svg','path');path.setAttribute('d',`M ${x1} ${y1} C ${x1+dx} ${y1}, ${x2-dx} ${y2}, ${x2} ${y2}`);path.setAttribute('marker-end','url(#pipeline-arrow)');const title=document.createElementNS('http://www.w3.org/2000/svg','title');title.textContent=edge.label||'';path.appendChild(title);svg.appendChild(path);});}
-async function refreshPipeline(notify=true){try{state.pipelineGraph=await api('/api/admin-ui/pipeline/graph');if(state.pipelineNode)state.pipelineNode=(state.pipelineGraph.nodes||[]).find(node=>node.id===state.pipelineNode.id&&node.configurable)||null;render();if(notify)toast('현재 챗봇 파이프라인 설정 구조를 다시 읽었습니다.');}catch(e){toast(e.message,true);}}
+async function refreshPipeline(notify=true){try{const readAt=Date.now(),[graph,health]=await Promise.all([api(`/api/admin-ui/pipeline/graph?read_at=${readAt}`),api(`/api/health?read_at=${readAt}`)]);if(health?.pipeline)graph.pipeline_name=health.pipeline;if(health?.runtime_build)graph.runtime_build=health.runtime_build;state.pipelineGraph=graph;if(state.pipelineNode)state.pipelineNode=(state.pipelineGraph.nodes||[]).find(node=>node.id===state.pipelineNode.id&&node.configurable)||null;render();if(notify)toast('현재 챗봇 파이프라인 설정 구조를 다시 읽었습니다.');}catch(e){toast(e.message,true);}}
 function selectPipelineNode(id){const node=(state.pipelineGraph?.nodes||[]).find(item=>item.id===id);if(!node)return;state.pipelineNode=node;render();}
 async function savePipelineLabel(){const node=state.pipelineNode,label=document.getElementById('pipeline-label')?.value?.trim();if(!node||!label)return toast('표시명을 입력해 주세요.',true);try{const out=await api('/api/admin-ui/pipeline/labels',{method:'PUT',body:JSON.stringify({labels:{[node.id]:label}})});state.pipelineGraph=out.graph;state.pipelineNode=(out.graph.nodes||[]).find(item=>item.id===node.id);render();toast('파이프라인 표시명을 저장했습니다.');}catch(e){toast(e.message,true);}}
 async function resetPipelineLabel(){const node=state.pipelineNode;if(!node)return;try{const out=await api(`/api/admin-ui/pipeline/labels/${encodeURIComponent(node.id)}`,{method:'DELETE'});state.pipelineGraph=out.graph;state.pipelineNode=(out.graph.nodes||[]).find(item=>item.id===node.id);render();toast('기본 표시명으로 복구했습니다.');}catch(e){toast(e.message,true);}}
@@ -295,7 +300,7 @@ async function refreshMonitoring(){state.monitorHours=num(document.getElementByI
 function jobsView(){return `${heading('JOB AUDIT','챗봇 작업 이력','각 요청의 실제 토큰·체감 응답시간과 캐시 사용 여부를 확인합니다. 캐시 HIT는 이번 요청의 LLM 사용량이 0입니다.')}<section class="panel jobs-panel">${jobsTable(state.jobs)}</section>`;}
 function jobsTable(rows){return rows.length?`<div class="job-table detailed"><div class="job-row job-header"><span>Job</span><span>질문</span><span>세션</span><span>상태</span><span>단계</span><span>캐시</span><span>이번 토큰</span><span>이번 응답시간</span><span>진행률</span><span>시작</span></div>${rows.map(j=>{const t=j.telemetry||{};const cache=t.cache_hit?`<span class="cache-cell">${cacheBadge(true)}<small>절감 ${durationText(t.saved_latency_ms)} · ${Number(t.saved_tokens||0).toLocaleString()} 토큰</small></span>`:`<span class="cache-cell">${cacheBadge(false)}<small>실제 LLM 호출</small></span>`;return `<div class="job-row"><b>${esc(String(j.job_id).slice(0,12))}</b><span title="${attr(j.question||'')}">${esc(String(j.question||'').slice(0,28))}</span><span>${esc(String(j.session_id||'').slice(0,10))}</span>${statusBadge(j.status)}<span>${esc(j.stage)}</span>${cache}<span class="request-metric"><b>${Number(t.total_tokens||0).toLocaleString()}</b><small>이번 요청</small></span><span class="request-metric"><b>${durationText(t.request_latency_ms)}</b><small>이번 요청</small></span><span>${j.progress||0}%</span><span>${fmtDate(j.created_at)}</span></div>`;}).join('')}</div><div class="monitor-note">캐시 HIT는 절감 시간·토큰을 행 안에 바로 표시합니다. 이번 토큰과 응답시간은 캐시 조회 요청의 실제 사용량입니다.</div>`:'<div class="empty-list">작업 이력이 없습니다.</div>';}
 
-function render(){const sameView=state.renderedView===state.view,oldMap=root.querySelector('.pipeline-map-wrap'),oldNav=root.querySelector('.sidebar nav');if(oldMap)state.pipelineScroll={left:oldMap.scrollLeft,top:oldMap.scrollTop};if(oldNav)state.sidebarScroll=oldNav.scrollTop;if(sameView)state.pageScroll=window.scrollY;else state.pageScroll=0;const views={dashboard,monitoring:monitoringView,pipeline:pipelineView,guardrails:guardrailsView,prompts:promptsView,runtime:runtimeView,evaluation:evaluationViewV2,data:dataView,keys:keysView,apply:applyViewV2,chat:chatView,jobs:jobsView};root.innerHTML=shell(views[state.view]());state.renderedView=state.view;requestAnimationFrame(()=>{const nav=root.querySelector('.sidebar nav');if(nav)nav.scrollTop=state.sidebarScroll||0;window.scrollTo({top:state.pageScroll||0,left:0,behavior:'auto'});if(state.view==='pipeline'){const map=root.querySelector('.pipeline-map-wrap');if(map){map.scrollLeft=state.pipelineScroll.left||0;map.scrollTop=state.pipelineScroll.top||0;}drawPipelineEdges();}});}
+function render(){const sameView=state.renderedView===state.view,oldMap=root.querySelector('.pipeline-map-wrap'),oldNav=root.querySelector('.sidebar nav');if(oldMap)state.pipelineScroll={left:oldMap.scrollLeft,top:oldMap.scrollTop};if(oldNav)state.sidebarScroll=oldNav.scrollTop;if(sameView)state.pageScroll=window.scrollY;else state.pageScroll=0;const views={dashboard,monitoring:monitoringView,pipeline:pipelineView,guardrails:guardrailsView,prompts:promptsView,runtime:runtimeView,evaluation:evaluationViewV2,data:dataIngestionView,data_ingest:dataIngestionView,data_library:dataLibraryView,keys:keysView,apply:applyViewV2,chat:chatView,jobs:jobsView};const selectedView=views[state.view]||dashboard;root.innerHTML=shell(selectedView());normalizeParameterTerminology(root);state.renderedView=state.view;requestAnimationFrame(()=>{const nav=root.querySelector('.sidebar nav');if(nav)nav.scrollTop=state.sidebarScroll||0;window.scrollTo({top:state.pageScroll||0,left:0,behavior:'auto'});if(state.view==='pipeline'){const map=root.querySelector('.pipeline-map-wrap');if(map){map.scrollLeft=state.pipelineScroll.left||0;map.scrollTop=state.pipelineScroll.top||0;}drawPipelineEdges();}});}
 async function refreshAll(silent=false){state.busy=true;render();try{const [summary,config,indices,jobs,monitoring,datasets,evalJobs,draft,keyStatus,prompts,pipelineGraph]=await Promise.all([api('/api/admin-ui/summary'),api('/api/admin-ui/runtime-config'),api('/api/admin-ui/indices'),api('/api/admin-ui/jobs?limit=100'),api(`/api/admin-ui/monitoring?hours=${state.monitorHours}&limit=200`),api('/api/admin-ui/evaluations/datasets'),api('/api/admin-ui/evaluations/jobs'),api('/api/admin-ui/draft'),api('/api/admin-ui/api-keys/status'),api('/api/admin-ui/prompts'),api('/api/admin-ui/pipeline/graph')]);Object.assign(state,{summary,config,indices:indices.items||[],jobs:jobs.items||[],monitoring,datasets:datasets.items||[],evalJobs:evalJobs.items||[],draft,keyStatus,prompts,pipelineGraph});if(!silent)toast('실제 런타임 상태를 새로고침했습니다.');}catch(e){toast(e.message,true);}finally{state.busy=false;render();}}
 async function searchIndex(notify=true){if(!state.index)return;state.busy=true;render();try{const out=await api('/api/admin-ui/search',{method:'POST',body:JSON.stringify({index:state.index,query:state.query,size:40,offset:0})});state.hits=out.items||[];state.hitTotal=out.total||0;state.document=null;if(notify)toast(`${state.hitTotal}건을 찾았습니다.`);}catch(e){toast(e.message,true);}finally{state.busy=false;render();}}
 async function loadDocument(id){try{state.document=await api(`/api/admin-ui/documents/${encodeURIComponent(state.index)}/${encodeURIComponent(id)}`);render();}catch(e){toast(e.message,true);}}
@@ -336,7 +341,7 @@ function loadChatDraft(){state.chatCandidateSource='draft';state.chatCustomConfi
 async function runChatCompare(){const q=state.chatQuestion.trim();if(!q)return toast('질문을 입력해 주세요.',true);const useEvaluation=state.chatCandidateSource==='evaluation'&&Boolean(state.evalResult?.candidate_config),source=state.chatCandidateSource==='custom'?'직접 입력 개선안':useEvaluation?'최근 검색평가 개선안':'저장된 개선안',candidate=state.chatCandidateSource==='custom'?readConfig('chat'):chatCandidateConfig(),active=state.draft?.active_config||{},hasParameterChange=Object.keys(active).some(key=>active[key]!==candidate[key]),hasPromptChange=(state.draft?.prompt_changes||[]).length>0;if(!hasParameterChange&&!hasPromptChange)return toast('선택한 개선안에 현재안과 다른 파라미터·프롬프트가 없습니다. 다른 개선안을 선택하거나 직접 값을 변경해 주세요.',true);if(state.chatCandidateSource==='custom')state.chatCustomConfig={...candidate};state.chatLastCandidate={config:{...candidate},source};const temporarilySelectCandidate=state.chatCandidateSource!=='draft',savedCandidate={...(state.draft?.candidate_config||active)},savedHasParameterChange=Object.keys(active).some(key=>active[key]!==savedCandidate[key]);state.chatCompareBusy=true;state.chatComparison=null;render();try{if(temporarilySelectCandidate)state.draft=await api('/api/admin-ui/draft/config',{method:'PUT',body:JSON.stringify({values:candidate})});state.chatComparison=await api('/api/admin-ui/chat/compare',{method:'POST',body:JSON.stringify({question:q,candidate_config:candidate,candidate_source:source})});state.chatLastCandidate={config:{...(state.chatComparison.candidate_config||candidate)},source:state.chatComparison.candidate_source||source};toast('현재안과 선택한 개선안의 최종 답변 비교를 완료했습니다.');}catch(e){toast(e.message,true);}finally{if(temporarilySelectCandidate){try{state.draft=savedHasParameterChange?await api('/api/admin-ui/draft/config',{method:'PUT',body:JSON.stringify({values:savedCandidate})}):await api('/api/admin-ui/draft/config',{method:'DELETE'});}catch(e){toast('비교 전 개선안 복원에 실패했습니다. 파라미터 화면에서 확인해 주세요.',true);}}state.chatCompareBusy=false;render();}}
 
 root.addEventListener('click',e=>{const target=e.target.closest('button[data-view]');if(!target)return;const group=navGroups.find(row=>row.items?.includes(String(target.dataset.view||'')));if(group){navExpansion[group.id]=true;localStorage.setItem('kdic-admin-nav-groups',JSON.stringify(navExpansion));}},true);
-root.addEventListener('click',e=>{const toggle=e.target.closest('button[data-action="toggle-nav-group"]');if(!toggle)return;const id=String(toggle.dataset.id||'');if(!['enhancement','security'].includes(id))return;navExpansion[id]=!isNavGroupOpen({id,items:id==='enhancement'?['pipeline','prompts','runtime','evaluation','chat']:['guardrails','keys']});localStorage.setItem('kdic-admin-nav-groups',JSON.stringify(navExpansion));e.stopImmediatePropagation();render();},true);
+root.addEventListener('click',e=>{const toggle=e.target.closest('button[data-action="toggle-nav-group"]');if(!toggle)return;const id=String(toggle.dataset.id||''),group=navGroups.find(row=>row.id===id&&Array.isArray(row.items));if(!group)return;navExpansion[id]=!isNavGroupOpen(group);localStorage.setItem('kdic-admin-nav-groups',JSON.stringify(navExpansion));e.stopImmediatePropagation();render();},true);
 root.addEventListener('click',e=>{const button=e.target.closest('button[data-action="reset-independent-eval-policy"]');if(!button)return;state.evalPolicy={evaluation_depth:20,baseline_metric_ks:{...standardMetricKs},candidate_metric_ks:{...standardMetricKs},curve_ks:[1,3,5,10,20]};render();});
 root.addEventListener('click',e=>{if(e.target.closest('button[data-action="stage-manual-add"]'))stageManualAdd();});
 root.addEventListener('click',e=>{if(e.target.closest('button[data-action="reset-defaults"]'))resetDefaults();});
@@ -399,7 +404,7 @@ function quickParameterDrawer(){const open=!!state.quickParameterPanelOpen;retur
 function pipelineSettingsPanel(){const node=state.pipelineNode,values=pipelineParameterValues(),openButton='<button class="secondary-btn pipeline-quick-trigger" data-action="toggle-quick-parameters">⚡ 빠른 파라미터 수정</button>';if(!node)return `<aside class="panel pipeline-setting-panel"><span class="section-kicker">PIPELINE BLOCK</span><h2>블록을 선택하세요</h2><p>블록에서는 관련 파라미터만 확인·수정합니다. 전체 파라미터 조정과 이름 저장은 오른쪽 빠른 수정 패널에서 진행하세요.</p><div class="pipeline-locked-note">회색 블록은 표시명만 변경할 수 있고 실제 코드는 로컬 개발 환경에서 관리합니다.</div><div class="pipeline-setting-actions" style="margin-top:14px">${openButton}</div></aside>`;const common=`<span class="section-kicker">${node.configurable?'PARAMETER WORKSPACE':'DISPLAY CUSTOM'}</span><h2>${esc(node.label)}</h2><p>${esc(node.description)}</p>${pipelineLabelEditor(node)}`;if(node.setting_view==='runtime'){const fields=(node.settings||[]).filter(key=>key!=='query_fusion_rrf_k'&&key!=='final_top_k');return `<aside class="panel pipeline-setting-panel">${common}<div class="setting-fields">${fields.map(key=>pipelineSettingField(key,values[key])).join('')}</div><p class="muted-note">이 블록의 변경값은 빠른 수정 패널에도 이어집니다. Top-K는 검색 비교 평가·챗봇 테스트에서 조정하고, 설정 이름 저장은 오른쪽 하단에서 한 번만 실행하세요.</p><div class="pipeline-setting-actions">${openButton}<button class="secondary-btn" data-view="evaluation">데이터셋 검색 비교 →</button><button class="secondary-btn" data-view="chat">최종 답변 비교 →</button></div></aside>`;}const target=node.setting_view;if(target==='prompts'||target==='guardrails')return `<aside class="panel pipeline-setting-panel">${common}<div class="pipeline-locked-note">${target==='prompts'?'이 단계의 동작은 버전 관리되는 답변 프롬프트에서 수정합니다.':'이 단계의 동작은 가드레일 규칙에서 수정합니다.'}</div><div class="pipeline-setting-actions"><button class="primary-btn" data-view="${attr(target)}">${target==='prompts'?'프롬프트 초안 열기':'가드레일 열기'} →</button>${openButton}</div></aside>`;return `<aside class="panel pipeline-setting-panel">${common}<div class="pipeline-locked-note">이 블록은 표시명만 변경할 수 있습니다. 동작 변경은 로컬에서 코드 검증 후 배포해야 합니다.</div><div class="pipeline-setting-actions" style="margin-top:14px">${openButton}</div></aside>`;}
 function pipelineView(){const graph=state.pipelineGraph;if(!graph)return `${heading('PIPELINE STUDIO','동적 파이프라인','현재 챗봇 런타임 구조를 불러오고 있습니다.')}<section class="panel empty-list">파이프라인 정보를 불러오는 중입니다.</section>`;const byId=Object.fromEntries((graph.nodes||[]).map(node=>[node.id,node]));const lanes=pipelineLanes.map(([id,label,ids])=>`<section class="pipeline-lane lane-${id}"><header><span>${label}</span><small>${ids.filter(nodeId=>byId[nodeId]).length}단계</small></header><div>${ids.map(nodeId=>byId[nodeId]?pipelineNodeCard(byId[nodeId]):'').join('')}</div></section>`).join('');const actions='<button class="secondary-btn" data-action="toggle-quick-parameters">⚡ 빠른 파라미터 수정</button><button class="secondary-btn" data-action="refresh-pipeline">↻ 런타임 다시 읽기</button>';return `${heading('PIPELINE STUDIO','운영 파이프라인 설정','코드는 로컬에서 관리하고, 운영 서버에서는 안전하게 허용된 설정과 모든 블록의 표시명만 변경합니다.',actions)}<section class="pipeline-flow-steps"><button data-view="pipeline"><b>1. 블록 선택</b><small>표시명·관련 설정</small></button><button data-action="toggle-quick-parameters"><b>2. 빠른 수정</b><small>전체 파라미터·이름 저장</small></button><button data-view="evaluation"><b>3. 검색 비교</b><small>평가데이터셋·차트</small></button><button data-view="chat"><b>4. 최종 답변 비교</b><small>현재안·개선안</small></button><button data-view="apply"><b>5. 반영·롤백</b><small>통합 변경 이력</small></button></section><div class="pipeline-runtime-strip"><span><b>런타임</b>${esc(graph.pipeline_name)}</span><span><b>설정 가능 블록</b>${(graph.nodes||[]).filter(n=>n.configurable).length}개</span><span><b>구조 해시</b><code>${esc(graph.fingerprint)}</code></span><span class="readonly-tag">서버 코드 편집 차단</span></div><section class="pipeline-workspace"><div class="panel pipeline-map-wrap"><div class="pipeline-map" id="pipeline-map"><svg class="pipeline-edges" id="pipeline-edges" aria-hidden="true"></svg>${lanes}</div><div class="pipeline-map-legend"><span><i class="legend-analysis"></i>질의 분석</span><span><i class="legend-retrieval"></i>검색·근거</span><span><i class="legend-answer"></i>답변</span><span>✓ 파라미터 설정 · 회색은 표시명만 변경</span></div></div>${pipelineSettingsPanel()}</section>${quickParameterDrawer()}`;}
 root.addEventListener('click',e=>{const t=e.target.closest('button[data-action="toggle-quick-parameters"]');if(!t)return;e.stopImmediatePropagation();capturePipelineParameters();state.quickParameterPanelOpen=!state.quickParameterPanelOpen;render();},true);
-function workspaceShortcuts(current){const items=[['pipeline','파이프라인'],['runtime','파라미터 창고'],['evaluation','검색 비교 평가'],['chat','챗봇 테스트'],['apply','반영 센터']];return `<nav class="workspace-shortcuts" aria-label="검색 설정 작업 바로가기"><span>바로가기</span>${items.map(([view,label])=>`<button class="secondary-btn ${view===current?'active':''}" data-view="${view}" ${view===current?'disabled':''}>${label}</button>`).join('')}</nav>`;}
+function workspaceShortcuts(current){const items=[['pipeline','파이프라인'],['runtime','파라미터 설정'],['evaluation','검색 비교 평가'],['chat','챗봇 테스트'],['apply','반영 센터']];return `<nav class="workspace-shortcuts" aria-label="검색 설정 작업 바로가기"><span>바로가기</span>${items.map(([view,label])=>`<button class="secondary-btn ${view===current?'active':''}" data-view="${view}" ${view===current?'disabled':''}>${label}</button>`).join('')}</nav>`;}
 const pipelineViewBeforeShortcutCleanup=pipelineView;
 pipelineView=function(){return pipelineViewBeforeShortcutCleanup().replace(/<section class="pipeline-flow-steps">[\s\S]*?<\/section>/,'').replace('<div class="pipeline-runtime-strip">',`${workspaceShortcuts('pipeline')}<div class="pipeline-runtime-strip">`);};
 const runtimeViewBeforeShortcuts=runtimeView;
@@ -505,9 +510,190 @@ resultSummaryStyle.textContent=`
 document.head.appendChild(resultSummaryStyle);
 function resultDecision(result,definitions){const rows=result.details||[],counts={improved:0,degraded:0,unchanged:0};rows.forEach(row=>counts[detailChange(row,definitions).verdict]++);const total=rows.length||1,up=Math.round(counts.improved/total*1000)/10,down=Math.round(counts.degraded/total*1000)/10,same=Math.round(counts.unchanged/total*1000)/10;const positives=definitions.filter(def=>Number(result.delta?.[def.name])>0).map(def=>def.label),negatives=definitions.filter(def=>Number(result.delta?.[def.name])<0).map(def=>def.label),caution=counts.degraded>counts.improved,kChanged=definitions.some(def=>String(def.baseline_k)!==String(def.candidate_k));return `<article class="result-decision"><span class="section-kicker decision-kicker">QUESTION-LEVEL SCORE DISTRIBUTION</span><div class="decision-title"><h3>질문별 종합점수 변화</h3><p>평가데이터셋의 Gold 청크를 기준으로 계산한 8개 검색지표의 질문별 평균 변화입니다. ‘악화’는 Gold 청크를 전혀 못 찾았다는 뜻이 아닙니다.</p></div><div class="decision-body"><section class="decision-column"><h4>${caution?'추가 검토 필요':'개선안 우세'}</h4><div class="decision-counts"><strong class="up">${counts.improved}</strong><span>상승</span><strong class="down">${counts.degraded}</strong><span>하락</span></div><p class="decision-guide">질문 수 기준으로는 ${caution?'하락 문항이 더 많습니다.':'상승 문항이 더 많습니다.'}</p></section><section class="decision-column"><h4>질문별 분포</h4><div class="change-stack" aria-label="질문별 종합점수 상승 하락 동일 비율"><i class="up" style="width:${up}%"></i><i class="down" style="width:${down}%"></i><i class="same" style="width:${same}%"></i></div><div class="change-legend"><span><b class="rank-up">상승 ${counts.improved}건</b> · ${up}%</span><span><b class="rank-down">하락 ${counts.degraded}건</b> · ${down}%</span><span><b>동일 ${counts.unchanged}건</b> · ${same}%</span></div></section><section class="decision-column"><h4>관리자 해석</h4><p class="decision-guide"><strong>범위 지표:</strong> ${esc(positives.join(', ')||'상승 없음')} 상승<br><strong>순도 지표:</strong> ${esc(negatives.join(', ')||'하락 없음')} 하락</p><p class="decision-guide">Top-K가 커지면 Gold 청크를 포함할 기회는 늘지만, 비정답 청크도 함께 늘어 Precision·F1이 하락할 수 있습니다.${kChanged?'<span class="warning">현재안·개선안의 지표 K가 다르므로, 이 결과는 파라미터 효과와 평가 범위 확대 효과가 함께 섞인 결과입니다.</span>':''}</p></section></div></article>`;}
 function llmReportPanel(result){const report=result?.llm_report,summary=report?.summary,counts=summary?.counts||{},rates=summary?.rates||{},defs=metricDefinitions(result),negative=defs.filter(def=>Number(result.delta?.[def.name])<0).map(def=>def.label),positive=defs.filter(def=>Number(result.delta?.[def.name])>0).map(def=>def.label),needsReview=(counts.degraded||0)>(counts.improved||0),kDifferent=defs.some(def=>String(def.baseline_k)!==String(def.candidate_k));return `<section class="llm-report-panel refined-report"><div class="panel-head"><div><span class="section-kicker">QUALITY REVIEW REPORT</span><h3>Gold 청크 기준 검색 품질 검토 리포트</h3><p>검색 결과가 평가데이터셋의 Gold 청크를 얼마나 정확히 찾았는지로 계산한 결과입니다.</p></div><button class="primary-btn" data-action="generate-eval-report" ${state.evalReportBusy||!state.evalJob?.job_id?'disabled':''}>${state.evalReportBusy?'LLM 분석 중…':report?'리포트 다시 생성':'LLM 리포트 생성'}</button></div><div class="report-lead"><article class="report-conclusion ${needsReview?'needs-review':'ready'}"><span class="section-kicker">EXECUTIVE SUMMARY</span><h4>${needsReview?'개선안은 즉시 반영보다 원인 검토가 우선입니다':'개선안은 반영 후보로 검토할 수 있습니다'}</h4><p>전체 ${summary?.question_count||result?.details?.length||0}개 평가 질문 중 <strong class="rank-up">${counts.improved||0}건 (${pct(rates.improved)})</strong>은 질문별 종합점수가 상승했고, <strong class="rank-down">${counts.degraded||0}건 (${pct(rates.degraded)})</strong>은 하락했습니다. 이는 Gold 청크를 전혀 못 찾았다는 뜻이 아니라, Gold 청크로 계산한 여러 지표의 평균 변화입니다.</p><ul><li><strong>상승:</strong> ${esc(positive.join(', ')||'없음')}</li><li><strong>하락:</strong> ${esc(negative.join(', ')||'없음')}</li><li>${kDifferent?'현재안과 개선안의 지표 K가 달라, 설정 효과와 더 넓어진 평가 범위의 효과가 함께 포함됩니다.':'동일 K 기준 비교이므로 지표 차이는 검색 설정 변화에 더 직접적으로 연결됩니다.'}</li></ul></article><article class="report-method"><span class="section-kicker">MEASUREMENT METHOD</span><h4>개선·악화는 무엇으로 판단했나요?</h4><ol><li>각 질문의 <strong>gold_chunk_ids</strong>를 정답 근거로 사용합니다.</li><li>현재안·개선안이 가져온 청크와 Gold 청크의 일치 여부로 Hit, Recall, MRR, MAP, Complete, nDCG, Precision, F1을 계산합니다.</li><li>질문별 적용 가능 지표의 평균 변화가 ±0.5%p를 넘으면 상승·하락으로 분류합니다.</li></ol></article></div>${report?`<div class="llm-report-summary"><div class="llm-report-stat"><small>전체 평가 질문</small><strong>${summary.question_count||0}건</strong></div><div class="llm-report-stat good"><small>질문별 종합점수 상승</small><strong>${counts.improved||0}건 · ${pct(rates.improved)}</strong></div><div class="llm-report-stat bad"><small>질문별 종합점수 하락</small><strong>${counts.degraded||0}건 · ${pct(rates.degraded)}</strong></div><div class="llm-report-stat"><small>동일</small><strong>${counts.unchanged||0}건 · ${pct(rates.unchanged)}</strong></div></div><section class="report-metric-section"><span class="section-kicker">METRIC EVIDENCE</span><h4>전체 지표 변화</h4><div class="eval-metric-table"><div class="eval-row eval-head"><span>지표</span><span>현재안</span><span>개선안</span><span>변화</span></div>${reportMetricRows(summary)}</div></section><div class="llm-report-layout">${reportCaseSection('대표 상승 사례',summary.representative_improvements,'improved')}${reportCaseSection('대표 하락 사례',summary.representative_degradations,'degraded')}</div><details class="report-raw"><summary>LLM 해석 원문 보기</summary><pre class="json-view">${esc(report.report_markdown||'')}</pre></details><div class="llm-report-meta"><span>판정 기준: ${esc(summary.classification_rule||'—')}</span><span>모델 ${esc(report.model)}</span><span>${Number(report.latency_ms||0).toFixed(0)}ms</span><span>총 ${Number(report.usage?.total_tokens||0).toLocaleString()} 토큰</span><span>${fmtDate(report.generated_at)}</span></div>`:`<div class="llm-report-empty">이 화면의 평가 기준과 질문별 진단은 이미 확인할 수 있습니다. LLM 리포트를 생성하면 대표 상승·하락 사례와 권장 설정 해석이 이 아래에 추가됩니다.</div>`}</section>`;}
+/* URL 재수집·재청킹은 청크 관리에서만 다룬다. 기존 운영 청크는 초안 승인 전까지 바뀌지 않는다. */
+const rechunkStyle=document.createElement('style');
+rechunkStyle.textContent=`
+.rechunk-panel{padding:20px;margin:0 0 14px}.rechunk-panel .panel-head{align-items:flex-start}.rechunk-panel .panel-head h2{margin:4px 0 7px;font-size:17px}.rechunk-panel .panel-head p,.rechunk-note{margin:0;color:var(--muted);font-size:10px;line-height:1.65}.rechunk-form{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:10px;align-items:end;margin-top:15px}.rechunk-form label{display:grid;gap:6px}.rechunk-form label span{font-size:9px;font-weight:800;color:var(--muted)}.rechunk-status{display:flex;align-items:center;gap:9px;flex-wrap:wrap;margin-top:14px;padding:11px 12px;border:1px solid var(--line);border-radius:10px;background:var(--bg);font-size:10px}.rechunk-status>span:last-child{color:var(--muted)}.rechunk-error{margin:10px 0 0;color:var(--red);font-size:10px;line-height:1.6;white-space:pre-wrap}.rechunk-preview{margin-top:12px;padding:13px;border:1px solid var(--line);border-radius:10px;background:var(--soft)}.rechunk-preview p,.rechunk-preview small{margin:0;color:var(--muted);font-size:10px;line-height:1.65}.rechunk-preview p b{color:var(--ink)}.rechunk-preview ul{display:grid;gap:7px;margin:11px 0;padding:0;list-style:none}.rechunk-preview li{display:grid;grid-template-columns:minmax(160px,.9fr) 1.6fr;gap:10px;padding:8px 10px;border-radius:7px;background:var(--panel);font-size:10px}.rechunk-preview li b{color:var(--blue);overflow-wrap:anywhere}.rechunk-preview li span{color:var(--muted);overflow-wrap:anywhere}.rechunk-draft-panel{padding:16px;margin:0 0 14px}.rechunk-draft-panel h2{margin:4px 0 7px;font-size:14px}.rechunk-draft-list{display:grid;gap:7px;list-style:none;padding:0;margin:10px 0 0}.rechunk-draft-list li{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:9px;padding:9px 10px;border:1px solid var(--line);border-radius:8px;background:var(--bg);font-size:10px}.rechunk-draft-list small{grid-column:1/-1;color:var(--muted);overflow-wrap:anywhere}@media(max-width:760px){.rechunk-form{grid-template-columns:1fr}.rechunk-preview li{grid-template-columns:1fr}.rechunk-draft-list li{grid-template-columns:1fr}}
+`;
+document.head.appendChild(rechunkStyle);
+function rechunkPanel(){const sources=state.chunkSources||[],job=state.rechunkJob,selected=job?.source_url||state.rechunkSelectedUrl||sources[0]?.source_url||'',options=sources.map(row=>`<option value="${attr(row.source_url)}" ${row.source_url===selected?'selected':''}>${esc(row.sample_title||row.document_ids?.join(', ')||'제목 없음')} · ${num(row.chunk_count)}개 청크</option>`).join(''),preview=(job?.preview_chunks||[]).slice(0,6).map(row=>`<li><b>${esc(row.chunk_id)}</b><span>${esc(row.section_title||row.title||'제목 없음')} · ${String(row.content||'').length.toLocaleString()}자</span></li>`).join(''),status=job?`<div class="rechunk-status">${statusBadge(job.status==='ready'?'done':job.status)}<b>${esc(job.stage||'')}</b><span>기존 ${num(job.old_chunk_count)}개 → 새 ${num(job.new_chunk_count)}개</span></div>${job.error?`<p class="rechunk-error">${esc(job.error)}</p>`:''}${job.status==='ready'?`<div class="rechunk-preview"><p><b>새 청크 ID는 기존 문서 ID를 유지해</b> <code>${esc((job.preview_chunks||[])[0]?.chunk_id||'')}</code> 형식으로 발급됩니다.</p><ul>${preview}</ul>${(job.preview_chunks||[]).length>6?`<small>외 ${(job.preview_chunks||[]).length-6}개 청크도 교체 초안에 함께 포함됩니다.</small>`:''}</div>`:''}`:'<p class="rechunk-note">URL별 기존 청크와 문서 ID를 기준으로 범위를 고정합니다. 현재 청크·Elasticsearch 인덱스는 아직 바뀌지 않습니다.</p>';return `<section class="panel rechunk-panel"><div class="panel-head"><div><span class="section-kicker">URL REFRESH</span><h2>기존 URL 재수집·재청킹</h2><p>사이트 내용이 바뀐 URL만 다시 수집해 새 청크를 미리 확인합니다. 승인 전에는 운영 검색에 반영되지 않습니다.</p></div><button class="secondary-btn" data-action="refresh-chunk-sources">↻ URL 목록 새로고침</button></div>${sources.length?`<div class="rechunk-form"><label><span>현재 수집 URL</span><select id="rechunk-source">${options}</select></label><button class="primary-btn" data-action="start-rechunk" ${state.rechunkBusy?'disabled':''}>${state.rechunkBusy?'재수집 요청 중…':'재수집·재청킹 미리보기'}</button>${job?`<button class="secondary-btn" data-action="refresh-rechunk">상태 확인</button>`:''}</div>${status}${job?.status==='ready'?`<div class="form-actions"><p>기존 ${num(job.old_chunk_count)}개를 제거 초안에, 새 ${num(job.new_chunk_count)}개를 추가 초안에 넣습니다.</p><button class="danger-btn" data-action="stage-rechunk" ${job.staged?'disabled':''}>${job.staged?'교체 초안 생성됨':'이 결과로 교체 초안 만들기'}</button></div>`:''}`:'<div class="empty-list">source_url 메타데이터가 있는 운영 청크를 찾지 못했습니다. 기존 데이터에 URL을 보존한 뒤 사용할 수 있습니다.</div>'}</section>`;}
+async function refreshChunkSources(notify=true){try{const out=await api('/api/admin-ui/chunk-sources');state.chunkSources=out.items||[];if(notify)toast(`재청킹 가능한 URL ${state.chunkSources.length}개를 불러왔습니다.`);render();}catch(e){toast(e.message,true);}}
+async function startRechunk(){const source_url=document.getElementById('rechunk-source')?.value||'';if(!source_url)return toast('재수집할 URL을 선택해 주세요.',true);state.rechunkSelectedUrl=source_url;if(!confirm('선택 URL만 다시 수집하고 재청킹합니다. 현재 운영 청크는 바뀌지 않습니다. 계속할까요?'))return;state.rechunkBusy=true;render();try{state.rechunkJob=await api('/api/admin-ui/chunk-sources/rechunk',{method:'POST',body:JSON.stringify({source_url})});toast('재수집·재청킹을 시작했습니다. 완료되면 상태 확인으로 미리보기를 검토하세요.');}catch(e){toast(e.message,true);}finally{state.rechunkBusy=false;render();}}
+async function refreshRechunk(){const id=state.rechunkJob?.job_id;if(!id)return;try{state.rechunkJob=await api(`/api/admin-ui/chunk-sources/rechunk/${encodeURIComponent(id)}`);render();if(state.rechunkJob.status==='ready')toast(`미리보기 준비 완료: 기존 ${state.rechunkJob.old_chunk_count}개 → 새 ${state.rechunkJob.new_chunk_count}개 청크`);else if(state.rechunkJob.status==='failed')toast(state.rechunkJob.error||'재청킹에 실패했습니다.',true);else toast('재수집·재청킹이 진행 중입니다.');}catch(e){toast(e.message,true);}}
+async function stageRechunk(){const id=state.rechunkJob?.job_id;if(!id||state.rechunkJob?.status!=='ready')return toast('준비된 재청킹 미리보기가 없습니다.',true);if(!confirm(`기존 ${state.rechunkJob.old_chunk_count}개 청크를 제거 초안에 넣고 새 ${state.rechunkJob.new_chunk_count}개 청크를 추가 초안에 만들까요? 실제 반영은 반영센터에서 별도로 승인해야 합니다.`))return;try{const out=await api(`/api/admin-ui/chunk-sources/rechunk/${encodeURIComponent(id)}/stage`,{method:'POST',body:'{}'});state.draft=out.draft;state.rechunkJob={...state.rechunkJob,staged:true};render();toast('URL 교체 초안을 만들었습니다. 반영센터에서 확인한 뒤 통합 반영하세요.');}catch(e){toast(e.message,true);}}
+const dataViewBeforeRechunk=dataView;
+dataView=function(){return dataViewBeforeRechunk().replace('<section class="panel add-chunk">',`${rechunkPanel()}<section class="panel add-chunk">`);};
+const applyViewBeforeRechunk=applyViewV2;
+applyViewV2=function(){const rows=state.draft?.rechunk||[],panel=rows.length?`<section class="panel rechunk-draft-panel"><span class="section-kicker">URL RECHUNK DRAFT</span><h2>URL 재청킹 교체 초안</h2><p class="rechunk-note">통합 운영 반영 전까지 챗봇 검색에는 적용되지 않습니다.</p><ul class="rechunk-draft-list">${rows.map(row=>`<li><b>${esc(row.document_ids?.join(', ')||'문서 ID 없음')}</b><span>${num(row.old_chunk_count)}개 → ${num(row.new_chunk_count)}개</span><small>${esc(row.source_url)}</small></li>`).join('')}</ul></section>`:'';return applyViewBeforeRechunk().replace('<section class="panel apply-panel">',`${panel}<section class="panel apply-panel">`);};
+const bootstrapBeforeRechunk=bootstrap;
+bootstrap=async function(){await bootstrapBeforeRechunk();if(!state.summary)return;try{const out=await api('/api/admin-ui/chunk-sources');state.chunkSources=out.items||[];render();}catch(e){toast(`URL 재청킹 목록을 불러오지 못했습니다: ${e.message}`,true);}};
+const refreshAllBeforeRechunk=refreshAll;
+refreshAll=async function(silent=false){await refreshAllBeforeRechunk(silent);if(!state.summary)return;try{const out=await api('/api/admin-ui/chunk-sources');state.chunkSources=out.items||[];render();}catch(e){if(!silent)toast(`URL 재청킹 목록을 불러오지 못했습니다: ${e.message}`,true);}};
+root.addEventListener('click',e=>{const button=e.target.closest?.('button[data-action]'),action=button?.dataset?.action;if(!['refresh-chunk-sources','start-rechunk','refresh-rechunk','stage-rechunk'].includes(action))return;e.preventDefault();e.stopImmediatePropagation();if(action==='refresh-chunk-sources')refreshChunkSources();if(action==='start-rechunk')startRechunk();if(action==='refresh-rechunk')refreshRechunk();if(action==='stage-rechunk')stageRechunk();},true);
+
+/* URL 재청킹 검수: 운영 반영 전 원문·청크·메타데이터를 모두 확인한다. */
+const rechunkReviewStyle=document.createElement('style');
+rechunkReviewStyle.textContent=`
+.rechunk-review{display:grid;gap:14px;margin-top:14px}.rechunk-review-banner{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:14px 16px;border:1px solid var(--line);border-left:4px solid var(--blue);border-radius:11px;background:var(--bg)}.rechunk-review-banner strong,.rechunk-review-banner small{display:block}.rechunk-review-banner strong{font-size:12px}.rechunk-review-banner small{margin-top:4px;color:var(--muted);font-size:9px}.rechunk-review-banner.blocked{border-left-color:var(--red);background:#fff4f5}.rechunk-review-banner.ready{border-left-color:var(--green);background:#f0faf5}.rechunk-review-counts{display:flex;gap:8px;flex-wrap:wrap}.rechunk-review-counts span{padding:5px 8px;border-radius:99px;background:var(--panel);border:1px solid var(--line);font-size:9px;font-weight:800}.rechunk-alert{padding:12px 14px;border-radius:10px;font-size:10px;line-height:1.65}.rechunk-alert b{display:block;margin-bottom:5px}.rechunk-alert ul{margin:0;padding-left:17px}.rechunk-alert.error{background:#fff1f3;border:1px solid #ffc4cc;color:#a52638}.rechunk-alert.warning{background:#fff8e7;border:1px solid #efd18b;color:#73520b}.rechunk-review details{border:1px solid var(--line);border-radius:10px;background:var(--panel);overflow:hidden}.rechunk-review summary{cursor:pointer;padding:12px 14px;font-size:11px;font-weight:800;list-style:none}.rechunk-review summary::-webkit-details-marker{display:none}.rechunk-review summary:after{content:'⌄';float:right;color:var(--blue)}.rechunk-review details[open] summary{border-bottom:1px solid var(--line);background:var(--soft)}.rechunk-review details[open] summary:after{content:'⌃'}.rechunk-parsed{padding:14px}.rechunk-document-meta{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-bottom:12px}.rechunk-document-meta span,.rechunk-meta-grid span{padding:8px 9px;border-radius:8px;background:var(--bg);font-size:9px;overflow-wrap:anywhere}.rechunk-document-meta small,.rechunk-meta-grid small{display:block;color:var(--muted);font-size:8px;margin-bottom:4px}.rechunk-source-text{max-height:460px;overflow:auto;margin:0;padding:13px;border-radius:9px;background:#0c1727;color:#d8e7fb;font:10px/1.7 Consolas,monospace;white-space:pre-wrap;word-break:break-word}.rechunk-compare{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.rechunk-compare-card{padding:11px;border:1px solid var(--line);border-radius:9px;background:var(--bg)}.rechunk-compare-card b,.rechunk-compare-card span{display:block}.rechunk-compare-card b{font-size:15px}.rechunk-compare-card span{margin-top:4px;color:var(--muted);font-size:8px}.rechunk-compare-list{display:grid;gap:0;max-height:340px;overflow:auto}.rechunk-compare-row{display:grid;grid-template-columns:minmax(180px,1fr) 90px 100px 100px;gap:9px;padding:9px 12px;border-top:1px solid var(--line);font-size:9px;align-items:center}.rechunk-compare-row code{overflow-wrap:anywhere}.rechunk-change{width:max-content;padding:4px 7px;border-radius:99px;font-weight:800}.rechunk-change.same{background:#e9f6ef;color:#15734c}.rechunk-change.changed{background:#e8f0ff;color:#1769e0}.rechunk-change.added{background:#e7f8f3;color:#008b68}.rechunk-change.removed{background:#fff0f2;color:#c2344c}.rechunk-chunk-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.rechunk-chunk-column{min-width:0}.rechunk-chunk-column h3{font-size:12px;margin:0 0 8px}.rechunk-chunk-card{margin-top:8px}.rechunk-chunk-card summary{display:flex;align-items:center;gap:8px}.rechunk-chunk-card summary code{color:var(--blue);overflow-wrap:anywhere}.rechunk-chunk-card summary small{margin-left:auto;color:var(--muted);font-size:9px;font-weight:500}.rechunk-chunk-body{padding:12px}.rechunk-meta-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px;margin-bottom:10px}.rechunk-chunk-body pre{max-height:250px;overflow:auto;margin:0;padding:10px;background:#0c1727;color:#d8e7fb;border-radius:8px;font:9px/1.65 Consolas,monospace;white-space:pre-wrap;word-break:break-word}.rechunk-inline-flags{display:flex;gap:6px;flex-wrap:wrap;margin:0 0 9px}.rechunk-inline-flags span{padding:4px 6px;border-radius:7px;background:#fff8e7;color:#73520b;font-size:8px}.rechunk-quality-table{display:grid;gap:0;max-height:260px;overflow:auto}.rechunk-quality-row{display:grid;grid-template-columns:120px 90px 1fr 90px;gap:9px;padding:9px 12px;border-top:1px solid var(--line);font-size:9px}.rechunk-stage-blocked{margin-top:13px;padding:13px 14px;border:1px solid #ffc4cc;background:#fff4f5;color:#a52638;border-radius:10px;font-size:10px;line-height:1.6}.rechunk-stage-blocked b{display:block;margin-bottom:4px}@media(max-width:900px){.rechunk-chunk-grid{grid-template-columns:1fr}.rechunk-document-meta,.rechunk-meta-grid{grid-template-columns:1fr 1fr}.rechunk-compare{grid-template-columns:1fr 1fr}.rechunk-compare-row,.rechunk-quality-row{grid-template-columns:1fr 90px;gap:6px}.rechunk-compare-row code{grid-column:1/-1}}@media(max-width:560px){.rechunk-document-meta,.rechunk-meta-grid,.rechunk-compare{grid-template-columns:1fr}.rechunk-review-banner{align-items:flex-start;flex-direction:column}}
+`;
+document.head.appendChild(rechunkReviewStyle);
+function rechunkLabel(value){return ({same:'동일',changed:'변경',added:'신규',removed:'제거'})[value]||value||'—';}
+function rechunkMetaCards(metadata={}){return Object.entries(metadata).filter(([,value])=>value!==''&&value!=null).map(([key,value])=>`<span><small>${esc(key)}</small>${esc(String(value))}</span>`).join('')||'<span><small>메타데이터</small>없음</span>';}
+function rechunkChunkCards(rows=[],kind){if(!rows.length)return '<div class="empty-list">표시할 청크가 없습니다.</div>';return rows.map((row,index)=>`<details class="rechunk-chunk-card" ${index===0?'open':''}><summary><code>${esc(row.chunk_id||'청크 ID 없음')}</code><small>${Number(row.content_chars||0).toLocaleString()}자</small></summary><div class="rechunk-chunk-body">${row.missing_required?.length?`<div class="rechunk-alert error"><b>필수 메타데이터 누락</b>${esc(row.missing_required.join(', '))}</div>`:''}${row.length_flags?.length?`<div class="rechunk-inline-flags">${row.length_flags.map(flag=>`<span>${esc(flag)}</span>`).join('')}</div>`:''}<div class="rechunk-meta-grid">${rechunkMetaCards(row.metadata||{})}</div><pre>${esc(row.content||'본문 없음')}</pre></div></details>`).join('');}
+function rechunkReviewPanel(job){const review=job?.review||{};if(job?.status!=='ready'||!review.parsed_documents)return '';const counts=review.comparison_counts||{},errors=review.errors||[],warnings=review.warnings||[],canStage=!!review.can_stage;const parsed=(review.parsed_documents||[]).map((doc,index)=>`<details ${index===0?'open':''}><summary>파싱 원문 · ${esc(doc.document_id||'문서 ID 없음')} · ${Number(doc.parsed_text_chars||0).toLocaleString()}자${doc.preview_truncated?' · 화면 표시 제한':''}</summary><div class="rechunk-parsed"><div class="rechunk-document-meta">${rechunkMetaCards(doc.metadata||{})}</div><div class="rechunk-inline-flags">${Object.entries(doc.quality||{}).filter(([,v])=>v!==''&&v!=null&&(!Array.isArray(v)||v.length)).map(([k,v])=>`<span>${esc(k)}: ${esc(Array.isArray(v)?v.join(', '):String(v))}</span>`).join('')}</div><pre class="rechunk-source-text">${esc(doc.parsed_markdown||'파싱된 본문 없음')}</pre></div></details>`).join('');const compare=(review.comparisons||[]).map(row=>`<div class="rechunk-compare-row"><code>${esc(row.chunk_id)}</code><span class="rechunk-change ${esc(row.status)}">${rechunkLabel(row.status)}</span><span>기존 ${Number(row.old_chars||0).toLocaleString()}자</span><span>새 ${Number(row.new_chars||0).toLocaleString()}자</span></div>`).join('')||'<div class="empty-list">비교할 청크가 없습니다.</div>';const quality=(review.quality_rows||[]).map(row=>`<div class="rechunk-quality-row"><b>${esc(row.url_id||'—')}</b><span>${esc(row.quality_status||'—')}</span><span>${esc(row.quality_reasons||'품질 경고 없음')}</span><span>보존율 ${row.chunk_coverage_ratio==null?'—':Math.round(Number(row.chunk_coverage_ratio)*100)+'%'}</span></div>`).join('')||'<div class="empty-list">품질 결과가 없습니다.</div>';return `<section class="rechunk-review"><div class="rechunk-review-banner ${canStage?'ready':'blocked'}"><div><strong>${canStage?'검수 통과 · 교체 초안을 만들 수 있습니다':'검수 오류 · 교체 초안 생성을 차단했습니다'}</strong><small>파싱 원문, 메타데이터, 기존·새 청크 비교를 확인한 뒤에만 운영 반영 단계로 이동합니다.</small></div><div class="rechunk-review-counts"><span>동일 ${counts.same||0}</span><span>변경 ${counts.changed||0}</span><span>신규 ${counts.added||0}</span><span>제거 ${counts.removed||0}</span></div></div>${errors.length?`<div class="rechunk-alert error"><b>반영 전 해결이 필요한 오류 ${errors.length}건</b><ul>${errors.map(error=>`<li>${esc(error)}</li>`).join('')}</ul></div>`:''}${warnings.length?`<div class="rechunk-alert warning"><b>관리자 검토 권장 ${warnings.length}건</b><ul>${warnings.map(warning=>`<li>${esc(warning)}</li>`).join('')}</ul></div>`:''}<details open><summary>1. 파싱된 원문과 문서 메타데이터 ${review.parsed_documents.length}건</summary><div class="rechunk-parsed">${parsed}</div></details><details open><summary>2. 기존 청크 ↔ 새 청크 비교 ${review.comparisons?.length||0}건</summary><div class="rechunk-compare-list">${compare}</div></details><details><summary>3. 청킹 품질 결과</summary><div class="rechunk-quality-table">${quality}</div></details><details><summary>4. 기존·새 청크 본문과 메타데이터 전체 보기</summary><div class="rechunk-parsed"><div class="rechunk-chunk-grid"><section class="rechunk-chunk-column"><h3>기존 청크 ${review.old_chunks?.length||0}개</h3>${rechunkChunkCards(review.old_chunks||[],'old')}</section><section class="rechunk-chunk-column"><h3>새 청크 ${review.new_chunks?.length||0}개</h3>${rechunkChunkCards(review.new_chunks||[],'new')}</section></div></div></details></section>`;}
+function rechunkPanel(){const sources=state.chunkSources||[],job=state.rechunkJob,selected=job?.source_url||state.rechunkSelectedUrl||sources[0]?.source_url||'',options=sources.map(row=>`<option value="${attr(row.source_url)}" ${row.source_url===selected?'selected':''}>${esc(row.sample_title||row.document_ids?.join(', ')||'제목 없음')} · ${num(row.chunk_count)}개 청크</option>`).join(''),review=job?.review||{},canStage=job?.status==='ready'&&review.can_stage===true,status=job?`<div class="rechunk-status">${statusBadge(job.status==='ready'?(canStage?'done':'failed'):job.status)}<b>${esc(job.stage||'')}</b><span>기존 ${num(job.old_chunk_count)}개 → 새 ${num(job.new_chunk_count)}개</span></div>${job.error?`<p class="rechunk-error">${esc(job.error)}</p>`:''}${rechunkReviewPanel(job)}`:'<p class="rechunk-note">URL별 기존 청크와 문서 ID를 기준으로 범위를 고정합니다. 현재 청크·Elasticsearch 인덱스는 아직 바뀌지 않습니다.</p>';return `<section class="panel rechunk-panel"><div class="panel-head"><div><span class="section-kicker">URL REFRESH</span><h2>기존 URL 재수집·재청킹</h2><p>사이트 내용이 바뀐 URL을 다시 수집합니다. 파싱 원문·청크·메타데이터 검수를 통과하기 전에는 운영 검색에 반영되지 않습니다.</p></div><button class="secondary-btn" data-action="refresh-chunk-sources">↻ URL 목록 새로고침</button></div>${sources.length?`<div class="rechunk-form"><label><span>현재 수집 URL</span><select id="rechunk-source">${options}</select></label><button class="primary-btn" data-action="start-rechunk" ${state.rechunkBusy?'disabled':''}>${state.rechunkBusy?'재수집 요청 중…':'재수집·재청킹 미리보기'}</button>${job?`<button class="secondary-btn" data-action="refresh-rechunk">상태 확인</button>`:''}</div>${status}${job?.status==='ready'?(canStage?`<div class="form-actions"><p>검수 통과 후 기존 ${num(job.old_chunk_count)}개를 제거 초안에, 새 ${num(job.new_chunk_count)}개를 추가 초안에 넣습니다.</p><button class="danger-btn" data-action="stage-rechunk" ${job.staged?'disabled':''}>${job.staged?'교체 초안 생성됨':'검수 통과 결과로 교체 초안 만들기'}</button></div>`:`<div class="rechunk-stage-blocked"><b>교체 초안 생성이 잠겨 있습니다.</b>위 오류를 해결하려면 원본 URL 또는 청킹 정책을 확인한 뒤 다시 재수집·재청킹하세요. 기존 운영 청크는 그대로 유지됩니다.</div>`):''}`:'<div class="empty-list">source_url 메타데이터가 있는 운영 청크를 찾지 못했습니다. 기존 데이터에 URL을 보존한 뒤 사용할 수 있습니다.</div>'}</section>`;}
+
+/* GET 응답을 브라우저 캐시가 아닌 현재 AWS 런타임에서 읽는다. */
+const apiBeforeRuntimeNoCache=api;
+api=async function(path,options={}){return apiBeforeRuntimeNoCache(path,{cache:'no-store',...options,headers:{'Cache-Control':'no-cache',...(options.headers||{})}});};
+refreshPipeline=async function(notify=true){try{state.pipelineGraph=await api(`/api/admin-ui/pipeline/graph?read_at=${Date.now()}`);if(state.pipelineNode)state.pipelineNode=(state.pipelineGraph.nodes||[]).find(node=>node.id===state.pipelineNode.id&&node.configurable)||null;render();if(notify)toast(`AWS 런타임 구조를 다시 읽었습니다. 해시 ${String(state.pipelineGraph.fingerprint||'—').slice(0,12)}`);}catch(error){toast(`런타임 다시 읽기 실패: ${error.message}`,true);}};
+
+/* 기존 URL 갱신과 신규 URL 수집을 동일한 안전 검수 흐름으로 운영한다. */
+const urlIngestStyle=document.createElement('style');
+urlIngestStyle.textContent=`
+.url-ingest-tabs{display:flex;gap:8px;margin-top:16px;padding:5px;border:1px solid var(--line);border-radius:11px;background:var(--bg);width:max-content}.url-ingest-tabs button{border:0;background:transparent;color:var(--muted);padding:8px 13px;border-radius:8px;font-size:10px;font-weight:800}.url-ingest-tabs button.active{background:var(--panel);color:var(--blue);box-shadow:0 2px 8px #0d2b5a12}.new-url-form{display:grid;grid-template-columns:1.3fr .7fr;gap:11px;margin-top:15px;padding:15px;border:1px solid var(--line);border-radius:11px;background:var(--bg)}.new-url-form label{display:grid;gap:6px}.new-url-form label.full{grid-column:1/-1}.new-url-form label span{font-size:9px;font-weight:800;color:var(--muted)}.new-url-form .new-url-actions{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:12px;padding-top:4px}.new-url-form .new-url-actions p{margin:0;color:var(--muted);font-size:9px;line-height:1.6}.rechunk-progress{margin-top:14px;padding:15px;border:1px solid var(--line);border-radius:11px;background:var(--bg)}.rechunk-progress-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.rechunk-progress-head strong,.rechunk-progress-head span{display:block}.rechunk-progress-head strong{font-size:12px}.rechunk-progress-head span{margin-top:4px;color:var(--muted);font-size:9px}.rechunk-progress-head b{font-size:18px;color:var(--blue)}.rechunk-progress-track{height:10px;margin:12px 0 9px;border-radius:99px;background:var(--line);overflow:hidden}.rechunk-progress-track i{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,#1769e0,#19a6cf);transition:width .5s ease}.rechunk-progress-foot{display:flex;justify-content:space-between;gap:12px;color:var(--muted);font-size:9px}.rechunk-progress.live .rechunk-progress-track i{position:relative}.rechunk-progress.live .rechunk-progress-track i:after{content:'';position:absolute;inset:0;background:linear-gradient(90deg,transparent,#ffffff90,transparent);animation:rechunkSweep 1.5s infinite}@keyframes rechunkSweep{from{transform:translateX(-100%)}to{transform:translateX(100%)}}.new-url-policy{padding:10px 12px;border-left:3px solid var(--blue);background:var(--soft);border-radius:8px;color:var(--muted);font-size:9px;line-height:1.65}.new-url-policy b{color:var(--ink)}@media(max-width:760px){.new-url-form{grid-template-columns:1fr}.new-url-form label.full,.new-url-form .new-url-actions{grid-column:auto}.new-url-form .new-url-actions{align-items:stretch;flex-direction:column}.url-ingest-tabs{width:100%}.url-ingest-tabs button{flex:1}}
+`;
+document.head.appendChild(urlIngestStyle);
+state.rechunkMode=state.rechunkMode||'refresh';
+state.chunkBusinessCatalog=state.chunkBusinessCatalog||[];
+let rechunkPollTimer=null;
+function rechunkElapsed(job){const started=Number(job?.started_at||job?.created_at||0);if(!started)return '대기 중';const seconds=Math.max(0,Math.floor(Date.now()/1000-started));const minutes=Math.floor(seconds/60),rest=seconds%60;return minutes?`${minutes}분 ${rest}초`:`${rest}초`;}
+function rechunkProgressPanel(job){if(!job)return '';const progress=Math.max(0,Math.min(100,Number(job.progress||0))),live=['queued','running'].includes(job.status);return `<section class="rechunk-progress ${live?'live':''}"><div class="rechunk-progress-head"><div><strong>${esc(job.stage||'처리 상태 확인 중')}</strong><span>${esc(job.stage_detail||'서버 처리 상태를 불러오고 있습니다.')}</span></div><b>${progress}%</b></div><div class="rechunk-progress-track"><i style="width:${progress}%"></i></div><div class="rechunk-progress-foot"><span>${live?'2초마다 자동 갱신':'자동 갱신 종료'}</span><span>경과 ${rechunkElapsed(job)} · 작업 ${esc(job.job_id||'—')}</span></div></section>`;}
+function newUrlForm(){const catalog=state.chunkBusinessCatalog||[],options=catalog.map(row=>`<option value="${attr(row.business_function)}" data-recommended="${attr(row.recommended_document_id)}">${esc(row.business_function)} · 추천 ${esc(row.recommended_document_id)}</option>`).join('');return `<div class="new-url-form"><label class="full"><span>신규 공개 URL *</span><input id="new-source-url" type="url" placeholder="https://www.kdic.or.kr/..." autocomplete="off"></label><label><span>업무 도메인 *</span><select id="new-business"><option value="">업무 선택</option>${options}</select></label><label><span>신규 문서 ID *</span><input id="new-document-id" placeholder="예: DP-007" autocomplete="off"></label><label class="full"><span>관리용 문서명 *</span><input id="new-title" placeholder="예: 예금자보호제도 안내" autocomplete="off"></label><div class="new-url-policy"><b>도메인별 ID 정책</b><br>선택한 업무의 기존 문서 ID를 기준으로 다음 번호를 추천합니다. 실제 청크는 <code>문서ID_chunk_000</code>부터 생성됩니다.</div><div class="new-url-actions"><p>신규 URL도 파싱·청킹·메타데이터 검수를 통과해야 초안을 만들 수 있으며, 운영 반영 전에는 검색에 포함되지 않습니다.</p><button class="primary-btn" data-action="start-new-url">신규 URL 크롤링·청킹 미리보기</button></div></div>`;}
+function rechunkOperationStatus(job){if(!job)return '<p class="rechunk-note">작업을 시작하면 실제 진행 단계와 경과시간이 여기에 표시됩니다.</p>';const review=job.review||{},canStage=job.status==='ready'&&review.can_stage===true,mode=job.mode||'refresh',summary=mode==='new'?`신규 청크 ${num(job.new_chunk_count)}개`:`기존 ${num(job.old_chunk_count)}개 → 새 ${num(job.new_chunk_count)}개`;return `${rechunkProgressPanel(job)}<div class="rechunk-status">${statusBadge(job.status==='ready'?(canStage?'done':'failed'):job.status)}<b>${esc(job.stage||'')}</b><span>${summary}</span></div>${job.error?`<p class="rechunk-error">${esc(job.error)}</p>`:''}${rechunkReviewPanel(job)}${job.status==='ready'?(canStage?`<div class="form-actions"><p>${mode==='new'?`검수한 새 청크 ${num(job.new_chunk_count)}개를 추가 초안에 넣습니다.`:`기존 ${num(job.old_chunk_count)}개를 제거 초안에, 새 ${num(job.new_chunk_count)}개를 추가 초안에 넣습니다.`}</p><button class="danger-btn" data-action="stage-rechunk" ${job.staged?'disabled':''}>${job.staged?'적재 초안 생성됨':mode==='new'?'검수 통과 결과를 신규 적재 초안으로':'검수 통과 결과로 교체 초안 만들기'}</button></div>`:`<div class="rechunk-stage-blocked"><b>적재 초안 생성이 잠겨 있습니다.</b>위 오류를 확인한 뒤 URL 또는 메타데이터를 수정하고 다시 실행하세요. 현재 운영 청크는 바뀌지 않았습니다.</div>`):''}`;}
+function rechunkPanel(){const sources=state.chunkSources||[],job=state.rechunkJob,mode=state.rechunkMode||'refresh',selected=job?.mode==='refresh'?job.source_url:(state.rechunkSelectedUrl||sources[0]?.source_url||''),options=sources.map(row=>`<option value="${attr(row.source_url)}" ${row.source_url===selected?'selected':''}>${esc(row.sample_title||row.document_ids?.join(', ')||'제목 없음')} · ${num(row.chunk_count)}개 청크</option>`).join('');const refreshForm=sources.length?`<div class="rechunk-form"><label><span>현재 수집 URL</span><select id="rechunk-source">${options}</select></label><button class="primary-btn" data-action="start-rechunk" ${state.rechunkBusy?'disabled':''}>${state.rechunkBusy?'재수집 요청 중…':'재수집·재청킹 시작'}</button>${job?`<button class="secondary-btn" data-action="refresh-rechunk">지금 상태 확인</button>`:''}</div>`:'<div class="empty-list">source_url 메타데이터가 있는 운영 청크를 찾지 못했습니다.</div>';return `<section class="panel rechunk-panel"><div class="panel-head"><div><span class="section-kicker">URL INGESTION</span><h2>URL 수집·청킹 관리</h2><p>기존 페이지 갱신과 신규 페이지 적재를 같은 검수·초안·승인 절차로 관리합니다.</p></div><button class="secondary-btn" data-action="refresh-chunk-sources">↻ URL·도메인 목록 새로고침</button></div><div class="url-ingest-tabs"><button class="${mode==='refresh'?'active':''}" data-action="switch-rechunk-mode" data-mode="refresh">기존 URL 갱신</button><button class="${mode==='new'?'active':''}" data-action="switch-rechunk-mode" data-mode="new">신규 URL 등록</button></div>${mode==='refresh'?refreshForm:newUrlForm()}${rechunkOperationStatus(job)}</section>`;}
+async function pollRechunkOnce(notify=false){const id=state.rechunkJob?.job_id;if(!id)return;try{state.rechunkJob=await api(`/api/admin-ui/chunk-sources/rechunk/${encodeURIComponent(id)}?read_at=${Date.now()}`);render();if(['queued','running'].includes(state.rechunkJob.status)){scheduleRechunkPoll();if(notify)toast(`${state.rechunkJob.stage} · ${state.rechunkJob.progress||0}%`);}else{if(rechunkPollTimer){clearTimeout(rechunkPollTimer);rechunkPollTimer=null;}if(notify){if(state.rechunkJob.status==='ready')toast('크롤링·청킹이 완료되었습니다. 검수 결과를 확인해 주세요.');else toast(state.rechunkJob.error||'처리에 실패했습니다.',true);}}}catch(error){if(rechunkPollTimer){clearTimeout(rechunkPollTimer);rechunkPollTimer=null;}toast(`상태 확인 실패: ${error.message}`,true);}}
+function scheduleRechunkPoll(){if(rechunkPollTimer)clearTimeout(rechunkPollTimer);if(!['queued','running'].includes(state.rechunkJob?.status))return;rechunkPollTimer=setTimeout(()=>{rechunkPollTimer=null;pollRechunkOnce(false);},2000);}
+refreshRechunk=async function(){await pollRechunkOnce(true);};
+startRechunk=async function(){const source_url=document.getElementById('rechunk-source')?.value||'';if(!source_url)return toast('재수집할 URL을 선택해 주세요.',true);state.rechunkMode='refresh';state.rechunkSelectedUrl=source_url;if(!confirm('선택 URL만 다시 수집하고 재청킹합니다. 현재 운영 청크는 바뀌지 않습니다. 계속할까요?'))return;state.rechunkBusy=true;render();try{state.rechunkJob=await api('/api/admin-ui/chunk-sources/rechunk',{method:'POST',body:JSON.stringify({source_url})});render();scheduleRechunkPoll();toast('재수집을 시작했습니다. 진행 상태가 자동으로 갱신됩니다.');}catch(error){toast(error.message,true);}finally{state.rechunkBusy=false;render();}};
+async function startNewUrl(){const source_url=document.getElementById('new-source-url')?.value.trim()||'',document_id=document.getElementById('new-document-id')?.value.trim().toUpperCase()||'',business_function=document.getElementById('new-business')?.value||'',title=document.getElementById('new-title')?.value.trim()||'';if(!source_url||!document_id||!business_function||!title)return toast('신규 URL, 업무 도메인, 문서 ID, 문서명을 모두 입력해 주세요.',true);if(!/^https?:\/\//i.test(source_url))return toast('URL은 https:// 또는 http://로 시작해야 합니다.',true);if(!/^[A-Z][A-Z0-9]{1,7}-\d{3,6}$/.test(document_id))return toast('문서 ID는 DP-007 같은 도메인 코드-숫자 형식이어야 합니다.',true);if(!confirm(`${document_id}로 신규 URL을 수집하고 청킹 미리보기를 만들까요? 운영에는 아직 반영되지 않습니다.`))return;state.rechunkBusy=true;state.rechunkMode='new';render();try{state.rechunkJob=await api('/api/admin-ui/chunk-sources/crawl',{method:'POST',body:JSON.stringify({source_url,document_id,business_function,title,target_business_function:business_function,page_type:'static_page'})});render();scheduleRechunkPoll();toast('신규 URL 수집을 시작했습니다. 진행 상태가 자동으로 갱신됩니다.');}catch(error){toast(error.message,true);}finally{state.rechunkBusy=false;render();}}
+stageRechunk=async function(){const job=state.rechunkJob,id=job?.job_id;if(!id||job?.status!=='ready'||job?.review?.can_stage!==true)return toast('검수 통과한 미리보기가 없습니다.',true);const isNew=job.mode==='new',message=isNew?`새 청크 ${job.new_chunk_count}개를 추가 초안으로 만들까요? 실제 반영은 반영센터에서 별도로 승인해야 합니다.`:`기존 ${job.old_chunk_count}개를 제거 초안에 넣고 새 ${job.new_chunk_count}개를 추가 초안으로 만들까요?`;if(!confirm(message))return;try{const out=await api(`/api/admin-ui/chunk-sources/rechunk/${encodeURIComponent(id)}/stage`,{method:'POST',body:'{}'});state.draft=out.draft;state.rechunkJob={...job,staged:true};render();toast(isNew?'신규 URL 적재 초안을 만들었습니다. 반영센터에서 승인해 주세요.':'URL 교체 초안을 만들었습니다. 반영센터에서 승인해 주세요.');}catch(error){toast(error.message,true);}};
+refreshChunkSources=async function(notify=true){try{const out=await api(`/api/admin-ui/chunk-sources?read_at=${Date.now()}`);state.chunkSources=out.items||[];state.chunkBusinessCatalog=out.business_catalog||[];if(notify)toast(`운영 URL ${state.chunkSources.length}개와 업무 도메인 ${state.chunkBusinessCatalog.length}개를 다시 읽었습니다.`);render();}catch(error){toast(error.message,true);}};
+const bootstrapBeforeNewUrl=bootstrap;bootstrap=async function(){await bootstrapBeforeNewUrl();if(state.summary)await refreshChunkSources(false);};
+const refreshAllBeforeNewUrl=refreshAll;refreshAll=async function(silent=false){await refreshAllBeforeNewUrl(silent);if(state.summary)await refreshChunkSources(false);};
+root.addEventListener('click',event=>{const button=event.target.closest?.('button[data-action]'),action=button?.dataset?.action;if(!['switch-rechunk-mode','start-new-url'].includes(action))return;event.preventDefault();event.stopImmediatePropagation();if(action==='switch-rechunk-mode'){state.rechunkMode=button.dataset.mode||'refresh';render();}if(action==='start-new-url')startNewUrl();},true);
+root.addEventListener('change',event=>{if(event.target?.id!=='new-business')return;const option=event.target.selectedOptions?.[0],input=document.getElementById('new-document-id');if(input&&!input.value&&option?.dataset?.recommended)input.value=option.dataset.recommended;});
+
+/* 청크 관리를 작업 목적에 따라 수집·적재와 운영 청크 관리로 분리한다. */
+function chunkDataSections(){
+  const full=dataViewBeforeRechunk();
+  const addStart=full.indexOf('<section class="panel add-chunk">');
+  const searchStart=full.indexOf('<section class="panel"><div class="api-searchbar">',Math.max(0,addStart));
+  if(addStart<0||searchStart<0){
+    return {add:full,browse:'<section class="panel"><div class="empty-list">청크 관리 화면을 불러오지 못했습니다.</div></section>'};
+  }
+  return {add:full.slice(addStart,searchStart),browse:full.slice(searchStart)};
+}
+function dataIngestionView(){
+  const sections=chunkDataSections();
+  return `${heading('CONTENT INGESTION','수집·적재 관리','기존·신규 URL을 수집하고 재청킹하거나, 검수된 JSON·JSONL 청크를 추가 초안으로 저장합니다.')}<div class="risk-banner"><b>i</b><span><strong>수집·추가 결과는 즉시 운영에 반영되지 않습니다.</strong>미리보기와 메타데이터 검수 후 초안을 만들고, 반영 센터에서 별도로 승인합니다.</span></div>${rechunkPanel()}${sections.add}`;
+}
+function dataLibraryView(){
+  const sections=chunkDataSections();
+  return `${heading('CHUNK LIBRARY','운영 청크 관리','현재 검색 인덱스에 적재된 청크를 조회하고, 본문·메타데이터를 확인하거나 제거 초안을 만듭니다.')}<div class="risk-banner"><b>i</b><span><strong>제거는 초안으로만 저장됩니다.</strong>반영 센터 승인 전까지 운영 검색 결과는 바뀌지 않습니다.</span></div>${sections.browse}`;
+}
+
+/* 종합점수의 산식과 지표별 기여도를 공개해 다음 파라미터 실험으로 연결한다. */
+const scoreGuideStyle=document.createElement('style');
+scoreGuideStyle.textContent=`
+.score-method-panel{margin-top:14px;padding:19px;border:1px solid var(--line);border-radius:12px;background:var(--panel)}.score-method-panel h4{margin:5px 0 6px;font-size:16px}.score-method-panel>p{margin:0;color:var(--muted);font-size:11px;line-height:1.7}.score-formula{margin:13px 0;padding:14px;border-radius:10px;background:var(--soft);color:var(--ink);font-size:12px;font-weight:850;line-height:1.65;text-align:center}.score-number-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.score-number{padding:12px;border:1px solid var(--line);border-radius:9px;background:var(--bg)}.score-number small,.score-number strong{display:block}.score-number small{font-size:9px;color:var(--muted)}.score-number strong{margin-top:5px;font-size:20px}.score-method-note{margin-top:10px!important;font-size:10px!important}.score-contribution-panel{margin-top:14px;padding:19px;border:1px solid var(--line);border-radius:12px;background:var(--panel)}.score-contribution-panel h4{margin:5px 0 5px;font-size:16px}.score-contribution-panel>p{font-size:11px!important;line-height:1.65!important}.score-contribution-table{margin-top:13px;overflow:auto}.score-contribution-row{min-width:900px;display:grid;grid-template-columns:125px 115px 125px 145px 130px minmax(220px,1fr);gap:9px;align-items:center;padding:10px 9px;border-top:1px solid var(--line);font-size:10px}.score-contribution-row.head{border:0;border-radius:8px;background:var(--soft);color:var(--muted);font-weight:800}.score-contribution-row b small{display:block;margin-top:3px;color:var(--muted);font-size:8px}.score-contribution-row .contribution-value{font-size:12px;font-weight:900}.score-contribution-row .question-counts{font-size:9px;line-height:1.55}.score-contribution-row .parameter-tags{display:flex;gap:5px;flex-wrap:wrap}.score-contribution-row .parameter-tags span{padding:4px 6px;border-radius:6px;background:var(--soft);color:var(--blue);font-size:8px;font-weight:800}.score-action-panel{margin-top:14px;padding:19px;border:1px solid var(--line);border-radius:12px;background:var(--panel)}.score-action-panel h4{margin:5px 0 5px;font-size:16px}.score-action-panel>p{font-size:11px!important;line-height:1.65!important}.score-action-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:13px}.score-action-card{padding:14px;border:1px solid var(--line);border-left:5px solid var(--red);border-radius:10px;background:var(--bg)}.score-action-card.positive{border-left-color:var(--green)}.score-action-card h5{margin:0 0 7px;font-size:12px}.score-action-card p{margin:6px 0!important;font-size:10px!important;line-height:1.65!important}.score-action-card ul{margin:8px 0 0;padding-left:18px}.score-action-card li{font-size:9px;line-height:1.65}.score-action-card .parameter-change{display:block;margin-top:8px;padding:7px;border-radius:7px;background:var(--panel);color:var(--blue);font-size:9px;font-weight:800;line-height:1.55}.score-causality-note{margin-top:12px!important;padding:10px 12px;border-left:4px solid #d59a18;border-radius:7px;background:#fff8e8;color:#765113!important}.admin-app.dark .score-method-panel,.admin-app.dark .score-contribution-panel,.admin-app.dark .score-action-panel{background:#101f31}.admin-app.dark .score-number,.admin-app.dark .score-action-card{background:#14273d}.admin-app.dark .score-causality-note{background:#3c3017;color:#ffd88b!important}@media(max-width:1000px){.score-action-grid{grid-template-columns:1fr}.score-number-grid{grid-template-columns:1fr}}
+`;
+document.head.appendChild(scoreGuideStyle);
+const scoreAdjustmentHints={
+hit:{group:'정답 포함 여부',parameters:['candidate_depth','final_top_k','min_relevance_score'],hints:['후보 깊이를 늘려 Gold 후보 누락 여부 확인','최종 Top-K를 단계적으로 늘려 재평가']},
+recall:{group:'정답 범위 회수',parameters:['candidate_depth','final_top_k','min_relevance_score'],hints:['후보 깊이 또는 최종 Top-K를 늘려 누락 Gold 청크 확인','임계치가 높다면 소폭 낮춰 재평가']},
+mrr:{group:'첫 정답 순위',parameters:['dense_weight','bm25_weight','candidate_depth'],hints:['Dense·BM25 가중치를 한 번에 하나씩 조정','Reranker 후보 깊이를 늘려 정답 재정렬 여부 확인']},
+map:{group:'복수 정답 순위',parameters:['dense_weight','bm25_weight','candidate_depth'],hints:['Dense·BM25 가중치를 재조정','Reranker 후보 깊이와 Gold 청크 순서를 질문별로 확인']},
+complete:{group:'다중 청크 완전 회수',parameters:['candidate_depth','final_top_k','parent_child'],hints:['다중 청크 문항에서 후보 깊이·최종 Top-K 확대','Parent-Child 확장이 검색 Child 순위를 가리지 않는지 확인']},
+ndcg:{group:'상위 순위 품질',parameters:['dense_weight','bm25_weight','candidate_depth'],hints:['Reranker 입력 후보와 Dense·BM25 가중치 점검','Gold 청크가 상위권에서 밀린 대표 문항 확인']},
+precision:{group:'검색 결과 순도',parameters:['final_top_k','min_relevance_score'],hints:['최종 Top-K를 낮추거나 최소 관련성 점수를 높여 불필요 청크 축소','Recall 손실이 없는 범위에서 한 단계씩 조정']},
+f1:{group:'정밀도·재현율 균형',parameters:['final_top_k','min_relevance_score','candidate_depth'],hints:['Precision과 Recall 중 어느 쪽이 F1을 끌어내렸는지 먼저 확인','Top-K와 임계치를 한 번에 바꾸지 말고 하나씩 조정']}
+};
+function signedPp(value,digits=2){const number=Number(value||0)*100;return `${number>0?'+':''}${number.toFixed(digits)}%p`;}
+function scoreExplanation(result,summary){
+  if(summary?.score_method&&Array.isArray(summary?.metric_contributions))return {method:summary.score_method,contributions:summary.metric_contributions};
+  const definitions=metricDefinitions(result),details=result?.details||[],total=details.length||1,contributionMap=new Map(definitions.map(def=>[def.name,{name:def.name,metric:def.label,group:scoreAdjustmentHints[def.name]?.group||'검색 품질',applicable_questions:0,mean_delta:0,score_contribution:0,positive_questions:0,negative_questions:0,unchanged_questions:0,adjustment_hints:scoreAdjustmentHints[def.name]?.hints||[],target_parameters:scoreAdjustmentHints[def.name]?.parameters||[]}]))
+  let currentTotal=0,improvementTotal=0;
+  details.forEach(detail=>{const changes=definitions.map(def=>{const current=Number(detail?.baseline?.[def.baseline_key]),improvement=Number(detail?.candidate?.[def.candidate_key]);return Number.isFinite(current)&&Number.isFinite(improvement)?{def,current,improvement,delta:improvement-current}:null;}).filter(Boolean);if(!changes.length)return;currentTotal+=changes.reduce((sum,row)=>sum+row.current,0)/changes.length;improvementTotal+=changes.reduce((sum,row)=>sum+row.improvement,0)/changes.length;changes.forEach(change=>{const row=contributionMap.get(change.def.name);row.applicable_questions++;row.mean_delta+=change.delta;row.score_contribution+=change.delta/changes.length/total;if(change.delta>.005)row.positive_questions++;else if(change.delta<-.005)row.negative_questions++;else row.unchanged_questions++;});});
+  const contributions=[...contributionMap.values()].filter(row=>row.applicable_questions).map(row=>({...row,mean_delta:row.mean_delta/row.applicable_questions}));
+  const current=currentTotal/total,improvement=improvementTotal/total;
+  return {method:{name:'질문별 검색 품질 종합점수',formula:'질문별 종합점수 = 적용 가능한 (Hit + Recall + MRR + MAP + Complete + nDCG + Precision + F1) ÷ 적용 지표 수',weighting:'모든 적용 지표 동일 가중치(1/n). Complete는 다중 청크 평가 문항에만 포함',threshold:.005,current,improvement,delta:improvement-current,contribution_check:contributions.reduce((sum,row)=>sum+row.score_contribution,0)},contributions};
+}
+function parameterGuideText(keys,result){const base=result?.baseline_config||{},candidate=result?.candidate_config||{};return (keys||[]).map(key=>{const field=resultConfigFields.find(item=>item[0]===key),label=field?.[1]||key;return String(base[key])!==String(candidate[key])?`${label} ${resultConfigText(key,base[key])} → ${resultConfigText(key,candidate[key])}`:label;}).join(' · ');}
+function scoreMethodPanel(result,summary){const scoring=scoreExplanation(result,summary),method=scoring.method||{},delta=Number(method.delta||0),contributionDelta=Number(method.contribution_check||0);return `<section class="score-method-panel"><span class="section-kicker">COMPOSITE SCORE METHOD</span><h4>종합점수는 이렇게 계산됐습니다</h4><p>LLM이 임의로 만든 점수가 아닙니다. 서버가 각 질문의 Gold 청크 일치 결과로 지표를 계산한 뒤 동일 비중으로 평균합니다.</p><div class="score-formula">${esc(method.formula||'질문별 적용 가능 지표의 단순평균')}</div><div class="score-number-grid"><div class="score-number"><small>현재안 질문별 평균</small><strong>${pct(method.current)}</strong></div><div class="score-number"><small>개선안 질문별 평균</small><strong>${pct(method.improvement)}</strong></div><div class="score-number"><small>종합점수 변화</small><strong class="${delta>0?'rank-up':delta<0?'rank-down':'rank-same'}">${signedPp(delta)}</strong></div></div><p class="score-method-note"><strong>가중 방식:</strong> ${esc(method.weighting||'동일 가중치')} · <strong>판정:</strong> 질문별 변화가 ±${(Number(method.threshold||.005)*100).toFixed(1)}%p를 넘으면 상승·하락</p><p class="score-method-note">지표별 기여도 합계 ${signedPp(contributionDelta)} = 종합점수 변화 ${signedPp(delta)}로 검산됩니다.</p></section>`;}
+function scoreContributionPanel(result,summary){const rows=scoreExplanation(result,summary).contributions||[];return `<section class="score-contribution-panel"><span class="section-kicker">METRIC CONTRIBUTION</span><h4>어떤 지표가 점수를 얼마나 움직였나요?</h4><p><strong>평균 변화</strong>는 해당 지표 자체의 변화이고, <strong>종합점수 기여</strong>는 문항별 1/n 가중치를 반영해 최종 점수에 실제 더해진 값입니다.</p><div class="score-contribution-table"><div class="score-contribution-row head"><span>지표</span><span>평균 변화</span><span>종합점수 기여</span><span>문항 분포</span><span>적용 문항</span><span>우선 확인 파라미터</span></div>${rows.map(row=>`<div class="score-contribution-row"><b>${esc(row.metric)}<small>${esc(row.group||'검색 품질')}</small></b><span class="${Number(row.mean_delta)>0?'rank-up':Number(row.mean_delta)<0?'rank-down':'rank-same'}">${signedPp(row.mean_delta)}</span><span class="contribution-value ${Number(row.score_contribution)>0?'rank-up':Number(row.score_contribution)<0?'rank-down':'rank-same'}">${signedPp(row.score_contribution)}</span><span class="question-counts"><b class="rank-up">↑ ${num(row.positive_questions)}</b> · <b class="rank-down">↓ ${num(row.negative_questions)}</b> · = ${num(row.unchanged_questions)}</span><span>${num(row.applicable_questions)}개</span><span class="parameter-tags">${(row.target_parameters||[]).map(key=>`<span>${esc(resultConfigFields.find(item=>item[0]===key)?.[1]||key)}</span>`).join('')||'—'}</span></div>`).join('')}</div></section>`;}
+function scoreActionPanel(result,summary){const rows=scoreExplanation(result,summary).contributions||[],negative=[...rows].filter(row=>Number(row.score_contribution)<0).sort((a,b)=>Number(a.score_contribution)-Number(b.score_contribution)).slice(0,3),positive=[...rows].filter(row=>Number(row.score_contribution)>0).sort((a,b)=>Number(b.score_contribution)-Number(a.score_contribution)).slice(0,1),cards=[...negative,...positive];return `<section class="score-action-panel"><span class="section-kicker">NEXT EXPERIMENT GUIDE</span><h4>다음 실험에서 무엇을 조정해야 하나요?</h4><p>기여도가 크게 하락한 지표부터 원인을 좁힙니다. 아래 내용은 인과관계 확정이 아니라, 동일 데이터셋으로 검증할 <strong>우선 실험 후보</strong>입니다.</p><div class="score-action-grid">${cards.map(row=>`<article class="score-action-card ${Number(row.score_contribution)>0?'positive':''}"><h5>${Number(row.score_contribution)>0?'유지하며 감시':'우선 개선'} · ${esc(row.metric)}</h5><p>종합점수 기여 <strong class="${Number(row.score_contribution)>0?'rank-up':'rank-down'}">${signedPp(row.score_contribution)}</strong> · 하락 문항 ${num(row.negative_questions)}개</p>${Number(row.score_contribution)<0?`<ul>${(row.adjustment_hints||[]).map(hint=>`<li>${esc(hint)}</li>`).join('')}</ul>`:'<p>현재 개선 효과를 유지하되 다른 지표 개선 과정에서 다시 하락하지 않는지 확인합니다.</p>'}<span class="parameter-change">${esc(parameterGuideText(row.target_parameters,result)||'관련 파라미터 없음')}</span></article>`).join('')||'<div class="empty-list">종합점수를 움직인 지표를 찾지 못했습니다.</div>'}</div><p class="score-causality-note"><strong>실험 원칙:</strong> Dense/BM25 비중, 후보 깊이, Top-K, 임계치를 동시에 바꾸면 원인을 분리할 수 없습니다. 한 번에 한 항목만 바꾸고 동일 K·동일 데이터셋으로 다시 비교하세요.</p></section>`;}
+const llmReportPanelBeforeScoreGuide=llmReportPanel;
+llmReportPanel=function(result){let html=llmReportPanelBeforeScoreGuide(result),summary=result?.llm_report?.summary;if(!result?.llm_report)return html;const panels=scoreMethodPanel(result,summary)+scoreContributionPanel(result,summary)+scoreActionPanel(result,summary);return html.replace('<section class="report-metric-section">',panels+'<section class="report-metric-section">');};
+
+/* 프롬프트도 이름이 있는 재사용 설정으로 저장하고 테스트·반영에 불러온다. */
+function promptPresetOptions(includeDraft=true){const first=includeDraft?'<option value="">편집 중인 개선안 사용</option>':'<option value="">저장된 프롬프트 설정 선택</option>';return first+(state.promptPresets||[]).map(row=>`<option value="${attr(row.preset_id)}" ${row.preset_id===state.selectedPromptPresetId?'selected':''}>${esc(row.name)}</option>`).join('');}
+function selectedPromptPreset(){return (state.promptPresets||[]).find(row=>row.preset_id===state.selectedPromptPresetId)||null;}
+function promptPresetDifference(values){const active=Object.fromEntries((state.prompts?.slots||[]).map(row=>[row.slot,row.active]));return Object.keys(active).filter(slot=>active[slot]!==values?.[slot]);}
+async function refreshPromptPresets(notify=true){try{const out=await api('/api/admin-ui/prompt-presets');state.promptPresets=out.items||[];if(state.selectedPromptPresetId&&!state.promptPresets.some(row=>row.preset_id===state.selectedPromptPresetId))state.selectedPromptPresetId='';if(notify){render();toast('저장된 프롬프트 설정을 다시 읽었습니다.');}}catch(error){toast(error.message,true);}}
+async function savePromptPreset(){const name=(document.getElementById('prompt-preset-name')?.value||state.promptPresetName||'').trim();if(!name)return toast('저장할 프롬프트 설정 이름을 입력해 주세요.',true);try{const row=await api('/api/admin-ui/prompt-presets',{method:'POST',body:JSON.stringify({name,values:readPromptValues()})});await refreshPromptPresets(false);state.selectedPromptPresetId=row.preset_id;state.promptPresetName='';render();toast(`프롬프트 설정 ‘${row.name}’을 저장했습니다. 운영 챗봇에는 아직 반영되지 않았습니다.`);}catch(error){toast(error.message,true);}}
+async function loadPromptPreset(){const preset=selectedPromptPreset();if(!preset)return toast('불러올 프롬프트 설정을 선택해 주세요.',true);try{const out=await api(`/api/admin-ui/prompt-presets/${encodeURIComponent(preset.preset_id)}/load-draft`,{method:'POST',body:'{}'});state.prompts=out.prompts;state.draft=out.draft;state.promptComparison=null;render();toast(`‘${preset.name}’ 설정을 편집기와 반영 초안에 불러왔습니다.`);}catch(error){toast(error.message,true);}}
+async function deletePromptPreset(){const preset=selectedPromptPreset();if(!preset)return toast('삭제할 프롬프트 설정을 선택해 주세요.',true);if(!confirm(`‘${preset.name}’ 프롬프트 설정을 삭제할까요? 운영 프롬프트와 반영 이력은 바뀌지 않습니다.`))return;try{await api(`/api/admin-ui/prompt-presets/${encodeURIComponent(preset.preset_id)}`,{method:'DELETE'});state.selectedPromptPresetId='';await refreshPromptPresets(false);render();toast('프롬프트 설정을 삭제했습니다.');}catch(error){toast(error.message,true);}}
+const promptsViewBeforePresetManager=promptsView;
+promptsView=function(){const preset=selectedPromptPreset(),count=(state.promptPresets||[]).length,manager=`<section class="panel prompt-preset-manager"><div class="panel-head"><div><span class="section-kicker">PROMPT PRESET</span><h2>저장된 프롬프트 설정</h2><p class="muted-note">세 개의 운영 프롬프트를 하나의 이름으로 묶어 저장하고, 테스트·최종 비교·반영에 다시 사용합니다.</p></div><span class="test-badge">${count}개 저장</span></div><div class="prompt-preset-toolbar"><label><span>설정 선택</span><select id="prompt-preset-select">${promptPresetOptions(false)}</select></label><button class="secondary-btn" data-action="load-prompt-preset" ${!preset?'disabled':''}>편집기로 불러오기</button><button class="icon-danger" data-action="delete-prompt-preset" ${!preset?'disabled':''}>삭제</button></div>${preset?`<div class="prompt-preset-summary"><span>${esc(preset.name)}</span><span>운영값과 ${promptPresetDifference(preset.values).length}개 프롬프트 다름</span><span>저장 ${fmtDate(preset.updated_at)}</span></div>`:'<div class="prompt-preset-note">새 설정은 아래 편집기에서 이름을 입력해 저장하세요.</div>'}</section>`;let html=promptsViewBeforePresetManager();html=html.replace('프롬프트 개선안','프롬프트 설정 관리').replace('<div class="risk-banner">',manager+'<div class="risk-banner">');const saveRow=`<div class="prompt-save-row"><label><span>새 프롬프트 설정 이름</span><input id="prompt-preset-name" maxlength="60" value="${attr(state.promptPresetName||'')}" placeholder="예: 교차업무 답변 간결화 v1"></label><button class="primary-btn" data-action="save-prompt-preset">이름을 붙여 설정 저장</button></div>`;html=html.replace('<div class="prompt-actions"><button class="secondary-btn" data-action="clear-prompt-draft"',saveRow+'<div class="prompt-actions"><button class="secondary-btn" data-action="clear-prompt-draft"');const testSelect=`<div class="prompt-test-preset"><label><span>테스트할 프롬프트 설정</span><select id="prompt-test-preset-select">${promptPresetOptions(true)}</select></label><button class="secondary-btn" data-action="load-prompt-preset" ${!preset?'disabled':''}>선택 설정 편집기로 불러오기</button></div>`;html=html.replace('<div class="prompt-question">',testSelect+'<div class="prompt-question">');if(preset)html=html.replace('개선안 저장 후 비교',`‘${esc(preset.name)}’으로 비교`);if(state.promptComparison?.candidate_preset?.name)html=html.replace('<h3>개선안 프롬프트</h3>',`<h3>개선안 · ${esc(state.promptComparison.candidate_preset.name)}</h3>`);return html;};
+const comparePromptsBeforePresetManager=comparePrompts;
+comparePrompts=async function(){const preset=selectedPromptPreset();if(!preset)return comparePromptsBeforePresetManager();const q=document.getElementById('prompt-question')?.value.trim()||'';if(!q)return toast('비교 질문을 입력해 주세요.',true);state.promptQuestion=q;state.promptBusy=true;state.promptComparison=null;render();try{state.promptComparison=await api('/api/admin-ui/prompts/compare',{method:'POST',body:JSON.stringify({question:q,preset_id:preset.preset_id})});toast(`운영 프롬프트와 ‘${preset.name}’ 설정의 답변 비교를 완료했습니다.`);}catch(error){toast(error.message,true);}finally{state.promptBusy=false;render();}};
+const refreshPromptsBeforePresetManager=refreshPrompts;refreshPrompts=async function(notify=true){await refreshPromptsBeforePresetManager(false);await refreshPromptPresets(false);render();if(notify)toast('프롬프트 운영값과 저장 설정을 다시 읽었습니다.');};
+root.addEventListener('input',event=>{if(event.target?.id==='prompt-preset-name')state.promptPresetName=event.target.value;},true);
+root.addEventListener('change',event=>{if(!['prompt-preset-select','prompt-test-preset-select'].includes(event.target?.id))return;state.promptQuestion=document.getElementById('prompt-question')?.value||state.promptQuestion;state.promptPresetName=document.getElementById('prompt-preset-name')?.value||state.promptPresetName;state.selectedPromptPresetId=event.target.value||'';state.promptComparison=null;render();},true);
+root.addEventListener('click',event=>{const button=event.target.closest?.('button[data-action]'),action=button?.dataset?.action;if(!['save-prompt-preset','load-prompt-preset','delete-prompt-preset'].includes(action))return;event.preventDefault();event.stopImmediatePropagation();if(action==='save-prompt-preset')savePromptPreset();if(action==='load-prompt-preset')loadPromptPreset();if(action==='delete-prompt-preset')deletePromptPreset();},true);
+
 /* 챗봇 로그는 운영 모니터링 안의 하위 메뉴로만 둔다. */
 const standaloneJobsIndex=navGroups.findIndex(group=>group.id==='jobs');if(standaloneJobsIndex>=0)navGroups.splice(standaloneJobsIndex,1);
 root.addEventListener('click',e=>{const button=e.target.closest?.('button[data-action="filter-eval-cases"]');if(!button)return;state.evalCaseFilter=button.dataset.filter||'all';render();},true);
+/* 공개 챗봇이 실제 등록한 런타임 이름을 파이프라인 화면에도 그대로 표시한다. */
+const bootstrapBeforeRuntimeIdentity=bootstrap;bootstrap=async function(){await bootstrapBeforeRuntimeIdentity();if(!state.summary)return;try{const health=await api('/api/health');if(state.pipelineGraph&&health?.pipeline){state.pipelineGraph.pipeline_name=health.pipeline;state.pipelineGraph.runtime_build=health.runtime_build||{};render();}}catch(_error){/* 기존 관리자 데이터는 그대로 유지 */}};
+const refreshAllBeforeRuntimeIdentity=refreshAll;refreshAll=async function(silent=false){await refreshAllBeforeRuntimeIdentity(silent);if(!state.summary)return;try{const health=await api('/api/health');if(state.pipelineGraph&&health?.pipeline){state.pipelineGraph.pipeline_name=health.pipeline;state.pipelineGraph.runtime_build=health.runtime_build||{};render();}}catch(_error){/* 기존 관리자 데이터는 그대로 유지 */}};
+const bootstrapBeforePromptPresets=bootstrap;bootstrap=async function(){await bootstrapBeforePromptPresets();if(state.summary){await refreshPromptPresets(false);render();}};
+const refreshAllBeforePromptPresets=refreshAll;refreshAll=async function(silent=false){await refreshAllBeforePromptPresets(silent);if(state.summary){await refreshPromptPresets(false);render();}};
+
+/* 키워드 버튼과 검증 답변을 현재 챗봇 레지스트리에 직접 연결해 관리한다. */
+const suggestionCacheStyle=document.createElement('style');
+suggestionCacheStyle.textContent=`
+.cache-admin-summary{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:14px}.cache-admin-layout{display:grid;grid-template-columns:minmax(560px,1.15fr) minmax(430px,.85fr);gap:14px;align-items:start}.cache-catalog,.cache-editor{padding:18px}.cache-toolbar{display:grid;grid-template-columns:minmax(180px,1fr) 170px 150px auto;gap:8px;margin:13px 0}.cache-toolbar input,.cache-toolbar select{min-width:0}.cache-list{display:grid;gap:8px;max-height:690px;overflow:auto;padding-right:4px}.cache-row{display:grid;grid-template-columns:42px minmax(105px,.55fr) minmax(190px,1.35fr) 94px 74px;gap:9px;align-items:center;width:100%;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--bg);color:var(--ink);text-align:left}.cache-row:hover,.cache-row.selected{border-color:var(--blue);box-shadow:0 0 0 3px #2671df14}.cache-row.disabled{opacity:.62}.cache-row .order{display:grid;place-items:center;width:30px;height:30px;border-radius:9px;background:var(--soft);font-weight:900}.cache-row .keyword b,.cache-row .question b{display:block;font-size:11px;line-height:1.4}.cache-row .keyword small,.cache-row .question small{display:block;margin-top:4px;color:var(--muted);font-size:8px;line-height:1.4}.cache-state{display:grid;gap:4px}.cache-state span{width:max-content;padding:4px 7px;border-radius:99px;font-size:8px;font-weight:850}.cache-ready{background:#e7f8f1;color:#087c58}.cache-missing{background:#fff3d9;color:#9a6500}.cache-disabled{background:#eef1f5;color:#667488}.cache-hits{text-align:right}.cache-hits b{display:block;font-size:14px}.cache-hits small{font-size:8px;color:var(--muted)}.cache-editor-header{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin-bottom:13px}.cache-editor-header h2{margin:4px 0;font-size:17px}.cache-editor-header p{margin:0;color:var(--muted);font-size:9px}.cache-id{font-size:8px;color:var(--muted);word-break:break-all}.cache-form{display:grid;grid-template-columns:1fr 1fr;gap:10px}.cache-form label{display:grid;gap:5px}.cache-form label.full{grid-column:1/-1}.cache-form label span{font-size:8px;color:var(--muted);font-weight:800}.cache-form textarea{min-height:72px;resize:vertical;line-height:1.55}.cache-answer{margin-top:14px;padding-top:14px;border-top:1px solid var(--line)}.cache-answer h3,.cache-history h3{margin:4px 0 5px;font-size:13px}.cache-answer>p,.cache-history>p{margin:0 0 10px;color:var(--muted);font-size:9px;line-height:1.55}.cache-answer textarea{width:100%;min-height:190px;resize:vertical;line-height:1.65}.cache-answer .source-editor{min-height:74px;margin-top:8px;font-size:9px}.cache-actions{display:flex;gap:7px;justify-content:flex-end;flex-wrap:wrap;margin-top:11px}.cache-history{margin-top:14px;padding-top:14px;border-top:1px solid var(--line)}.cache-version{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;padding:9px 0;border-top:1px solid var(--line)}.cache-version:first-of-type{border-top:0}.cache-version b,.cache-version small{display:block}.cache-version b{font-size:9px}.cache-version small{margin-top:3px;color:var(--muted);font-size:8px}.cache-policy-note{margin-bottom:14px;padding:11px 13px;border-left:4px solid var(--blue);border-radius:9px;background:var(--soft);font-size:9px;line-height:1.6}.cache-empty-editor{min-height:480px;display:grid;place-items:center;color:var(--muted);text-align:center}.cache-empty-editor b{display:block;color:var(--ink);font-size:15px;margin-bottom:6px}.admin-app.dark .cache-ready{background:#123c32;color:#7be1bd}.admin-app.dark .cache-missing{background:#3d3014;color:#ffd477}.admin-app.dark .cache-disabled{background:#253346;color:#a9b8ca}@media(max-width:1100px){.cache-admin-layout{grid-template-columns:1fr}.cache-admin-summary{grid-template-columns:repeat(2,1fr)}}@media(max-width:720px){.cache-toolbar{grid-template-columns:1fr}.cache-row{grid-template-columns:38px 1fr 80px}.cache-row .question{grid-column:2/-1}.cache-admin-summary,.cache-form{grid-template-columns:1fr}.cache-form label.full{grid-column:auto}}
+`;
+document.head.appendChild(suggestionCacheStyle);
+Object.assign(state,{suggestionCache:null,selectedSuggestionId:'',suggestionCacheSearch:'',suggestionCacheBusiness:'all',suggestionCacheStatus:'all',suggestionCacheNew:false,suggestionCacheJob:null});
+navItems.push(['suggestion_cache','⚡','질의 캐시 관리','키워드·검증 답변']);
+if(monitoringNavigation){monitoringNavigation.items=['monitoring','suggestion_cache','jobs'];monitoringNavigation.cap='운영 현황·질의 캐시·챗봇 로그';}
+function cacheCatalog(){return state.suggestionCache?.items||[];}
+function selectedSuggestion(){return cacheCatalog().find(row=>row.suggestion_id===state.selectedSuggestionId)||null;}
+function cacheStatusMarkup(row){if(row.draft_change)return '<span class="cache-missing">반영 대기</span>';if(!row.is_enabled)return '<span class="cache-disabled">챗봇 제외</span>';if(row.has_active_answer)return '<span class="cache-ready">즉시 응답</span>';return '<span class="cache-missing">답변 필요</span>';}
+function cacheSourcesText(rows){return (rows||[]).map(row=>`${row.title||'공식 안내'} | ${row.url||''}`).join('\n');}
+function parseCacheSources(value){return String(value||'').split(/\r?\n/).map(line=>line.trim()).filter(Boolean).map(line=>{const parts=line.split('|');return {title:(parts.length>1?parts.shift():'공식 안내').trim(),url:parts.join('|').trim()};}).filter(row=>/^https?:\/\//i.test(row.url));}
+function cacheCatalogRows(){const search=state.suggestionCacheSearch.toLowerCase(),business=state.suggestionCacheBusiness,status=state.suggestionCacheStatus;return cacheCatalog().filter(row=>{const text=`${row.business_key} ${row.business} ${row.keyword} ${row.canonical_question}`.toLowerCase();const statusMatch=status==='all'||(status==='ready'&&row.is_enabled&&row.has_active_answer)||(status==='missing'&&row.is_enabled&&!row.has_active_answer)||(status==='disabled'&&!row.is_enabled);return (!search||text.includes(search))&&(business==='all'||row.business_key===business)&&statusMatch;});}
+function suggestionEditor(row){const isNew=state.suggestionCacheNew;if(!row&&!isNew)return '<article class="panel cache-editor cache-empty-editor"><div><b>관리할 키워드를 선택하세요.</b><span>왼쪽 목록에서 선택하거나 새 키워드를 추가할 수 있습니다.</span></div></article>';const item=row||{suggestion_id:'새 항목',business_key:'',business:'',keyword:'',canonical_question:'',display_order:(state.suggestionCache?.summary?.registered||0)+1,is_enabled:true,row_version:1,answer:'',sources:[],versions:[]};const versions=item.versions||[];return `<article class="panel cache-editor"><div class="cache-editor-header"><div><span class="section-kicker">${isNew?'ADD KEYWORD':'CACHE DETAIL'}</span><h2>${isNew?'새 질의 캐시 추가':esc(item.keyword)}</h2><p class="cache-id">${esc(item.suggestion_id)}</p></div>${!isNew?cacheStatusMarkup(item):''}</div><div class="cache-policy-note">키워드나 표준 질문을 바꾸면 기존 검증 답변은 자동으로 분리됩니다. 이전 답변은 이력에 남아 필요할 때 다시 선택할 수 있습니다.</div><div class="cache-form"><label><span>업무 키</span><input id="cache-business-key" maxlength="100" value="${attr(item.business_key)}" placeholder="예: 착오송금"></label><label><span>챗봇 표시 업무명</span><input id="cache-business" maxlength="200" value="${attr(item.business)}" placeholder="예: 착오송금 반환 신청"></label><label><span>키워드 버튼명</span><input id="cache-keyword" maxlength="100" value="${attr(item.keyword)}" placeholder="예: 신청 대상"></label><label><span>표시 순서</span><input id="cache-order" type="number" min="1" max="32767" value="${attr(item.display_order)}"></label><label class="full"><span>버튼 클릭 시 전달할 표준 질문</span><textarea id="cache-question" maxlength="4000">${esc(item.canonical_question)}</textarea></label></div><div class="cache-actions"><button class="primary-btn" data-action="${isNew?'create-suggestion-cache':'save-suggestion-cache'}">${isNew?'키워드 추가':'기본 정보 저장'}</button>${!isNew?(item.is_enabled?'<button class="icon-danger" data-action="disable-suggestion-cache">챗봇에서 제거</button>':'<button class="secondary-btn" data-action="restore-suggestion-cache">챗봇에 복구</button>'):''}</div>${!isNew?`<section class="cache-answer"><span class="section-kicker">VALIDATED ANSWER</span><h3>즉시 응답할 검증 답변</h3><p>직접 수정해 저장하거나, 현재 챗봇 파이프라인으로 다시 생성할 수 있습니다. 직접 저장한 답변도 새 버전으로 남습니다.</p><textarea id="cache-answer" placeholder="검증된 답변을 입력하세요.">${esc(item.answer||'')}</textarea><textarea id="cache-sources" class="source-editor" placeholder="출처명 | https://공식URL (한 줄에 하나)">${esc(cacheSourcesText(item.sources))}</textarea><div class="cache-actions"><button class="secondary-btn" data-action="generate-suggestion-answer" ${!item.is_enabled?'disabled':''}>챗봇으로 재생성</button><button class="primary-btn" data-action="save-suggestion-answer">답변 새 버전 저장</button><button class="icon-danger" data-action="clear-suggestion-answer" ${!item.has_active_answer?'disabled':''}>활성 답변 비우기</button></div></section><section class="cache-history"><span class="section-kicker">ANSWER HISTORY</span><h3>검증 답변 버전</h3><p>현재 답변을 지우더라도 이전 버전은 남아 있으며 언제든 다시 활성화할 수 있습니다.</p>${versions.length?versions.map(version=>`<div class="cache-version"><div><b>${version.is_active?'● 현재 사용 · ':''}${esc(version.pipeline_name||'생성 버전')}</b><small>${fmtDate(version.created_at)} · 사용 ${num(version.hit_count)}회 · ${esc(version.answer_preview||'답변 미리보기 없음')}</small></div>${version.is_active?'<span class="test-badge">사용 중</span>':`<button class="secondary-btn" data-action="activate-suggestion-version" data-key="${attr(version.cache_key)}">이 버전 사용</button>`}</div>`).join(''):'<div class="empty-list">저장된 검증 답변 버전이 없습니다.</div>'}</section>`:''}</article>`;}
+const suggestionEditorBeforeStaging=suggestionEditor;
+suggestionEditor=function(row){let html=suggestionEditorBeforeStaging(row);html=html.replace('키워드나 표준 질문을 바꾸면 기존 검증 답변은 자동으로 분리됩니다. 이전 답변은 이력에 남아 필요할 때 다시 선택할 수 있습니다.','여기서 저장한 변경은 반영 초안입니다. 반영센터 승인 전까지 현재 키워드와 검증 답변이 그대로 운영됩니다.').replace('키워드 추가</button>','추가 초안 저장</button>').replace('기본 정보 저장</button>','수정 초안 저장</button>').replace('직접 수정해 저장하거나, 현재 챗봇 파이프라인으로 다시 생성할 수 있습니다. 직접 저장한 답변도 새 버전으로 남습니다.','답변 수정과 챗봇 재생성 결과도 초안으로 저장됩니다. 승인 후 새 버전으로 활성화됩니다.').replace('>챗봇으로 재생성</button>','>답변 후보 생성</button>').replace('>답변 새 버전 저장</button>','>답변 수정 초안 저장</button>').replace('>활성 답변 비우기</button>','>답변 비우기 초안</button>');if(row?.draft_change)html=html.replace('<div class="cache-policy-note">','<div class="cache-actions"><button class="icon-danger" data-action="cancel-suggestion-draft">이 항목 초안 취소</button><button class="secondary-btn" data-view="apply">반영센터에서 확인</button></div><div class="cache-policy-note">');return html;};
+function suggestionCacheView(){const data=state.suggestionCache||{summary:{},items:[]},summary=data.summary||{},businesses=[...new Set(cacheCatalog().map(row=>row.business_key).filter(Boolean))],rows=cacheCatalogRows();return `${heading('MANAGED QUERY CACHE','질의 캐시 관리','추천 키워드 버튼과 검증 답변을 초안으로 관리합니다. 저장한 변경은 반영센터 승인 전까지 운영 챗봇에 영향을 주지 않습니다.','<div class="button-row"><button class="secondary-btn" data-view="apply">반영센터에서 확인</button><button class="primary-btn" data-action="refresh-suggestion-cache">↻ 다시 읽기</button></div>')}<div class="risk-banner"><b>i</b><span><strong>모든 추가·수정·삭제는 반영 초안입니다.</strong>답변 버전은 보존되고, 통합 반영 후 다음 챗봇 요청부터 적용됩니다.</span></div><section class="cache-admin-summary">${metric('등록 키워드',num(summary.registered).toLocaleString(),`초안 포함 · 운영 사용 ${num(summary.enabled)}개`)}${metric('반영 대기',num(summary.pending).toLocaleString(),'반영센터 승인 필요','cyan')}${metric('즉시 응답 가능',num(summary.ready).toLocaleString(),`답변 필요 ${num(summary.missing)}개`,'purple')}${metric('응답 정책','ID + 표준 질문','둘 다 일치할 때만 HIT','red')}</section><section class="cache-admin-layout"><article class="panel cache-catalog"><div class="panel-head"><div><span class="section-kicker">KEYWORD CATALOG</span><h2>키워드 목록</h2></div><button class="primary-btn" data-action="new-suggestion-cache">＋ 새 키워드</button></div><div class="cache-toolbar"><input id="cache-search" value="${attr(state.suggestionCacheSearch)}" placeholder="업무·키워드·질문 검색"><select id="cache-business-filter"><option value="all">전체 업무</option>${businesses.map(value=>`<option value="${attr(value)}" ${state.suggestionCacheBusiness===value?'selected':''}>${esc(value)}</option>`).join('')}</select><select id="cache-status-filter"><option value="all">전체 상태</option><option value="ready" ${state.suggestionCacheStatus==='ready'?'selected':''}>즉시 응답</option><option value="missing" ${state.suggestionCacheStatus==='missing'?'selected':''}>답변 필요</option><option value="disabled" ${state.suggestionCacheStatus==='disabled'?'selected':''}>챗봇 제외</option></select><span class="test-badge">${rows.length}개 표시</span></div><div class="cache-list">${rows.length?rows.map(row=>`<button class="cache-row ${row.suggestion_id===state.selectedSuggestionId?'selected':''} ${!row.is_enabled?'disabled':''}" data-action="select-suggestion-cache" data-id="${attr(row.suggestion_id)}"><span class="order">${num(row.display_order)}</span><span class="keyword"><b>${esc(row.keyword)}</b><small>${esc(row.business_key)}</small></span><span class="question"><b>${esc(row.canonical_question)}</b><small>${esc(row.suggestion_id)}</small></span><span class="cache-state">${cacheStatusMarkup(row)}</span><span class="cache-hits"><b>${num(row.hit_count)}</b><small>사용</small></span></button>`).join(''):'<div class="empty-list">조건에 맞는 키워드가 없습니다.</div>'}</div></article>${suggestionEditor(selectedSuggestion())}</section>`;}
+async function refreshSuggestionCache(notify=true){try{state.suggestionCache=await api('/api/admin-ui/suggestion-cache');if(state.selectedSuggestionId&&!cacheCatalog().some(row=>row.suggestion_id===state.selectedSuggestionId))state.selectedSuggestionId='';if(notify)toast('질의 캐시 상태를 다시 읽었습니다.');render();}catch(error){toast(error.message,true);}}
+function cacheFormPayload(){return {business_key:document.getElementById('cache-business-key')?.value.trim()||'',business:document.getElementById('cache-business')?.value.trim()||'',keyword:document.getElementById('cache-keyword')?.value.trim()||'',canonical_question:document.getElementById('cache-question')?.value.trim()||'',display_order:num(document.getElementById('cache-order')?.value)};}
+async function createSuggestionCache(){const payload=cacheFormPayload();if(!payload.business_key||!payload.business||!payload.keyword||!payload.canonical_question)return toast('업무 키, 표시 업무명, 키워드, 표준 질문을 모두 입력해 주세요.',true);try{const out=await api('/api/admin-ui/suggestion-cache',{method:'POST',body:JSON.stringify(payload)});state.suggestionCache=out.catalog;state.draft=out.draft;state.selectedSuggestionId=out.item.suggestion_id;state.suggestionCacheNew=false;render();toast('새 키워드를 반영 초안에 저장했습니다. 검증 답변을 추가한 뒤 반영센터에서 승인해 주세요.');}catch(error){toast(error.message,true);}}
+async function saveSuggestionCache(){const row=selectedSuggestion(),payload={...cacheFormPayload(),row_version:row?.row_version};if(!row)return;try{const out=await api(`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}`,{method:'PUT',body:JSON.stringify(payload)});state.suggestionCache=out.catalog;state.draft=out.draft;render();toast('키워드와 표준 질문의 수정 초안을 저장했습니다.');}catch(error){toast(error.message,true);}}
+async function setSuggestionEnabled(enabled){const row=selectedSuggestion();if(!row)return;if(!enabled&&!confirm(`‘${row.keyword}’ 버튼을 챗봇에서 제거하는 초안을 만들까요? 반영센터 승인 전까지 운영에는 남아 있습니다.`))return;try{const path=`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}${enabled?'/restore':''}`;const out=await api(path,{method:enabled?'POST':'DELETE',body:enabled?'{}':undefined});state.suggestionCache=out.catalog;state.draft=out.draft;render();toast(enabled?'챗봇 복구 초안을 저장했습니다.':'챗봇 제거 초안을 저장했습니다.');}catch(error){toast(error.message,true);}}
+async function saveSuggestionAnswer(){const row=selectedSuggestion(),answer=document.getElementById('cache-answer')?.value.trim()||'',sources=parseCacheSources(document.getElementById('cache-sources')?.value);if(!row||!answer)return toast('저장할 검증 답변을 입력해 주세요.',true);if(!sources.length)return toast('금융 답변은 예금보험공사 공식 출처 URL을 1개 이상 입력해야 저장할 수 있습니다.',true);try{const out=await api(`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}/answer`,{method:'PUT',body:JSON.stringify({answer,sources})});state.suggestionCache=out.catalog;state.draft=out.draft;render();toast('검증 답변 수정안을 반영 초안에 저장했습니다.');}catch(error){toast(error.message,true);}}
+async function clearSuggestionAnswer(){const row=selectedSuggestion();if(!row||!confirm('활성 답변을 비우는 초안을 만들까요? 반영센터 승인 전까지 현재 답변은 계속 사용됩니다.'))return;try{const out=await api(`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}/answer`,{method:'DELETE'});state.suggestionCache=out.catalog;state.draft=out.draft;render();toast('활성 답변 비우기 초안을 저장했습니다.');}catch(error){toast(error.message,true);}}
+async function activateSuggestionVersion(cacheKey){const row=selectedSuggestion();if(!row)return;try{const out=await api(`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}/activate`,{method:'POST',body:JSON.stringify({cache_key:cacheKey})});state.suggestionCache=out.catalog;state.draft=out.draft;render();toast('선택한 답변 버전을 사용하는 반영 초안을 저장했습니다.');}catch(error){toast(error.message,true);}}
+async function cancelSuggestionDraft(){const row=selectedSuggestion();if(!row?.draft_change)return;try{const out=await api(`/api/admin-ui/draft/suggestion-cache/${encodeURIComponent(row.suggestion_id)}`,{method:'DELETE'});state.draft=out.draft;state.suggestionCache=out.catalog;if(row.draft_change.create)state.selectedSuggestionId='';render();toast('이 질의 캐시 항목의 반영 초안을 취소했습니다.');}catch(error){toast(error.message,true);}}
+async function generateSuggestionAnswer(){const row=selectedSuggestion();if(!row||!confirm('현재 운영 답변은 유지한 채 최신 챗봇으로 답변 후보를 생성해 반영 초안에 넣을까요?'))return;try{const out=await api(`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}/generate`,{method:'POST',body:'{}'});state.suggestionCacheJob=out.job_id;toast('답변 후보 생성을 시작했습니다. 운영 캐시는 변경되지 않습니다.');for(let attempt=0;attempt<180;attempt++){await sleep(1000);const job=await api(`/api/jobs/${encodeURIComponent(out.job_id)}`);if(job.status==='done'){const result=job.result||{},answer=String(result.answer||'').trim(),sources=result.official_sources||result.sources||[];if(!answer)throw new Error('생성된 답변 본문이 없습니다.');const staged=await api(`/api/admin-ui/suggestion-cache/${encodeURIComponent(row.suggestion_id)}/answer`,{method:'PUT',body:JSON.stringify({answer,sources})});state.suggestionCache=staged.catalog;state.draft=staged.draft;render();toast('생성된 답변 후보를 반영 초안에 저장했습니다.');return;}if(job.status==='error')throw new Error(job.error||'답변 생성에 실패했습니다.');}throw new Error('생성 시간이 길어지고 있습니다. 챗봇 로그에서 작업 상태를 확인해 주세요.');}catch(error){toast(error.message,true);}finally{state.suggestionCacheJob=null;}}
+const renderBeforeSuggestionCache=render;render=function(){if(state.view!=='suggestion_cache')return renderBeforeSuggestionCache();const oldNav=root.querySelector('.sidebar nav');if(oldNav)state.sidebarScroll=oldNav.scrollTop;root.innerHTML=shell(suggestionCacheView());requestAnimationFrame(()=>{const nav=root.querySelector('.sidebar nav');if(nav)nav.scrollTop=state.sidebarScroll||0;});};
+const bootstrapBeforeSuggestionCache=bootstrap;bootstrap=async function(){await bootstrapBeforeSuggestionCache();if(state.summary)await refreshSuggestionCache(false);};
+const refreshAllBeforeSuggestionCache=refreshAll;refreshAll=async function(silent=false){await refreshAllBeforeSuggestionCache(silent);if(state.summary)await refreshSuggestionCache(false);};
+let suggestionSearchTimer=null;
+root.addEventListener('input',event=>{if(event.target?.id==='cache-search'){state.suggestionCacheSearch=event.target.value;const caret=event.target.selectionStart;clearTimeout(suggestionSearchTimer);suggestionSearchTimer=setTimeout(()=>{render();requestAnimationFrame(()=>{const input=document.getElementById('cache-search');if(input){input.focus();input.setSelectionRange(caret,caret);}});},120);}},true);
+root.addEventListener('change',event=>{if(event.target?.id==='cache-business-filter'){state.suggestionCacheBusiness=event.target.value;render();}if(event.target?.id==='cache-status-filter'){state.suggestionCacheStatus=event.target.value;render();}},true);
+root.addEventListener('click',event=>{const button=event.target.closest?.('button[data-action]'),action=button?.dataset?.action;if(!['refresh-suggestion-cache','new-suggestion-cache','select-suggestion-cache','create-suggestion-cache','save-suggestion-cache','disable-suggestion-cache','restore-suggestion-cache','save-suggestion-answer','clear-suggestion-answer','generate-suggestion-answer','activate-suggestion-version','cancel-suggestion-draft'].includes(action))return;event.preventDefault();event.stopImmediatePropagation();if(action==='refresh-suggestion-cache')refreshSuggestionCache();if(action==='new-suggestion-cache'){state.suggestionCacheNew=true;state.selectedSuggestionId='';render();}if(action==='select-suggestion-cache'){state.suggestionCacheNew=false;state.selectedSuggestionId=button.dataset.id||'';render();}if(action==='create-suggestion-cache')createSuggestionCache();if(action==='save-suggestion-cache')saveSuggestionCache();if(action==='disable-suggestion-cache')setSuggestionEnabled(false);if(action==='restore-suggestion-cache')setSuggestionEnabled(true);if(action==='save-suggestion-answer')saveSuggestionAnswer();if(action==='clear-suggestion-answer')clearSuggestionAnswer();if(action==='generate-suggestion-answer')generateSuggestionAnswer();if(action==='activate-suggestion-version')activateSuggestionVersion(button.dataset.key);if(action==='cancel-suggestion-draft')cancelSuggestionDraft();},true);
+function cacheDraftLabel(change){const labels=[];if(change.create)labels.push('신규 키워드');else if(change.metadata)labels.push('기본정보 수정');if(change.enabled===false)labels.push('챗봇 제외');if(change.enabled===true&&!change.create)labels.push('챗봇 복구');const action=change.answer_change?.action;if(action==='save')labels.push('답변 수정');if(action==='clear')labels.push('답변 비우기');if(action==='activate')labels.push('이전 답변 복원');return labels.join(' · ')||'변경';}
+function cacheDraftName(change){return change.metadata?.keyword||cacheCatalog().find(row=>row.suggestion_id===change.suggestion_id)?.keyword||change.suggestion_id;}
+const applyViewBeforeSuggestionDraft=applyViewV2;
+applyViewV2=function(){const base=applyViewBeforeSuggestionDraft(),changes=state.draft?.suggestion_cache||[],history=(state.draft?.history||[]).filter(row=>(row.summary?.suggestion_cache||[]).length);const pending=`<section class="panel rechunk-draft-panel"><div class="panel-head"><div><span class="section-kicker">QUERY CACHE DRAFT</span><h2>질의 캐시 변경 ${changes.length}건</h2></div><button class="secondary-btn" data-view="suggestion_cache">질의 캐시 관리로 이동</button></div><p class="rechunk-note">승인 전에는 현재 키워드와 캐시 답변이 그대로 운영됩니다. 각 항목은 여기서 개별 취소할 수 있습니다.</p>${changes.length?`<ul class="rechunk-draft-list">${changes.map(change=>`<li><b>${esc(cacheDraftName(change))}</b><span>${esc(cacheDraftLabel(change))}</span><small>${esc(change.suggestion_id)}</small><button class="icon-danger" data-action="cancel-cache-draft-from-apply" data-id="${attr(change.suggestion_id)}">초안 취소</button></li>`).join('')}</ul>`:'<div class="empty-list">질의 캐시 변경 초안이 없습니다.</div>'}</section>`;const audit=history.length?`<section class="panel rechunk-draft-panel"><span class="section-kicker">QUERY CACHE ROLLBACK</span><h2>질의 캐시 포함 반영 이력</h2><p class="rechunk-note">아래 통합 이력을 복구하면 당시 키워드·활성 답변 포인터까지 함께 돌아갑니다.</p><div class="chip-list">${history.map(row=>`<span>${esc(row.version_id)} · 캐시 ${(row.summary.suggestion_cache||[]).length}건</span>`).join('')}</div></section>`:'';return base.replace('<section class="panel apply-panel">',`${pending}<section class="panel apply-panel">`).replace('<section class="panel jobs-panel">',`${audit}<section class="panel jobs-panel">`);};
+async function cancelCacheDraftFromApply(suggestionId){try{const out=await api(`/api/admin-ui/draft/suggestion-cache/${encodeURIComponent(suggestionId)}`,{method:'DELETE'});state.draft=out.draft;state.suggestionCache=out.catalog;render();toast('선택한 질의 캐시 초안을 취소했습니다.');}catch(error){toast(error.message,true);}}
+root.addEventListener('click',event=>{const button=event.target.closest?.('button[data-action="cancel-cache-draft-from-apply"]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();cancelCacheDraftFromApply(button.dataset.id||'');},true);
 bootstrap();
 })();
 
diff --git a/Development/ec2_deploy/kdic_deploy_assets/kdic_admin_extension_aws.py b/Development/ec2_deploy/kdic_deploy_assets/kdic_admin_extension_aws.py
index 304df91..58d3b96 100644
--- a/Development/ec2_deploy/kdic_deploy_assets/kdic_admin_extension_aws.py
+++ b/Development/ec2_deploy/kdic_deploy_assets/kdic_admin_extension_aws.py
@@ -10,6 +10,7 @@ import json
 import math
 import os
 import pickle
+import re
 import secrets
 import threading
 import time
@@ -17,6 +18,7 @@ import uuid
 from concurrent.futures import ThreadPoolExecutor
 from pathlib import Path
 from typing import Any, Mapping, MutableMapping, Sequence
+from urllib.parse import urlparse
 
 import numpy as np
 import pandas as pd
@@ -29,6 +31,7 @@ COOKIE_NAME = "kdic_admin_session"
 SESSION_TTL_SECONDS = 8 * 60 * 60
 MAX_DATASET_BYTES = 25 * 1024 * 1024
 MAX_PARAMETER_PRESETS = 30
+MAX_PROMPT_PRESETS = 30
 ALLOWED_CONFIG = {
     "dense_weight", "bm25_weight", "candidate_depth", "final_top_k",
     "query_fusion_rrf_k", "min_relevance_score", "parent_child", "parent_context_max_chars",
@@ -88,6 +91,11 @@ class ParameterPresetPayload(BaseModel):
     values: dict[str, Any]
 
 
+class PromptPresetPayload(BaseModel):
+    name: str = Field(min_length=1, max_length=60)
+    values: dict[str, str]
+
+
 class ChunkAddPayload(BaseModel):
     chunk: dict[str, Any]
 
@@ -127,6 +135,7 @@ class PromptDraftPayload(BaseModel):
 
 class PromptComparePayload(BaseModel):
     question: str = Field(min_length=2, max_length=4_000)
+    preset_id: str | None = Field(default=None, max_length=100)
 
 
 class PromptApplyPayload(BaseModel):
@@ -137,6 +146,45 @@ class PipelineLabelsPayload(BaseModel):
     labels: dict[str, str] = Field(default_factory=dict)
 
 
+class RechunkPreviewPayload(BaseModel):
+    """Request a safe preview for one already-crawled source URL."""
+
+    source_url: str = Field(min_length=8, max_length=4_000)
+
+
+class SuggestionCreatePayload(BaseModel):
+    business_key: str = Field(min_length=1, max_length=100)
+    business: str = Field(min_length=1, max_length=200)
+    keyword: str = Field(min_length=1, max_length=100)
+    canonical_question: str = Field(min_length=2, max_length=4_000)
+    display_order: int | None = Field(default=None, ge=1, le=32_767)
+
+
+class SuggestionUpdatePayload(SuggestionCreatePayload):
+    display_order: int = Field(ge=1, le=32_767)
+    row_version: int = Field(ge=1)
+
+
+class SuggestionAnswerPayload(BaseModel):
+    answer: str = Field(min_length=2, max_length=30_000)
+    sources: list[dict[str, str]] = Field(default_factory=list, max_length=20)
+
+
+class SuggestionVersionPayload(BaseModel):
+    cache_key: str = Field(min_length=1, max_length=500)
+
+
+class NewUrlCrawlPayload(BaseModel):
+    """Create an isolated crawl/chunk preview for one new KDIC page."""
+
+    source_url: str = Field(min_length=8, max_length=4_000)
+    document_id: str = Field(min_length=3, max_length=80)
+    business_function: str = Field(min_length=1, max_length=200)
+    title: str = Field(min_length=1, max_length=500)
+    target_business_function: str | None = Field(default=None, max_length=200)
+    page_type: str = Field(default="static_page", min_length=1, max_length=100)
+
+
 def _clean(value: Any) -> str:
     return " ".join(str(value or "").split()).strip()
 
@@ -145,6 +193,15 @@ def _safe(value: Any) -> Any:
     return value if value is None or isinstance(value, (str, int, float, bool)) else str(value)
 
 
+def _is_public_kdic_url(value: str) -> bool:
+    try:
+        parsed = urlparse(value)
+        host = (parsed.hostname or "").lower().rstrip(".")
+        return parsed.scheme.lower() in {"http", "https"} and (host == "kdic.or.kr" or host.endswith(".kdic.or.kr"))
+    except Exception:
+        return False
+
+
 DEFAULT_PIPELINE_GRAPH_SPEC: list[dict[str, Any]] = [
     {
         "id": "question",
@@ -579,20 +636,182 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
     """Cookie-authenticated admin UI with isolated A/B eval and staged apply/rollback."""
     app, page = service_module.app, Path(html_path).resolve()
     auth_lock, mutation_lock, eval_lock = threading.RLock(), threading.RLock(), threading.RLock()
+    rechunk_lock = threading.RLock()
     prompt_compare_lock = threading.Lock()
     report_lock = threading.Lock()
     bootstrap_state = {"used": False}
     datasets: dict[str, dict[str, Any]] = {}
     eval_jobs: dict[str, dict[str, Any]] = {}
     executor = ThreadPoolExecutor(max_workers=1, thread_name_prefix="kdic-admin-eval")
-    drafts: dict[str, Any] = {"config": {}, "add": {}, "remove": set()}
+    rechunk_executor = ThreadPoolExecutor(max_workers=1, thread_name_prefix="kdic-admin-rechunk")
+    rechunk_jobs: dict[str, dict[str, Any]] = {}
+    suggestion_cache = getattr(service_module, "SUGGESTION_ANSWER_CACHE", None)
+    drafts: dict[str, Any] = {
+        "config": {},
+        "add": {},
+        "remove": set(),
+        "rechunk": [],
+        "suggestion_cache": {},
+    }
     history_path = Path(os.getenv("KDIC_ADMIN_HISTORY_PATH", "/opt/kdic/runtime/admin_change_history.pkl")).resolve()
     default_snapshot_path = Path(os.getenv("KDIC_ADMIN_DEFAULT_SNAPSHOT_PATH", "/opt/kdic/runtime/admin_default_snapshot.pkl")).resolve()
     pipeline_labels_path = Path(os.getenv("KDIC_PIPELINE_LABELS_PATH", "/opt/kdic/runtime/admin_pipeline_labels.json")).resolve()
     parameter_presets_path = Path(os.getenv("KDIC_PARAMETER_PRESETS_PATH", "/opt/kdic/runtime/admin_parameter_presets.json")).resolve()
+    prompt_presets_path = Path(os.getenv("KDIC_PROMPT_PRESETS_PATH", "/opt/kdic/runtime/admin_prompt_presets.json")).resolve()
     admin_sessions_path = Path(os.getenv("KDIC_ADMIN_SESSIONS_PATH", "/opt/kdic/runtime/admin_sessions.json")).resolve()
     evaluation_state_path = Path(os.getenv("KDIC_ADMIN_EVALUATIONS_PATH", "/opt/kdic/runtime/admin_evaluations.pkl")).resolve()
 
+    def require_suggestion_cache() -> Any:
+        required = (
+            "list_catalog",
+            "create_suggestion",
+            "update_suggestion",
+            "set_enabled",
+            "clear_active",
+            "versions",
+            "save_manual_answer",
+            "activate",
+            "snapshot_catalog",
+            "restore_catalog",
+        )
+        if suggestion_cache is None or any(
+            not callable(getattr(suggestion_cache, name, None)) for name in required
+        ):
+            raise HTTPException(
+                status_code=503,
+                detail="현재 챗봇의 영속 질의 캐시 관리 기능이 연결되지 않았습니다.",
+            )
+        return suggestion_cache
+
+    def suggestion_public() -> dict[str, Any]:
+        manager = require_suggestion_cache()
+        rows = manager.list_catalog(include_disabled=True)
+        for row in rows:
+            public_result = dict(row.pop("public_result", None) or {})
+            row["answer"] = str(public_result.get("answer") or "").strip()
+            row["sources"] = copy.deepcopy(
+                public_result.get("sources")
+                or public_result.get("official_sources")
+                or []
+            )
+            row["has_active_answer"] = bool(row.get("active_cache_key")) and bool(
+                row["answer"]
+            )
+            row["versions"] = manager.versions(row["suggestion_id"])
+            for version in row["versions"]:
+                result = dict(version.pop("public_result", None) or {})
+                version["answer_preview"] = _clean(result.get("answer"))[:240]
+        by_id = {row["suggestion_id"]: row for row in rows}
+        for suggestion_id, change in drafts["suggestion_cache"].items():
+            row = by_id.get(suggestion_id)
+            if row is None and change.get("create"):
+                metadata = dict(change.get("metadata") or {})
+                row = {
+                    "suggestion_id": suggestion_id,
+                    **metadata,
+                    "is_enabled": bool(change.get("enabled", True)),
+                    "is_locked": False,
+                    "active_cache_key": None,
+                    "row_version": 1,
+                    "hit_count": 0,
+                    "last_hit_at": None,
+                    "answer": "",
+                    "sources": [],
+                    "has_active_answer": False,
+                    "versions": [],
+                    "created_by": "ADMIN_UI_DRAFT",
+                    "updated_by": "ADMIN_UI_DRAFT",
+                }
+                rows.append(row)
+                by_id[suggestion_id] = row
+            if row is None:
+                continue
+            row["operating_state"] = {
+                key: copy.deepcopy(row.get(key))
+                for key in (
+                    "business_key", "business", "keyword", "canonical_question",
+                    "display_order", "is_enabled", "active_cache_key", "answer", "sources",
+                )
+            }
+            if change.get("metadata"):
+                row.update(copy.deepcopy(change["metadata"]))
+            if "enabled" in change:
+                row["is_enabled"] = bool(change["enabled"])
+            answer_change = dict(change.get("answer_change") or {})
+            if answer_change.get("action") == "save":
+                row["answer"] = answer_change.get("answer", "")
+                row["sources"] = copy.deepcopy(answer_change.get("sources") or [])
+                row["has_active_answer"] = True
+            elif answer_change.get("action") == "clear":
+                row["answer"] = ""
+                row["sources"] = []
+                row["has_active_answer"] = False
+            elif answer_change.get("action") == "activate":
+                selected = next(
+                    (
+                        version for version in row.get("versions") or []
+                        if version.get("cache_key") == answer_change.get("cache_key")
+                    ),
+                    None,
+                )
+                if selected:
+                    row["answer"] = selected.get("answer_preview", "")
+                    row["has_active_answer"] = True
+            row["draft_change"] = copy.deepcopy(change)
+        rows.sort(key=lambda row: (int(row.get("display_order") or 0), row["suggestion_id"]))
+        enabled = [row for row in rows if row.get("is_enabled")]
+        ready = [row for row in enabled if row.get("has_active_answer")]
+        return {
+            "items": rows,
+            "summary": {
+                "registered": len(rows),
+                "enabled": len(enabled),
+                "ready": len(ready),
+                "missing": len(enabled) - len(ready),
+                "hits": sum(int(row.get("hit_count") or 0) for row in rows),
+                "pending": len(drafts["suggestion_cache"]),
+            },
+            "draft_changes": copy.deepcopy(list(drafts["suggestion_cache"].values())),
+            "policy": {
+                "exact_id_and_question": True,
+                "edit_invalidates_answer": True,
+                "delete_is_reversible": True,
+                "answer_history_retained": True,
+            },
+        }
+
+    def stage_suggestion_change(suggestion_id: str, **values: Any) -> dict[str, Any]:
+        clean_id = _clean(suggestion_id).upper()
+        current = copy.deepcopy(drafts["suggestion_cache"].get(clean_id) or {})
+        current.update({key: copy.deepcopy(value) for key, value in values.items()})
+        current["suggestion_id"] = clean_id
+        current["staged_at"] = time.time()
+        drafts["suggestion_cache"][clean_id] = current
+        return current
+
+    def require_known_suggestion(suggestion_id: str) -> str:
+        clean_id = _clean(suggestion_id).upper()
+        known = {
+            row["suggestion_id"]
+            for row in require_suggestion_cache().list_catalog(include_disabled=True)
+        }
+        if clean_id not in known and not drafts["suggestion_cache"].get(clean_id, {}).get("create"):
+            raise KeyError(suggestion_id)
+        return clean_id
+
+    def suggestion_id_for(payload: SuggestionCreatePayload) -> str:
+        return "SQ-" + uuid.uuid5(
+            uuid.NAMESPACE_URL,
+            "kdic://managed-suggestion/"
+            + "/".join(
+                [
+                    _clean(payload.business),
+                    _clean(payload.keyword),
+                    _clean(payload.canonical_question),
+                ]
+            ),
+        ).hex[:16].upper()
+
     def load_admin_sessions() -> dict[str, float]:
         try:
             loaded = json.loads(admin_sessions_path.read_text(encoding="utf-8"))
@@ -703,6 +922,73 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
             "updated_at": float(row.get("updated_at") or 0),
         }
 
+    def validate_prompt_preset_values(raw: Mapping[str, Any]) -> dict[str, str]:
+        manager = require_prompt_manager()
+        active = manager.active_values()
+        values = dict(raw or {})
+        unknown = set(values) - set(active)
+        missing = set(active) - set(values)
+        if unknown:
+            raise ValueError("관리 대상이 아닌 프롬프트가 포함되어 있습니다: " + ", ".join(sorted(unknown)))
+        if missing:
+            raise ValueError("필수 프롬프트가 누락되었습니다: " + ", ".join(sorted(missing)))
+        validated: dict[str, str] = {}
+        secret_pattern = re.compile(r"(?i)(?:nv-|sk-)[A-Za-z0-9_-]{16,}")
+        for slot in active:
+            value = str(values.get(slot) or "").strip()
+            if len(value) < 100:
+                raise ValueError(f"{slot}: 프롬프트는 100자 이상이어야 합니다.")
+            if len(value) > 30_000:
+                raise ValueError(f"{slot}: 프롬프트는 30,000자 이하여야 합니다.")
+            if secret_pattern.search(value):
+                raise ValueError(f"{slot}: API 키로 보이는 값이 포함되어 있습니다.")
+            validated[slot] = value
+        return validated
+
+    def load_prompt_presets() -> dict[str, dict[str, Any]]:
+        try:
+            loaded = json.loads(prompt_presets_path.read_text(encoding="utf-8"))
+            rows = loaded.get("items") if isinstance(loaded, Mapping) else loaded
+            if not isinstance(rows, Sequence) or isinstance(rows, (str, bytes)):
+                return {}
+            records: dict[str, dict[str, Any]] = {}
+            for row in rows:
+                if not isinstance(row, Mapping):
+                    continue
+                preset_id, name = _clean(row.get("preset_id")), _clean(row.get("name"))
+                if not preset_id or not name or len(name) > 60:
+                    continue
+                try:
+                    values = validate_prompt_preset_values(dict(row.get("values") or {}))
+                except Exception:
+                    continue
+                records[preset_id] = {
+                    "preset_id": preset_id,
+                    "name": name,
+                    "values": values,
+                    "created_at": float(row.get("created_at") or time.time()),
+                    "updated_at": float(row.get("updated_at") or row.get("created_at") or time.time()),
+                }
+            return records
+        except (FileNotFoundError, json.JSONDecodeError, OSError, TypeError, ValueError):
+            return {}
+
+    def save_prompt_presets() -> None:
+        prompt_presets_path.parent.mkdir(parents=True, exist_ok=True)
+        ordered = sorted(prompt_presets.values(), key=lambda row: float(row.get("updated_at") or 0), reverse=True)
+        temp = prompt_presets_path.with_suffix(prompt_presets_path.suffix + ".tmp")
+        temp.write_text(json.dumps({"items": ordered[:MAX_PROMPT_PRESETS]}, ensure_ascii=False, indent=2), encoding="utf-8")
+        os.replace(temp, prompt_presets_path)
+
+    def prompt_preset_public(row: Mapping[str, Any]) -> dict[str, Any]:
+        return {
+            "preset_id": str(row["preset_id"]),
+            "name": str(row["name"]),
+            "values": copy.deepcopy(dict(row["values"])),
+            "created_at": float(row.get("created_at") or 0),
+            "updated_at": float(row.get("updated_at") or 0),
+        }
+
     def load_history() -> list[dict[str, Any]]:
         try:
             if not history_path.is_file():
@@ -775,7 +1061,438 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                 "parent_child": bool(runtime_globals.get("PARENT_CHILD_ENABLED", True)),
                 "parent_context_max_chars": int(runtime_globals.get("PARENT_CONTEXT_MAX_CHARS", 8192))}
 
+    def chunk_source_url(chunk: Mapping[str, Any]) -> str:
+        return _clean(chunk.get("source_url"))
+
+    def chunk_source_catalog() -> list[dict[str, Any]]:
+        """Group only existing runtime chunks that retain their source URL.
+
+        A title/document ID is never treated as a URL.  That prevents a broad
+        replacement from accidentally affecting another source page.
+        """
+        grouped: dict[str, dict[str, Any]] = {}
+        for chunk in runtime_globals.get("CHUNKS", []):
+            if not isinstance(chunk, Mapping):
+                continue
+            source_url = chunk_source_url(chunk)
+            if not source_url:
+                continue
+            row = grouped.setdefault(source_url, {
+                "source_url": source_url,
+                "document_ids": set(),
+                "business_functions": set(),
+                "chunk_count": 0,
+                "sample_title": "",
+            })
+            row["chunk_count"] += 1
+            document_id = _clean(chunk.get("document_id"))
+            if document_id:
+                row["document_ids"].add(document_id)
+            business = _clean(chunk.get("business_function"))
+            if business:
+                row["business_functions"].add(business)
+            if not row["sample_title"]:
+                row["sample_title"] = _clean(chunk.get("title")) or _clean(chunk.get("section_title"))
+        return [{
+            "source_url": row["source_url"],
+            "document_ids": sorted(row["document_ids"]),
+            "business_functions": sorted(row["business_functions"]),
+            "chunk_count": row["chunk_count"],
+            "sample_title": row["sample_title"],
+        } for row in sorted(grouped.values(), key=lambda item: (str(item["sample_title"]), str(item["source_url"])))]
+
+    def source_chunks(source_url: str) -> list[dict[str, Any]]:
+        return [copy.deepcopy(dict(chunk)) for chunk in runtime_globals.get("CHUNKS", [])
+                if isinstance(chunk, Mapping) and chunk_source_url(chunk) == source_url]
+
+    def new_url_business_catalog() -> list[dict[str, Any]]:
+        """Recommend the next domain document ID from current runtime data."""
+        grouped: dict[str, dict[str, Any]] = {}
+        all_document_ids: set[str] = set()
+        for chunk in runtime_globals.get("CHUNKS", []):
+            if not isinstance(chunk, Mapping):
+                continue
+            business = _clean(chunk.get("business_function")) or "미분류"
+            document_id = _clean(chunk.get("document_id"))
+            if document_id:
+                all_document_ids.add(document_id)
+            match = re.fullmatch(r"([A-Z][A-Z0-9]{1,7})-(\d{3,6})", document_id)
+            if not match:
+                continue
+            row = grouped.setdefault(business, {"business_function": business, "prefix_counts": {}, "ids": []})
+            prefix, number = match.group(1), int(match.group(2))
+            row["prefix_counts"][prefix] = row["prefix_counts"].get(prefix, 0) + 1
+            row["ids"].append((prefix, number, len(match.group(2))))
+        items: list[dict[str, Any]] = []
+        for business, row in grouped.items():
+            prefix = sorted(row["prefix_counts"], key=lambda value: (-row["prefix_counts"][value], value))[0]
+            matching = [(number, width) for current_prefix, number, width in row["ids"] if current_prefix == prefix]
+            width = max([value[1] for value in matching] or [3])
+            next_number = max([value[0] for value in matching] or [0]) + 1
+            recommendation = f"{prefix}-{next_number:0{width}d}"
+            while recommendation in all_document_ids:
+                next_number += 1
+                recommendation = f"{prefix}-{next_number:0{width}d}"
+            items.append({
+                "business_function": business,
+                "document_prefix": prefix,
+                "recommended_document_id": recommendation,
+            })
+        return sorted(items, key=lambda row: row["business_function"])
+
+    # A URL refresh is deliberately reviewed before it is staged.  These are
+    # the fields needed to preserve document identity, provenance and search
+    # context after replacement.  Missing values are a blocking error, not a
+    # best-effort warning.
+    rechunk_required_fields = (
+        "chunk_id", "document_id", "parent_doc_id", "title",
+        "business_function", "source_url", "content",
+    )
+    rechunk_preview_text_limit = 120_000
+
+    def rechunk_json_value(value: Any) -> Any:
+        """JSONResponse rejects NaN values emitted by pandas quality reports."""
+        if isinstance(value, (float, np.floating)):
+            try:
+                return None if not math.isfinite(float(value)) else float(value)
+            except (TypeError, ValueError):
+                return None
+        if isinstance(value, (bool, np.bool_)):
+            return bool(value)
+        if isinstance(value, (int, np.integer)):
+            return int(value)
+        return _safe(value)
+
+    def rechunk_chunk_review(chunk: Mapping[str, Any]) -> dict[str, Any]:
+        """Return an admin-safe, human-readable review record for one chunk."""
+        content = str(chunk.get("content") or "").strip()
+        metadata = {
+            key: _safe(chunk.get(key)) for key in (
+                "chunk_id", "document_id", "parent_doc_id", "chunk_index",
+                "title", "section_title", "business_function",
+                "target_business_function", "source_url", "page_type",
+                "chunk_type",
+            )
+        }
+        missing = [key for key in rechunk_required_fields if not _clean(chunk.get(key))]
+        length_flags: list[str] = []
+        if content and len(content) < 80:
+            length_flags.append("본문이 80자 미만입니다. 제목·표·FAQ 단위 청크인지 확인하세요.")
+        if len(content) > 2_000:
+            length_flags.append("본문이 2,000자를 초과합니다. 청크 분할 지점을 확인하세요.")
+        return {
+            "chunk_id": str(chunk.get("chunk_id") or ""),
+            "content": content,
+            "content_chars": len(content),
+            "metadata": metadata,
+            "missing_required": missing,
+            "length_flags": length_flags,
+        }
+
+    def rechunk_document_review(document: Mapping[str, Any]) -> dict[str, Any]:
+        """Expose parsed text and parsing quality without exposing raw HTML."""
+        markdown = str(document.get("content_markdown") or document.get("body_markdown") or "").strip()
+        text = str(document.get("content_text") or "").strip()
+        shown = markdown or text
+        truncated = len(shown) > rechunk_preview_text_limit
+        quality = document.get("quality") if isinstance(document.get("quality"), Mapping) else {}
+        return {
+            "document_id": _clean(document.get("doc_id")) or _clean(document.get("document_id")),
+            "title": _clean(document.get("title")),
+            "source_url": _clean(document.get("source_url")),
+            "parsed_markdown": shown[:rechunk_preview_text_limit],
+            "parsed_text_chars": len(text) or len(shown),
+            "preview_truncated": truncated,
+            "metadata": {
+                key: _safe(document.get(key)) for key in (
+                    "doc_id", "parent_doc_id", "title", "business_function",
+                    "target_business_function", "source_url", "final_url", "page_type",
+                    "parser_profile", "chunk_strategy", "parsed_at",
+                )
+            },
+            "quality": {
+                "status": _safe(quality.get("status")),
+                "reasons": [_safe(value) for value in (quality.get("reasons") or [])],
+                "source_text_chars": _safe(quality.get("source_text_chars")),
+                "parsed_text_chars": _safe(quality.get("parsed_text_chars")),
+                "retention_ratio": _safe(quality.get("retention_ratio")),
+                "faq_count": _safe(quality.get("faq_count")),
+                "table_count": _safe(quality.get("table_count")),
+            },
+        }
+
+    def rechunk_review_bundle(*, old_chunks: Sequence[Mapping[str, Any]],
+                              new_chunks: Sequence[Mapping[str, Any]],
+                              documents: Sequence[Mapping[str, Any]],
+                              quality_rows: Sequence[Mapping[str, Any]],
+                              validation_rows: Sequence[Mapping[str, Any]],
+                              document_ids: set[str]) -> dict[str, Any]:
+        """Build comparison, warnings and hard staging checks for a refresh."""
+        old = [rechunk_chunk_review(chunk) for chunk in old_chunks]
+        new = [rechunk_chunk_review(chunk) for chunk in new_chunks]
+        old_by_id = {row["chunk_id"]: row for row in old if row["chunk_id"]}
+        new_by_id = {row["chunk_id"]: row for row in new if row["chunk_id"]}
+        comparisons: list[dict[str, Any]] = []
+        counts = {"same": 0, "changed": 0, "removed": 0, "added": 0}
+        for chunk_id in sorted(set(old_by_id) | set(new_by_id)):
+            before, after = old_by_id.get(chunk_id), new_by_id.get(chunk_id)
+            if before is None:
+                status = "added"
+            elif after is None:
+                status = "removed"
+            elif before["content"] == after["content"] and before["metadata"] == after["metadata"]:
+                status = "same"
+            else:
+                status = "changed"
+            counts[status] += 1
+            comparisons.append({
+                "chunk_id": chunk_id,
+                "status": status,
+                "old_chars": before["content_chars"] if before else 0,
+                "new_chars": after["content_chars"] if after else 0,
+                "old_section_title": (before or {}).get("metadata", {}).get("section_title", ""),
+                "new_section_title": (after or {}).get("metadata", {}).get("section_title", ""),
+            })
+
+        selected_documents = [rechunk_document_review(document) for document in documents
+                              if (_clean(document.get("doc_id")) in document_ids
+                                  and _clean(document.get("source_url")))]
+        selected_quality = [dict(row) for row in quality_rows if _clean(row.get("url_id")) in document_ids]
+        selected_validations = [dict(row) for row in validation_rows
+                                if any(str(document_id) in str(row.get("validation") or "")
+                                       for document_id in document_ids)]
+        errors: list[str] = []
+        warnings: list[str] = []
+        if not new:
+            errors.append("새 청크가 0개입니다. 기존 청크를 제거하거나 교체 초안을 만들 수 없습니다.")
+        if not selected_documents:
+            errors.append("재수집한 파싱 문서를 찾지 못했습니다. 파싱 결과를 검토할 수 없어 반영을 차단했습니다.")
+        for row in new:
+            if row["missing_required"]:
+                errors.append(f"{row['chunk_id'] or '청크 ID 없음'}: 필수 메타데이터 누락 ({', '.join(row['missing_required'])})")
+            for flag in row["length_flags"]:
+                warnings.append(f"{row['chunk_id']}: {flag}")
+        for row in selected_documents:
+            if not row["parsed_markdown"]:
+                errors.append(f"{row['document_id'] or '문서 ID 없음'}: 파싱된 본문이 비어 있습니다.")
+            if row["quality"].get("status") == "fail":
+                reasons = ", ".join(map(str, row["quality"].get("reasons") or [])) or "품질 검사 실패"
+                errors.append(f"{row['document_id']}: 파싱 품질 검사 실패 ({reasons})")
+            try:
+                ratio = float(row["quality"].get("retention_ratio") or 0)
+                if ratio and ratio < .98:
+                    errors.append(f"{row['document_id']}: 본문 보존율 {ratio:.2%}로 기준 98% 미만입니다.")
+            except (TypeError, ValueError):
+                warnings.append(f"{row['document_id']}: 본문 보존율을 숫자로 확인하지 못했습니다.")
+        for row in selected_quality:
+            if str(row.get("quality_status") or "").lower() == "fail":
+                errors.append(f"{row.get('url_id')}: 청킹 품질 검사 실패 ({row.get('quality_reasons') or '사유 없음'})")
+        for row in selected_validations:
+            passed = row.get("passed")
+            if isinstance(passed, str):
+                passed = passed.strip().lower() in {"true", "1", "yes", "y"}
+            if passed is False:
+                errors.append(f"회귀 검사 실패: {row.get('validation')}")
+        # Preserve first occurrence: one failed quality check can be represented in
+        # both document and quality reports.
+        errors = list(dict.fromkeys(errors))
+        warnings = list(dict.fromkeys(warnings))
+        return {
+            "parsed_documents": selected_documents,
+            "old_chunks": old,
+            "new_chunks": new,
+            "comparisons": comparisons,
+            "comparison_counts": counts,
+            "quality_rows": [{key: rechunk_json_value(value) for key, value in row.items()} for row in selected_quality],
+            "validation_rows": [{key: rechunk_json_value(value) for key, value in row.items()} for row in selected_validations],
+            "errors": errors,
+            "warnings": warnings,
+            "can_stage": not errors,
+        }
+
+    def normalized_rechunk_preview(chunks: Sequence[Mapping[str, Any]], *, source_url: str,
+                                   document_ids: set[str]) -> list[dict[str, Any]]:
+        """Keep the source document's ID convention while issuing fresh chunk IDs."""
+        candidates: list[dict[str, Any]] = []
+        for raw in chunks:
+            chunk = copy.deepcopy(dict(raw))
+            document_id = _clean(chunk.get("document_id"))
+            if document_id not in document_ids:
+                continue
+            content = str(chunk.get("content") or "").strip()
+            if not content:
+                continue
+            chunk.update(document_id=document_id,
+                         parent_doc_id=_clean(chunk.get("parent_doc_id")) or document_id,
+                         source_url=source_url,
+                         content=content,
+                         title=_clean(chunk.get("title")),
+                         section_title=_clean(chunk.get("section_title")),
+                         business_function=_clean(chunk.get("business_function")))
+            candidates.append(chunk)
+        if not candidates:
+            raise ValueError("재수집 결과에서 선택한 URL의 본문 청크를 찾지 못했습니다. 문서 ID와 URL 연결을 확인해 주세요.")
+        def index_of(row: Mapping[str, Any]) -> int:
+            try:
+                return int(row.get("chunk_index") or 0)
+            except (TypeError, ValueError):
+                return 0
+        candidates.sort(key=lambda row: (_clean(row.get("document_id")), index_of(row), _clean(row.get("chunk_id"))))
+        counters: dict[str, int] = {}
+        normalized: list[dict[str, Any]] = []
+        for chunk in candidates:
+            document_id = str(chunk["document_id"])
+            index = counters.get(document_id, 0)
+            counters[document_id] = index + 1
+            chunk["chunk_id"] = f"{document_id}_chunk_{index:03d}"
+            chunk["chunk_index"] = index
+            normalized.append(chunk)
+        return normalized
+
+    def rechunk_public(row: Mapping[str, Any], *, include_preview: bool = False) -> dict[str, Any]:
+        out = {key: copy.deepcopy(row.get(key)) for key in (
+            "job_id", "mode", "source_url", "status", "stage", "stage_detail", "progress",
+            "error", "created_at", "started_at", "heartbeat_at", "updated_at",
+            "old_chunk_count", "new_chunk_count", "document_ids", "business_functions", "staged",
+        )}
+        if include_preview:
+            out["preview_chunks"] = copy.deepcopy(list(row.get("preview_chunks") or []))
+            out["review"] = copy.deepcopy(dict(row.get("review") or {}))
+        return out
+
+    def build_rechunk_review_csv(job_id: str, *, source_url: str, document_ids: set[str],
+                                 new_document: Mapping[str, Any] | None = None) -> Path:
+        """Create the minimum review manifest for one already-approved URL.
+
+        Older EC2 deployments keep the published chunks but not the original
+        crawling review CSV.  Recreating a single-row (or document-ID scoped)
+        manifest from the current runtime metadata lets this admin operation
+        refresh only its selected URL without fabricating a whole policy table.
+        """
+        by_document: dict[str, dict[str, Any]] = {}
+        if new_document is not None:
+            document_id = _clean(new_document.get("document_id"))
+            if document_id:
+                by_document[document_id] = dict(new_document)
+        else:
+            for chunk in source_chunks(source_url):
+                document_id = _clean(chunk.get("document_id"))
+                if document_id in document_ids and document_id not in by_document:
+                    by_document[document_id] = chunk
+        missing = sorted(document_ids - set(by_document))
+        if missing:
+            raise ValueError(f"선택 URL의 문서 메타데이터를 찾지 못했습니다: {', '.join(missing)}")
+        common = {
+            "권장_최종결정": "include_full",
+            "RAG_본문_인덱싱": "O",
+            "인덱싱_범위": "본문 전체",
+            "Action_링크": "",
+            "권장_Action_Type": "",
+            "Action_인증": "",
+            "다중페이지_수집정책": "",
+            "검토_의견": "관리자 URL 재수집·재청킹",
+            "검토_근거": "기존 운영 청크의 source_url과 document_id를 사용",
+            "첨부파일_원본수집": "",
+            "첨부파일_RAG_정책": "",
+            "첨부파일_사용자제공정책": "",
+            "영상_처리정책": "",
+            "웹툰_처리정책": "",
+            "일반이미지_처리정책": "",
+            "보조콘텐츠_표시조건": "",
+            "보조콘텐츠_링크라벨": "",
+        }
+        rows: list[dict[str, str]] = []
+        for document_id in sorted(document_ids):
+            chunk = by_document[document_id]
+            business = _clean(chunk.get("business_function")) or "미분류"
+            rows.append({
+                **common,
+                "문서_ID": document_id,
+                "업무_도메인": business,
+                "목표_도메인": _clean(chunk.get("target_business_function")) or business,
+                "문서명": _clean(chunk.get("title")) or document_id,
+                "페이지_유형": _clean(chunk.get("page_type")) or "static_page",
+                "출처_URL": source_url,
+            })
+        path = service_module._crawler_runtime_root() / "admin_rechunk_manifests" / f"{job_id}.csv"
+        path.parent.mkdir(parents=True, exist_ok=True)
+        pd.DataFrame(rows).to_csv(path, index=False, encoding="utf-8-sig")
+        return path
+
+    def run_rechunk_preview(job_id: str) -> None:
+        def update_progress(percent: int, stage: str, detail: str = "") -> None:
+            with rechunk_lock:
+                current = rechunk_jobs.get(job_id)
+                if current is not None:
+                    current.update(
+                        progress=max(0, min(100, int(percent))),
+                        stage=stage,
+                        stage_detail=str(detail or ""),
+                        heartbeat_at=time.time(),
+                        updated_at=time.time(),
+                    )
+
+        with rechunk_lock:
+            job = rechunk_jobs.get(job_id)
+            if job is None:
+                return
+            job.update(status="running", stage="수집 준비", stage_detail="정책과 URL을 확인하고 있습니다.",
+                       progress=1, started_at=time.time(), heartbeat_at=time.time(), updated_at=time.time())
+            source_url, document_ids = str(job["source_url"]), set(job["document_ids"])
+            old_chunks = copy.deepcopy(list(job.get("old_chunks_snapshot") or []))
+            new_document = copy.deepcopy(job.get("new_document")) if isinstance(job.get("new_document"), Mapping) else None
+        try:
+            review_csv_path = build_rechunk_review_csv(
+                job_id, source_url=source_url, document_ids=document_ids, new_document=new_document,
+            )
+            config = service_module.kdic_final_pipeline.PipelineConfig(run_only_url_ids=sorted(document_ids))
+            result = service_module.kdic_final_pipeline.run_pipeline(
+                review_csv_path=review_csv_path,
+                runtime_root=service_module._crawler_runtime_root(),
+                config=config,
+                progress_callback=update_progress,
+            )
+            if new_document is not None:
+                redirected = [
+                    _clean(document.get("final_url")) or _clean(document.get("source_url"))
+                    for document in list(result.get("page_documents") or [])
+                ]
+                invalid_redirects = [url for url in redirected if url and not _is_public_kdic_url(url)]
+                if invalid_redirects:
+                    raise ValueError("신규 URL이 예금보험공사 공개 도메인 밖으로 이동해 처리를 차단했습니다.")
+            preview = normalized_rechunk_preview(result.get("chunks") or [], source_url=source_url,
+                                                 document_ids=document_ids)
+            quality_df = result.get("quality_df")
+            validation_df = result.get("validation_df")
+            quality_rows = quality_df.to_dict("records") if isinstance(quality_df, pd.DataFrame) else []
+            validation_rows = validation_df.to_dict("records") if isinstance(validation_df, pd.DataFrame) else []
+            review = rechunk_review_bundle(
+                old_chunks=old_chunks,
+                new_chunks=preview,
+                documents=list(result.get("page_documents") or result.get("documents") or []),
+                quality_rows=quality_rows,
+                validation_rows=validation_rows,
+                document_ids=document_ids,
+            )
+            with rechunk_lock:
+                job = rechunk_jobs[job_id]
+                stage = "재청킹 미리보기 준비 완료"
+                if not review["can_stage"]:
+                    stage = "검수 오류 발견 · 교체 초안 생성 차단"
+                job.update(status="ready", stage=stage, preview_chunks=preview, review=review,
+                           new_chunk_count=len(preview), progress=100, stage_detail="검수 화면을 확인해 주세요.",
+                           heartbeat_at=time.time(), updated_at=time.time())
+        except Exception as error:
+            with rechunk_lock:
+                job = rechunk_jobs.get(job_id)
+                if job is not None:
+                    job.update(status="failed", stage="재청킹 실패",
+                               stage_detail="오류 내용을 확인하고 다시 실행해 주세요.", progress=100,
+                               error=f"{type(error).__name__}: {error}", heartbeat_at=time.time(),
+                               updated_at=time.time())
+
     parameter_presets: dict[str, dict[str, Any]] = load_parameter_presets()
+    prompt_presets: dict[str, dict[str, Any]] = load_prompt_presets()
 
     def job_telemetry(public: Mapping[str, Any], stored: Any) -> dict[str, Any]:
         """Return request-time metrics without confusing a cache hit with its origin run."""
@@ -957,8 +1674,10 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
         prompt_state = prompt_manager.public() if prompt_manager is not None else {"has_changes": False, "slots": []}
         return {"active_config": active_config(), "candidate_config": _validate_config(drafts["config"], active_config()),
                 "add": list(drafts["add"].values()), "remove": sorted(drafts["remove"]),
+                "rechunk": copy.deepcopy(drafts["rechunk"]),
+                "suggestion_cache": copy.deepcopy(list(drafts["suggestion_cache"].values())),
                 "prompt_changes": [row["slot"] for row in prompt_state.get("slots") or [] if row.get("changed")],
-                "has_changes": bool(drafts["config"] or drafts["add"] or drafts["remove"] or prompt_state.get("has_changes")),
+                "has_changes": bool(drafts["config"] or drafts["add"] or drafts["remove"] or drafts["suggestion_cache"] or prompt_state.get("has_changes")),
                 "history": [{k: v for k, v in row.items() if k != "snapshot"} for row in history[-10:]][::-1]}
 
     def normalize_dataset(data: bytes, filename: str, requested_sheet: str | None) -> dict[str, Any]:
@@ -1138,6 +1857,7 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                 current_values.append(current_value)
                 improvement_values.append(improvement_value)
                 metric_deltas.append({
+                    "name": str(definition.get("name") or baseline_key),
                     "metric": str(definition.get("label") or definition.get("name") or baseline_key),
                     "current": round(current_value, 6),
                     "improvement": round(improvement_value, 6),
@@ -1181,9 +1901,103 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                 "delta": (result.get("delta") or {}).get(definition.get("name")),
             })
         latency_delta = (result.get("delta") or {}).get("latency_avg_ms")
+        current_overall_score = sum(float(row["current_score"]) for row in rows) / total if total else 0.0
+        improvement_overall_score = sum(float(row["improvement_score"]) for row in rows) / total if total else 0.0
+        adjustment_hints = {
+            "hit": {
+                "group": "정답 포함 여부",
+                "down": ["후보 깊이를 늘려 Gold 후보 누락 여부 확인", "최종 Top-K를 단계적으로 늘려 재평가"],
+                "parameters": ["candidate_depth", "final_top_k", "min_relevance_score"],
+            },
+            "recall": {
+                "group": "정답 범위 회수",
+                "down": ["후보 깊이 또는 최종 Top-K를 늘려 누락 Gold 청크 확인", "임계치가 높다면 소폭 낮춰 재평가"],
+                "parameters": ["candidate_depth", "final_top_k", "min_relevance_score"],
+            },
+            "mrr": {
+                "group": "첫 정답 순위",
+                "down": ["Dense·BM25 가중치를 한 번에 하나씩 조정", "Reranker 후보 깊이를 늘려 정답 재정렬 여부 확인"],
+                "parameters": ["dense_weight", "bm25_weight", "candidate_depth"],
+            },
+            "map": {
+                "group": "복수 정답 순위",
+                "down": ["Dense·BM25 가중치를 재조정", "Reranker 후보 깊이와 정답 청크 순서를 질문별로 확인"],
+                "parameters": ["dense_weight", "bm25_weight", "candidate_depth"],
+            },
+            "complete": {
+                "group": "다중 청크 완전 회수",
+                "down": ["다중 청크 문항에서 후보 깊이·최종 Top-K 확대", "Parent-Child 확장이 검색 Child 순위를 가리지 않는지 확인"],
+                "parameters": ["candidate_depth", "final_top_k", "parent_child"],
+            },
+            "ndcg": {
+                "group": "상위 순위 품질",
+                "down": ["Reranker 입력 후보와 Dense·BM25 가중치를 함께 점검", "Gold 청크가 상위권에서 밀린 대표 문항 확인"],
+                "parameters": ["dense_weight", "bm25_weight", "candidate_depth"],
+            },
+            "precision": {
+                "group": "검색 결과 순도",
+                "down": ["최종 Top-K를 낮추거나 최소 관련성 점수를 높여 불필요 청크 축소", "Recall 손실이 없는 범위에서 한 단계씩 조정"],
+                "parameters": ["final_top_k", "min_relevance_score"],
+            },
+            "f1": {
+                "group": "정밀도·재현율 균형",
+                "down": ["Top-K와 최소 관련성 점수를 함께 보지 말고 한 항목씩 조정", "Precision과 Recall 중 어느 쪽이 F1을 끌어내렸는지 먼저 확인"],
+                "parameters": ["final_top_k", "min_relevance_score", "candidate_depth"],
+            },
+        }
+        metric_contributions = []
+        for definition in definitions:
+            name = str(definition.get("name") or "")
+            per_question = [
+                metric
+                for row in rows
+                for metric in row["metric_deltas"]
+                if metric.get("name") == name
+            ]
+            if not per_question:
+                continue
+            # 질문마다 적용 가능한 지표 수가 다를 수 있으므로 delta/n을 먼저 계산한다.
+            # 이 기여도를 전체 질문에 대해 더하면 아래 overall delta와 정확히 일치한다.
+            score_contribution = sum(
+                next(
+                    (
+                        float(metric["delta"]) / len(row["metric_deltas"])
+                        for metric in row["metric_deltas"]
+                        if metric.get("name") == name
+                    ),
+                    0.0,
+                )
+                for row in rows
+            ) / total if total else 0.0
+            mean_delta = sum(float(metric["delta"]) for metric in per_question) / len(per_question)
+            hint = adjustment_hints.get(name, {"group": "검색 품질", "down": [], "parameters": []})
+            metric_contributions.append({
+                "name": name,
+                "metric": str(definition.get("label") or name),
+                "group": hint["group"],
+                "applicable_questions": len(per_question),
+                "mean_delta": round(mean_delta, 6),
+                "score_contribution": round(score_contribution, 6),
+                "positive_questions": sum(float(metric["delta"]) > tolerance for metric in per_question),
+                "negative_questions": sum(float(metric["delta"]) < -tolerance for metric in per_question),
+                "unchanged_questions": sum(abs(float(metric["delta"])) <= tolerance for metric in per_question),
+                "adjustment_hints": list(hint["down"]),
+                "target_parameters": list(hint["parameters"]),
+            })
         return {
             "question_count": total,
-            "classification_rule": "질문별 적용 가능 검색지표의 단순평균 변화가 ±0.5%p를 넘으면 개선/악화, 이하는 동일",
+            "classification_rule": "질문별 적용 가능 검색지표를 동일 가중치로 평균하고, 현재안 대비 ±0.5%p를 넘으면 개선/악화, 이하는 동일",
+            "score_method": {
+                "name": "질문별 검색 품질 종합점수",
+                "formula": "질문별 종합점수 = 적용 가능한 (Hit + Recall + MRR + MAP + Complete + nDCG + Precision + F1) / 적용 지표 수",
+                "weighting": "모든 적용 지표 동일 가중치(1/n). Complete는 다중 청크 평가 대상 문항에만 포함",
+                "threshold": tolerance,
+                "current": round(current_overall_score, 6),
+                "improvement": round(improvement_overall_score, 6),
+                "delta": round(improvement_overall_score - current_overall_score, 6),
+                "contribution_check": round(sum(float(row["score_contribution"]) for row in metric_contributions), 6),
+            },
+            "metric_contributions": metric_contributions,
             "counts": counts,
             "rates": {name: (counts[name] / total if total else 0.0) for name in counts},
             "metric_changes": metric_changes,
@@ -1225,9 +2039,12 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
         system_prompt = (
             "당신은 공공기관 RAG 검색 품질 검토자입니다. 제공된 수치만 사용하고 추측하지 마세요. "
             "전체 테스트셋 집계는 이미 코드로 계산되었으므로 수치를 다시 만들지 말고 해석하세요. "
-            "반드시 한국어로 작성하며, 1) 결론 2) 품질 변화 3) 대표 개선 사례 "
-            "4) 대표 악화 사례 5) 권장 설정과 적용 조건 6) 적용 전 확인사항 순서로 간결하게 작성하세요. "
-            "성능 향상과 지연시간 증가의 교환관계를 함께 판단하고, 근거가 부족하면 명시하세요."
+            "score_method의 산식과 metric_contributions를 근거로 어떤 지표가 종합점수를 얼마나 올리거나 내렸는지 설명하세요. "
+            "config_changes와 지표별 adjustment_hints를 연결하되 인과관계가 확정되지 않았으면 '가설'이라고 표시하세요. "
+            "반드시 한국어로 작성하며, 1) 결론 2) 종합점수 계산 근거 3) 점수를 올린 지표와 내린 지표 "
+            "4) 대표 개선·악화 사례 5) 다음 A/B 테스트에서 한 번에 하나씩 바꿀 파라미터 6) 적용 전 확인사항 순서로 작성하세요. "
+            "권장안에는 대상 파라미터, 조정 방향, 확인할 지표를 함께 적고 성능과 지연시간의 교환관계도 판단하세요. "
+            "근거가 부족하면 명시하고, 제공되지 않은 수치나 원인을 만들지 마세요."
         )
         started = time.perf_counter()
         response = client.chat.completions.create(
@@ -1354,8 +2171,12 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
         if prompt_manager is not None:
             prompt_public = prompt_manager.public()
             prompt_snapshot = {"version": prompt_public["active_version"], "values": prompt_manager.active_values()}
+        cache_snapshot = None
+        if suggestion_cache is not None and callable(getattr(suggestion_cache, "snapshot_catalog", None)):
+            cache_snapshot = suggestion_cache.snapshot_catalog()
         return {"config": active_config(), "chunks": copy.deepcopy(list(runtime_globals["CHUNKS"])),
                 "prompts": prompt_snapshot,
+                "suggestion_catalog": cache_snapshot,
                 "vectors": {cid: np.asarray(vec, dtype=np.float32).copy() for cid, vec in runtime_globals["DENSE_VECTOR_BY_ID"].items()}}
 
     def load_default_snapshot() -> dict[str, Any]:
@@ -1415,6 +2236,93 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                 version=str(prompt_target.get("version") or "prompt-restored"),
                 archive_current=archive_prompts,
             )
+        cache_target = target.get("suggestion_catalog")
+        if cache_target is not None:
+            manager = require_suggestion_cache()
+            restore = getattr(manager, "restore_catalog", None)
+            if not callable(restore):
+                raise RuntimeError("질의 캐시 롤백 기능이 연결되지 않았습니다.")
+            restore(copy.deepcopy(list(cache_target)), actor="ADMIN_ROLLBACK")
+
+    def apply_suggestion_cache_drafts() -> None:
+        manager = require_suggestion_cache()
+        for suggestion_id, change in copy.deepcopy(drafts["suggestion_cache"]).items():
+            rows = {
+                row["suggestion_id"]: row
+                for row in manager.list_catalog(include_disabled=True)
+            }
+            metadata = dict(change.get("metadata") or {})
+            if change.get("create"):
+                if suggestion_id in rows:
+                    raise ValueError(f"이미 존재하는 질의 캐시 ID입니다: {suggestion_id}")
+                created = manager.create_suggestion(
+                    suggestion_id=suggestion_id,
+                    business_key=metadata["business_key"],
+                    business=metadata["business"],
+                    keyword=metadata["keyword"],
+                    canonical_question=metadata["canonical_question"],
+                    display_order=None,
+                    actor="ADMIN_APPLY",
+                )
+                requested_order = int(metadata.get("display_order") or created["display_order"])
+                if requested_order != int(created["display_order"]):
+                    manager.update_suggestion(
+                        suggestion_id,
+                        business_key=metadata["business_key"],
+                        business=metadata["business"],
+                        keyword=metadata["keyword"],
+                        canonical_question=metadata["canonical_question"],
+                        display_order=requested_order,
+                        row_version=int(created["row_version"]),
+                        actor="ADMIN_APPLY",
+                    )
+                rows = {
+                    row["suggestion_id"]: row
+                    for row in manager.list_catalog(include_disabled=True)
+                }
+            elif metadata:
+                row = rows.get(suggestion_id)
+                if row is None:
+                    raise KeyError(suggestion_id)
+                expected = change.get("expected_row_version")
+                if expected is not None and int(row["row_version"]) != int(expected):
+                    raise RuntimeError(
+                        f"{suggestion_id} 항목이 초안 저장 후 변경되었습니다. "
+                        "질의 캐시 목록을 다시 읽고 초안을 확인해 주세요."
+                    )
+                manager.update_suggestion(
+                    suggestion_id,
+                    business_key=metadata["business_key"],
+                    business=metadata["business"],
+                    keyword=metadata["keyword"],
+                    canonical_question=metadata["canonical_question"],
+                    display_order=int(metadata["display_order"]),
+                    row_version=int(row["row_version"]),
+                    actor="ADMIN_APPLY",
+                )
+            if "enabled" in change:
+                manager.set_enabled(
+                    suggestion_id,
+                    bool(change["enabled"]),
+                    actor="ADMIN_APPLY",
+                )
+            answer_change = dict(change.get("answer_change") or {})
+            action = answer_change.get("action")
+            if action == "save":
+                manager.save_manual_answer(
+                    suggestion_id,
+                    answer=answer_change["answer"],
+                    sources=answer_change["sources"],
+                    actor="ADMIN_APPLY",
+                )
+            elif action == "clear":
+                manager.clear_active(suggestion_id, actor="ADMIN_APPLY")
+            elif action == "activate":
+                manager.activate(
+                    suggestion_id,
+                    answer_change["cache_key"],
+                    actor="ADMIN_APPLY",
+                )
 
     @app.get("/admin/bootstrap", include_in_schema=False)
     def admin_bootstrap(admin_token: str = Query(min_length=20, max_length=300)):
@@ -1511,6 +2419,138 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
         base = service_module.admin_document(index, document_id)
         return {**base, "chunk_metadata": copy.deepcopy(runtime_globals.get("CHUNKS_BY_ID", {}).get(str(document_id)))}
 
+    @app.get("/api/admin-ui/chunk-sources")
+    def list_chunk_sources(_: None = Depends(require_admin_session)):
+        items = chunk_source_catalog()
+        return {"items": items, "count": len(items), "business_catalog": new_url_business_catalog()}
+
+    @app.post("/api/admin-ui/chunk-sources/rechunk")
+    def start_rechunk_preview(payload: RechunkPreviewPayload, _: None = Depends(require_admin_session)):
+        source_url = _clean(payload.source_url)
+        source = next((row for row in chunk_source_catalog() if row["source_url"] == source_url), None)
+        if source is None:
+            raise HTTPException(status_code=404, detail="현재 운영 청크에서 해당 URL을 찾지 못했습니다. URL이 없는 기존 청크는 재청킹할 수 없습니다.")
+        if not source["document_ids"]:
+            raise HTTPException(status_code=422, detail="해당 URL의 document_id가 없어 도메인별 청크 ID를 재발급할 수 없습니다.")
+        with rechunk_lock:
+            if any(row.get("source_url") == source_url and row.get("status") in {"queued", "running"}
+                   for row in rechunk_jobs.values()):
+                raise HTTPException(status_code=409, detail="이 URL은 이미 재수집·재청킹 중입니다.")
+            job_id = f"rechunk-{uuid.uuid4().hex[:12]}"
+            rechunk_jobs[job_id] = {
+                "job_id": job_id, "mode": "refresh", "source_url": source_url,
+                "status": "queued", "stage": "재수집 대기", "stage_detail": "작업 실행을 기다리고 있습니다.",
+                "progress": 0,
+                "error": None, "created_at": time.time(), "updated_at": time.time(),
+                "old_chunk_count": int(source["chunk_count"]), "new_chunk_count": 0,
+                "document_ids": list(source["document_ids"]),
+                "business_functions": list(source["business_functions"]),
+                "preview_chunks": [], "review": {}, "staged": False,
+                # This fingerprint protects against removing a newer runtime
+                # version while an administrator is reviewing the preview.
+                "old_chunk_ids": sorted(str(chunk.get("chunk_id")) for chunk in source_chunks(source_url)
+                                        if chunk.get("chunk_id")),
+                "old_chunks_snapshot": source_chunks(source_url),
+            }
+        rechunk_executor.submit(run_rechunk_preview, job_id)
+        return rechunk_public(rechunk_jobs[job_id])
+
+    @app.post("/api/admin-ui/chunk-sources/crawl")
+    def start_new_url_preview(payload: NewUrlCrawlPayload, _: None = Depends(require_admin_session)):
+        source_url = _clean(payload.source_url)
+        if not _is_public_kdic_url(source_url):
+            raise HTTPException(status_code=422, detail="신규 URL은 예금보험공사 공개 도메인(kdic.or.kr) 주소만 등록할 수 있습니다.")
+        document_id = _clean(payload.document_id).upper()
+        if not re.fullmatch(r"[A-Z][A-Z0-9]{1,7}-\d{3,6}", document_id):
+            raise HTTPException(status_code=422, detail="문서 ID는 도메인 코드-숫자 형식으로 입력해 주세요. 예: DP-007")
+        if any(row["source_url"] == source_url for row in chunk_source_catalog()):
+            raise HTTPException(status_code=409, detail="이미 운영 중인 URL입니다. ‘기존 URL 갱신’에서 재수집해 주세요.")
+        existing_document_ids = {
+            _clean(chunk.get("document_id")) for chunk in runtime_globals.get("CHUNKS", [])
+            if isinstance(chunk, Mapping) and _clean(chunk.get("document_id"))
+        }
+        if document_id in existing_document_ids:
+            raise HTTPException(status_code=409, detail=f"이미 사용 중인 문서 ID입니다: {document_id}")
+        business = _clean(payload.business_function)
+        title = _clean(payload.title)
+        new_document = {
+            "document_id": document_id,
+            "parent_doc_id": document_id,
+            "title": title,
+            "business_function": business,
+            "target_business_function": _clean(payload.target_business_function) or business,
+            "page_type": _clean(payload.page_type) or "static_page",
+            "source_url": source_url,
+        }
+        with rechunk_lock:
+            if any(row.get("source_url") == source_url and row.get("status") in {"queued", "running"}
+                   for row in rechunk_jobs.values()):
+                raise HTTPException(status_code=409, detail="이 URL은 이미 신규 수집 중입니다.")
+            job_id = f"crawl-{uuid.uuid4().hex[:12]}"
+            rechunk_jobs[job_id] = {
+                "job_id": job_id, "mode": "new", "source_url": source_url,
+                "status": "queued", "stage": "신규 수집 대기", "stage_detail": "작업 실행을 기다리고 있습니다.",
+                "progress": 0, "error": None, "created_at": time.time(), "updated_at": time.time(),
+                "old_chunk_count": 0, "new_chunk_count": 0,
+                "document_ids": [document_id], "business_functions": [business],
+                "preview_chunks": [], "review": {}, "staged": False,
+                "old_chunk_ids": [], "old_chunks_snapshot": [], "new_document": new_document,
+            }
+        rechunk_executor.submit(run_rechunk_preview, job_id)
+        return rechunk_public(rechunk_jobs[job_id])
+
+    @app.get("/api/admin-ui/chunk-sources/rechunk/{job_id}")
+    def get_rechunk_preview(job_id: str, _: None = Depends(require_admin_session)):
+        with rechunk_lock:
+            job = rechunk_jobs.get(job_id)
+            if job is None:
+                raise HTTPException(status_code=404, detail="재청킹 작업을 찾지 못했습니다. 서버 재시작 후에는 새로 재수집해 주세요.")
+            return rechunk_public(job, include_preview=job.get("status") == "ready")
+
+    @app.post("/api/admin-ui/chunk-sources/rechunk/{job_id}/stage")
+    def stage_rechunk_preview(job_id: str, _: None = Depends(require_admin_session)):
+        with rechunk_lock:
+            job = copy.deepcopy(rechunk_jobs.get(job_id))
+        if job is None:
+            raise HTTPException(status_code=404, detail="재청킹 작업을 찾지 못했습니다.")
+        if job.get("status") != "ready":
+            raise HTTPException(status_code=409, detail="재청킹 미리보기가 준비된 뒤에만 교체 초안을 만들 수 있습니다.")
+        new_chunks = list(job.get("preview_chunks") or [])
+        if not new_chunks:
+            raise HTTPException(status_code=422, detail="교체할 새 청크가 없습니다.")
+        review = job.get("review") if isinstance(job.get("review"), Mapping) else {}
+        if not review.get("can_stage"):
+            errors = "; ".join(map(str, list(review.get("errors") or [])[:3]))
+            raise HTTPException(status_code=422, detail=f"검수 오류가 있어 교체 초안을 만들 수 없습니다. {errors}")
+        source_url = str(job["source_url"])
+        mode = str(job.get("mode") or "refresh")
+        with mutation_lock:
+            current_ids = {str(row.get("chunk_id")) for row in source_chunks(source_url) if row.get("chunk_id")}
+            if mode == "refresh" and not current_ids:
+                raise HTTPException(status_code=409, detail="현재 운영 청크가 바뀌었습니다. URL 목록을 새로고침하고 다시 재청킹해 주세요.")
+            if mode == "refresh" and current_ids != set(map(str, job.get("old_chunk_ids") or [])):
+                raise HTTPException(status_code=409, detail="미리보기 이후 운영 청크가 변경되었습니다. 이전 결과를 적용하지 않고 다시 재청킹해 주세요.")
+            if mode == "new" and current_ids:
+                raise HTTPException(status_code=409, detail="미리보기 이후 같은 URL이 운영에 추가되었습니다. 신규 수집을 다시 시작해 주세요.")
+            new_ids = {str(row.get("chunk_id")) for row in new_chunks}
+            all_ids = set(map(str, runtime_globals.get("CHUNKS_BY_ID", {})))
+            conflicts = sorted((new_ids & (all_ids - current_ids)) | (new_ids & (set(drafts["add"]) - current_ids)))
+            if conflicts:
+                raise HTTPException(status_code=409, detail=f"다른 운영 문서와 청크 ID가 충돌합니다: {', '.join(conflicts[:5])}")
+            if mode == "refresh":
+                drafts["remove"].update(current_ids)
+            for chunk in new_chunks:
+                drafts["add"][str(chunk["chunk_id"])] = copy.deepcopy(chunk)
+            drafts["rechunk"] = [row for row in drafts["rechunk"] if row.get("source_url") != source_url] + [{
+                "job_id": job_id, "source_url": source_url, "document_ids": list(job["document_ids"]),
+                "mode": mode, "old_chunk_count": len(current_ids), "new_chunk_count": len(new_chunks), "status": "staged",
+            }]
+        with rechunk_lock:
+            if job_id in rechunk_jobs:
+                rechunk_jobs[job_id].update(staged=True, updated_at=time.time())
+        return {"staged": True, "job_id": job_id, "old_chunk_count": len(current_ids),
+                "new_chunk_count": len(new_chunks), "draft": draft_public()}
+
     @app.post("/api/admin-ui/evaluations/upload")
     def upload(payload: DatasetUploadPayload, _: None = Depends(require_admin_session)):
         try:
@@ -1690,6 +2730,58 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
             drafts["config"] = copy.deepcopy(dict(row["values"]))
         return {"preset": parameter_preset_public(row), "draft": draft_public()}
 
+    @app.get("/api/admin-ui/prompt-presets")
+    def list_prompt_presets(_: None = Depends(require_admin_session)):
+        items = sorted(prompt_presets.values(), key=lambda row: float(row.get("updated_at") or 0), reverse=True)
+        return {"items": [prompt_preset_public(row) for row in items], "limit": MAX_PROMPT_PRESETS}
+
+    @app.post("/api/admin-ui/prompt-presets")
+    def create_prompt_preset(payload: PromptPresetPayload, _: None = Depends(require_admin_session)):
+        name = _clean(payload.name)
+        if not name:
+            raise HTTPException(status_code=422, detail="프롬프트 설정 이름을 입력해 주세요.")
+        with mutation_lock:
+            duplicate = next((row for row in prompt_presets.values() if _clean(row.get("name")) == name), None)
+            if duplicate is not None:
+                raise HTTPException(status_code=409, detail="같은 이름의 프롬프트 설정이 이미 있습니다. 다른 이름을 사용해 주세요.")
+            if len(prompt_presets) >= MAX_PROMPT_PRESETS:
+                raise HTTPException(status_code=409, detail=f"프롬프트 설정은 최대 {MAX_PROMPT_PRESETS}개까지 저장할 수 있습니다.")
+            try:
+                values = validate_prompt_preset_values(payload.values)
+            except ValueError as error:
+                raise HTTPException(status_code=422, detail=str(error)) from error
+            now = time.time()
+            row = {
+                "preset_id": f"prompt-preset-{uuid.uuid4().hex[:12]}",
+                "name": name,
+                "values": values,
+                "created_at": now,
+                "updated_at": now,
+            }
+            prompt_presets[row["preset_id"]] = row
+            save_prompt_presets()
+        return prompt_preset_public(row)
+
+    @app.delete("/api/admin-ui/prompt-presets/{preset_id}")
+    def delete_prompt_preset(preset_id: str, _: None = Depends(require_admin_session)):
+        with mutation_lock:
+            row = prompt_presets.pop(_clean(preset_id), None)
+            if row is None:
+                raise HTTPException(status_code=404, detail="프롬프트 설정을 찾지 못했습니다.")
+            save_prompt_presets()
+        return {"deleted": True, "preset_id": str(row["preset_id"])}
+
+    @app.post("/api/admin-ui/prompt-presets/{preset_id}/load-draft")
+    def load_prompt_preset_to_draft(preset_id: str, _: None = Depends(require_admin_session)):
+        row = prompt_presets.get(_clean(preset_id))
+        if row is None:
+            raise HTTPException(status_code=404, detail="프롬프트 설정을 찾지 못했습니다.")
+        try:
+            prompt_state = require_prompt_manager().save_draft(row["values"])
+        except ValueError as error:
+            raise HTTPException(status_code=422, detail=str(error)) from error
+        return {"preset": prompt_preset_public(row), "prompts": prompt_state, "draft": draft_public()}
+
     @app.get("/api/admin-ui/draft")
     def get_draft(_: None = Depends(require_admin_session)): return draft_public()
 
@@ -1782,11 +2874,19 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
 
     @app.delete("/api/admin-ui/draft")
     def clear_draft(_: None = Depends(require_admin_session)):
-        drafts["config"].clear(); drafts["add"].clear(); drafts["remove"].clear()
+        drafts["config"].clear(); drafts["add"].clear(); drafts["remove"].clear(); drafts["rechunk"].clear(); drafts["suggestion_cache"].clear()
         if prompt_manager is not None:
             prompt_manager.clear_draft()
         return draft_public()
 
+    @app.delete("/api/admin-ui/draft/suggestion-cache/{suggestion_id}")
+    def clear_suggestion_cache_draft(suggestion_id: str, _: None = Depends(require_admin_session)):
+        clean_id = _clean(suggestion_id).upper()
+        if clean_id not in drafts["suggestion_cache"]:
+            raise HTTPException(status_code=404, detail="질의 캐시 초안을 찾지 못했습니다.")
+        drafts["suggestion_cache"].pop(clean_id, None)
+        return {"cleared": True, "draft": draft_public(), "catalog": suggestion_public()}
+
     @app.post("/api/admin-ui/apply")
     def apply(payload: ApplyPayload, _: None = Depends(require_admin_session)):
         if payload.confirmation != "운영 반영": raise HTTPException(status_code=422, detail="확인 문구로 '운영 반영'을 입력해 주세요.")
@@ -1809,14 +2909,21 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                     prompt_target = {"version": f"{version_id}-prompt", "values": prompt_manager.draft_values()}
                 target = {"config": _validate_config(drafts["config"], active_config()), "chunks": target_chunks,
                           "vectors": target_vectors, "prompts": prompt_target}
-                apply_snapshot(target, archive_prompts=bool(prompt_changes))
+                runtime_changes = bool(
+                    drafts["config"] or drafts["add"] or drafts["remove"] or prompt_changes
+                )
+                if runtime_changes:
+                    apply_snapshot(target, archive_prompts=bool(prompt_changes))
+                cache_changes = copy.deepcopy(list(drafts["suggestion_cache"].values()))
+                apply_suggestion_cache_drafts()
                 history.append({"version_id": version_id, "created_at": time.time(), "snapshot": before, "active": True,
                                 "summary": {"config": copy.deepcopy(drafts["config"]), "added": sorted(drafts["add"]),
-                                            "removed": sorted(drafts["remove"]), "prompts": prompt_changes,
+                                            "removed": sorted(drafts["remove"]), "rechunk": copy.deepcopy(drafts["rechunk"]), "prompts": prompt_changes,
+                                            "suggestion_cache": cache_changes,
                                             "label": "초기 운영 상태" if not history else "이전 운영 상태"}})
                 history[:] = history[-10:]
                 for row in history[:-1]: row["active"] = False
-                drafts["config"].clear(); drafts["add"].clear(); drafts["remove"].clear()
+                drafts["config"].clear(); drafts["add"].clear(); drafts["remove"].clear(); drafts["rechunk"].clear(); drafts["suggestion_cache"].clear()
                 save_history()
                 return {"applied": True, "version_id": version_id, "chunk_count": len(target_chunks), "active_config": active_config()}
             except Exception as error:
@@ -1843,14 +2950,14 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                     "snapshot": before,
                     "active": True,
                     "summary": {
-                        "config": {}, "added": [], "removed": [], "prompts": [],
+                        "config": {}, "added": [], "removed": [], "rechunk": [], "prompts": [],
                         "label": "기본값 초기화 직전 상태",
                     },
                 })
                 history[:] = history[-10:]
                 for row in history[:-1]:
                     row["active"] = False
-                drafts["config"].clear(); drafts["add"].clear(); drafts["remove"].clear()
+                drafts["config"].clear(); drafts["add"].clear(); drafts["remove"].clear(); drafts["rechunk"].clear(); drafts["suggestion_cache"].clear()
                 if prompt_manager is not None:
                     prompt_manager.clear_draft()
                 save_history()
@@ -1931,6 +3038,166 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
                    _: None = Depends(require_admin_session)):
         return monitoring_snapshot(hours, limit, offset)
 
+    @app.get("/api/admin-ui/suggestion-cache")
+    def suggestion_cache_list(_: None = Depends(require_admin_session)):
+        return suggestion_public()
+
+    @app.post("/api/admin-ui/suggestion-cache")
+    def suggestion_cache_create(payload: SuggestionCreatePayload, _: None = Depends(require_admin_session)):
+        manager = require_suggestion_cache()
+        try:
+            suggestion_id = suggestion_id_for(payload)
+            existing = {
+                row["suggestion_id"]
+                for row in manager.list_catalog(include_disabled=True)
+            }
+            if suggestion_id in existing or suggestion_id in drafts["suggestion_cache"]:
+                raise ValueError("같은 업무·키워드·표준 질문의 항목이 이미 존재합니다.")
+            stage_suggestion_change(
+                suggestion_id,
+                create=True,
+                metadata={
+                    "business_key": payload.business_key,
+                    "business": payload.business,
+                    "keyword": payload.keyword,
+                    "canonical_question": payload.canonical_question,
+                    "display_order": payload.display_order
+                    or len(existing) + 1,
+                },
+                enabled=True,
+            )
+            catalog = suggestion_public()
+            row = next(item for item in catalog["items"] if item["suggestion_id"] == suggestion_id)
+            return {"staged": True, "created": True, "item": row, "catalog": catalog,
+                    "draft": draft_public()}
+        except Exception as error:
+            raise HTTPException(status_code=422, detail=f"질의 캐시 추가 초안 실패: {error}") from error
+
+    @app.put("/api/admin-ui/suggestion-cache/{suggestion_id}")
+    def suggestion_cache_update(suggestion_id: str, payload: SuggestionUpdatePayload,
+                                _: None = Depends(require_admin_session)):
+        try:
+            clean_id = require_known_suggestion(suggestion_id)
+            stage_suggestion_change(
+                clean_id,
+                metadata={
+                    "business_key": payload.business_key,
+                    "business": payload.business,
+                    "keyword": payload.keyword,
+                    "canonical_question": payload.canonical_question,
+                    "display_order": payload.display_order,
+                },
+                expected_row_version=payload.row_version,
+            )
+            catalog = suggestion_public()
+            row = next(item for item in catalog["items"] if item["suggestion_id"] == clean_id)
+            return {"staged": True, "updated": True, "item": row, "catalog": catalog,
+                    "draft": draft_public()}
+        except KeyError as error:
+            raise HTTPException(status_code=404, detail="질의 캐시 항목을 찾지 못했습니다.") from error
+        except RuntimeError as error:
+            raise HTTPException(status_code=409, detail=str(error)) from error
+        except Exception as error:
+            raise HTTPException(status_code=422, detail=f"질의 캐시 수정 실패: {error}") from error
+
+    @app.delete("/api/admin-ui/suggestion-cache/{suggestion_id}")
+    def suggestion_cache_disable(suggestion_id: str, _: None = Depends(require_admin_session)):
+        try:
+            clean_id = require_known_suggestion(suggestion_id)
+            stage_suggestion_change(clean_id, enabled=False)
+            return {"staged": True, "removed_from_chatbot": False,
+                    "catalog": suggestion_public(), "draft": draft_public()}
+        except KeyError as error:
+            raise HTTPException(status_code=404, detail="질의 캐시 항목을 찾지 못했습니다.") from error
+
+    @app.post("/api/admin-ui/suggestion-cache/{suggestion_id}/restore")
+    def suggestion_cache_restore(suggestion_id: str, _: None = Depends(require_admin_session)):
+        try:
+            clean_id = require_known_suggestion(suggestion_id)
+            stage_suggestion_change(clean_id, enabled=True)
+            return {"staged": True, "restored": False,
+                    "catalog": suggestion_public(), "draft": draft_public()}
+        except KeyError as error:
+            raise HTTPException(status_code=404, detail="질의 캐시 항목을 찾지 못했습니다.") from error
+
+    @app.put("/api/admin-ui/suggestion-cache/{suggestion_id}/answer")
+    def suggestion_cache_answer_save(suggestion_id: str, payload: SuggestionAnswerPayload,
+                                     _: None = Depends(require_admin_session)):
+        sources = []
+        for row in payload.sources:
+            title = _clean(row.get("title") or "공식 안내")
+            url = _clean(row.get("url"))
+            if not url:
+                continue
+            if not _is_public_kdic_url(url):
+                raise HTTPException(
+                    status_code=422,
+                    detail="검증 답변의 출처는 예금보험공사 공식 URL(kdic.or.kr)만 사용할 수 있습니다.",
+                )
+            sources.append({"title": title, "url": url})
+        if not sources:
+            raise HTTPException(
+                status_code=422,
+                detail="검증 답변을 저장하려면 예금보험공사 공식 출처 URL을 1개 이상 입력해 주세요.",
+            )
+        try:
+            clean_id = require_known_suggestion(suggestion_id)
+            stage_suggestion_change(
+                clean_id,
+                answer_change={"action": "save", "answer": payload.answer, "sources": sources},
+            )
+            return {"staged": True, "saved": False, "catalog": suggestion_public(),
+                    "draft": draft_public()}
+        except KeyError as error:
+            raise HTTPException(status_code=404, detail="질의 캐시 항목을 찾지 못했습니다.") from error
+        except Exception as error:
+            raise HTTPException(status_code=422, detail=f"검증 답변 저장 실패: {error}") from error
+
+    @app.delete("/api/admin-ui/suggestion-cache/{suggestion_id}/answer")
+    def suggestion_cache_answer_clear(suggestion_id: str, _: None = Depends(require_admin_session)):
+        try:
+            stage_suggestion_change(
+                require_known_suggestion(suggestion_id),
+                answer_change={"action": "clear"},
+            )
+            return {"staged": True, "cleared": False, "catalog": suggestion_public(),
+                    "draft": draft_public()}
+        except KeyError as error:
+            raise HTTPException(status_code=404, detail="질의 캐시 항목을 찾지 못했습니다.") from error
+
+    @app.post("/api/admin-ui/suggestion-cache/{suggestion_id}/activate")
+    def suggestion_cache_activate(suggestion_id: str, payload: SuggestionVersionPayload,
+                                  _: None = Depends(require_admin_session)):
+        try:
+            clean_id = require_known_suggestion(suggestion_id)
+            versions = require_suggestion_cache().versions(clean_id)
+            if not any(row.get("cache_key") == payload.cache_key for row in versions):
+                raise ValueError("선택한 검증 답변 버전을 찾지 못했습니다.")
+            stage_suggestion_change(
+                clean_id,
+                answer_change={"action": "activate", "cache_key": payload.cache_key},
+            )
+            return {"staged": True, "activated": False, "catalog": suggestion_public(),
+                    "draft": draft_public()}
+        except (KeyError, ValueError) as error:
+            raise HTTPException(status_code=422, detail=str(error)) from error
+
+    @app.post("/api/admin-ui/suggestion-cache/{suggestion_id}/generate", status_code=202)
+    def suggestion_cache_generate(suggestion_id: str, _: None = Depends(require_admin_session)):
+        manager = require_suggestion_cache()
+        row = next((item for item in manager.list_catalog(include_disabled=False)
+                    if item["suggestion_id"] == _clean(suggestion_id).upper()), None)
+        if row is None:
+            raise HTTPException(status_code=409, detail="신규 키워드는 먼저 운영 반영한 뒤 챗봇 답변을 생성해 주세요.")
+        try:
+            job_id = service_module.JOB_SERVICE.submit(
+                f"admin-cache-{uuid.uuid4().hex[:12]}", row["canonical_question"],
+                suggestion_id="",
+            )
+        except Exception as error:
+            raise HTTPException(status_code=422, detail=f"검증 답변 생성 시작 실패: {error}") from error
+        return {"job_id": job_id, "suggestion_id": row["suggestion_id"], "status": "running"}
+
     @app.get("/api/admin-ui/guardrails")
     def guardrails(_: None = Depends(require_admin_session)):
         return require_guardrail_manager().public()
@@ -1989,16 +3256,24 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
         try:
             question = str(guard["text"])
             active = manager.active_values()
-            draft = manager.draft_values()
+            selected_preset = None
+            if payload.preset_id:
+                selected_preset = prompt_presets.get(_clean(payload.preset_id))
+                if selected_preset is None:
+                    raise HTTPException(status_code=404, detail="비교할 프롬프트 설정을 찾지 못했습니다.")
+                candidate_values = copy.deepcopy(dict(selected_preset["values"]))
+            else:
+                candidate_values = manager.draft_values()
             baseline = run_prompt_variant(question, active)
-            candidate = run_prompt_variant(question, draft)
+            candidate = run_prompt_variant(question, candidate_values)
             return {
                 "question": question,
                 "guardrail_hits": guard["hits"],
                 "active_version": manager.public()["active_version"],
+                "candidate_preset": prompt_preset_public(selected_preset) if selected_preset else None,
                 "baseline": baseline,
                 "candidate": candidate,
-                "changed_slots": [slot for slot in active if active[slot] != draft[slot]],
+                "changed_slots": [slot for slot in active if active[slot] != candidate_values[slot]],
             }
         finally:
             prompt_compare_lock.release()
@@ -2039,7 +3314,7 @@ def install_admin_routes(service_module: Any, html_path: str | Path, runtime_glo
     @app.get("/api/admin-ui/capabilities")
     def capabilities(_: None = Depends(require_admin_session)):
         base = service_module.admin_capabilities(); features = list(base.get("features") or [])
-        for value in ("chat_pipeline_test", "chat_pipeline_compare", "runtime_config", "evaluation_dataset_upload", "evaluation_run", "evaluation_k_curve", "evaluation_record_delete", "parameter_apply", "chunk_staged_write", "draft_partial_clear", "rollback", "history_delete", "api_key_test", "api_key_runtime_rotation", "pipeline_runtime_graph", "pipeline_setting_links", "pipeline_label_customization", "operations_monitoring", "guardrail_draft", "guardrail_test", "guardrail_apply", "guardrail_rollback", "prompt_draft", "prompt_compare"):
+        for value in ("chat_pipeline_test", "chat_pipeline_compare", "runtime_config", "evaluation_dataset_upload", "evaluation_run", "evaluation_k_curve", "evaluation_record_delete", "parameter_apply", "chunk_staged_write", "draft_partial_clear", "rollback", "history_delete", "api_key_test", "api_key_runtime_rotation", "pipeline_runtime_graph", "pipeline_setting_links", "pipeline_label_customization", "operations_monitoring", "guardrail_draft", "guardrail_test", "guardrail_apply", "guardrail_rollback", "prompt_draft", "prompt_compare", "prompt_presets"):
             if value not in features: features.append(value)
         blocked = [v for v in (base.get("disabled_mutations") or []) if v not in {"document_write", "document_delete", "evaluation_run", "parameter_apply"}]
         return {**base, "admin_mode": "STAGED_WRITE", "features": features, "disabled_mutations": blocked}
diff --git a/Development/ec2_deploy/kdic_deploy_assets/kdic_ec2_launcher.py b/Development/ec2_deploy/kdic_deploy_assets/kdic_ec2_launcher.py
index 8951ada..61c3b0e 100644
--- a/Development/ec2_deploy/kdic_deploy_assets/kdic_ec2_launcher.py
+++ b/Development/ec2_deploy/kdic_deploy_assets/kdic_ec2_launcher.py
@@ -63,6 +63,9 @@ def main() -> None:
         admin_token=os.getenv("KDIC_ADMIN_TOKEN"),
     )
     print(f"KDIC pipeline attached: {attached['pipeline']}")
+    # 관리자 파이프라인 화면도 공개 챗봇 어댑터가 실제로 등록한 동일한
+    # 런타임 이름을 사용한다. 별도 표시명이나 추정값을 두지 않는다.
+    pipeline_module.PIPELINE_VERSION = str(attached["pipeline"])
 
     # 관리자 파라미터테스트/평가/발행 라우트는 adapter가 아니라 원본 파이프라인
     # 모듈(hybrid_minmax_search, CHUNKS 등)이 필요해서 별도로 연결해준다.
diff --git a/Development/ec2_deploy/kdic_deploy_assets/kdic_final_pipeline.py b/Development/ec2_deploy/kdic_deploy_assets/kdic_final_pipeline.py
index 6108eed..7d6235e 100644
--- a/Development/ec2_deploy/kdic_deploy_assets/kdic_final_pipeline.py
+++ b/Development/ec2_deploy/kdic_deploy_assets/kdic_final_pipeline.py
@@ -11,7 +11,7 @@ import time
 from dataclasses import asdict, dataclass
 from datetime import datetime, timedelta, timezone
 from pathlib import Path
-from typing import Any, Iterable
+from typing import Any, Callable, Iterable
 from urllib.parse import parse_qs, urlencode, urljoin, urlparse, urlunparse
 from urllib.robotparser import RobotFileParser
 
@@ -3709,11 +3709,22 @@ def run_pipeline(
     review_csv_path: str | Path,
     runtime_root: str | Path,
     config: PipelineConfig | None = None,
+    progress_callback: Callable[[int, str, str], None] | None = None,
 ) -> dict[str, Any]:
+    def report_progress(percent: int, stage: str, detail: str = "") -> None:
+        if progress_callback is None:
+            return
+        try:
+            progress_callback(max(0, min(100, int(percent))), stage, detail)
+        except Exception:
+            # Progress reporting is observational and must never break crawling.
+            pass
+
     config = config or PipelineConfig()
     validate_media_policy(config)
     runtime_root = Path(runtime_root)
     paths = create_output_paths(runtime_root)
+    report_progress(2, "작업 폴더 준비", paths.root.name)
     session = create_session(config)
 
     review_df = pd.read_csv(review_csv_path, encoding="utf-8-sig", dtype=str).fillna("")
@@ -3732,14 +3743,24 @@ def run_pipeline(
     if config.max_urls is not None:
         target_df = target_df.head(config.max_urls)
     target_df.to_csv(paths.manifest / "manifest_run.csv", index=False, encoding="utf-8-sig")
+    report_progress(8, "수집 정책 확인", f"대상 URL {len(target_df)}건")
 
     page_documents: list[dict[str, Any]] = []
     embedded_documents: list[dict[str, Any]] = []
     run_results: list[dict[str, Any]] = []
     crawl_jsonl = paths.logs / "crawl_results.jsonl"
 
-    for _, series in tqdm(target_df.iterrows(), total=len(target_df), desc="KDIC 42개 정책 기반 수집"):
+    target_count = max(1, len(target_df))
+    for target_index, (_, series) in enumerate(
+        tqdm(target_df.iterrows(), total=len(target_df), desc="KDIC 42개 정책 기반 수집"),
+        start=1,
+    ):
         row = row_to_dict(series)
+        report_progress(
+            10 + int((target_index - 1) / target_count * 42),
+            "URL 수집·파싱",
+            f"{target_index}/{len(target_df)} · {row.get('page_title') or row.get('url_id')}",
+        )
         started = now_kst_iso()
         try:
             if row["normalized_decision"] == "link_only":
@@ -3822,10 +3843,16 @@ def run_pipeline(
         result["finished_at"] = now_kst_iso()
         run_results.append(result)
         append_jsonl(crawl_jsonl, result)
+        report_progress(
+            10 + int(target_index / target_count * 42),
+            "URL 수집·파싱",
+            f"{target_index}/{len(target_df)} · {result.get('status')}",
+        )
         if row["normalized_decision"] != "link_only":
             time.sleep(config.request_delay_seconds)
 
     # JavaScript 기반 공개 첨부파일도 다운로드합니다.
+    report_progress(55, "문서 후처리", "첨부파일·페이지 구조 확인")
     download_js_attachments(page_documents, paths, config)
 
     # 모든 첨부파일을 검증·텍스트 추출·정책 분류합니다.
@@ -3833,6 +3860,7 @@ def run_pipeline(
         page_documents, paths, config
     )
     documents = page_documents + embedded_documents + attachment_documents
+    report_progress(62, "문서 구조화 완료", f"문서 {len(documents)}건")
 
     # 영상·웹툰은 다운로드하지 않고 공식 게시 페이지 참고 링크 목록으로 저장합니다.
     supplementary_rows: list[dict[str, Any]] = []
@@ -3879,10 +3907,17 @@ def run_pipeline(
     chunks: list[dict[str, Any]] = []
     chunks_by_doc: dict[str, list[dict[str, Any]]] = {}
     if config.create_chunks:
-        for document in documents:
+        document_count = max(1, len(documents))
+        for document_index, document in enumerate(documents, start=1):
+            report_progress(
+                65 + int((document_index - 1) / document_count * 14),
+                "구조 기반 청킹",
+                f"{document_index}/{len(documents)} · {document.get('title') or document.get('doc_id')}",
+            )
             doc_chunks = structure_aware_chunks(document, config)
             chunks.extend(doc_chunks)
             chunks_by_doc[document["doc_id"]] = doc_chunks
+        report_progress(79, "구조 기반 청킹 완료", f"청크 {len(chunks)}개")
     chunks_path = paths.processed / "chunks.jsonl"
     with chunks_path.open("w", encoding="utf-8") as file:
         for chunk in chunks:
@@ -4025,6 +4060,7 @@ def run_pipeline(
 
     validation_df = pd.DataFrame(validations)
     validation_df.to_csv(paths.quality / "regression_tests.csv", index=False, encoding="utf-8-sig")
+    report_progress(92, "품질·회귀 검사 완료", f"검사 {len(validations)}건")
 
     service_action_count = sum(
         1 for document in page_documents for action in document.get("actions", [])
@@ -4067,10 +4103,12 @@ def run_pipeline(
     }
     write_json(paths.quality / "quality_summary.json", summary)
 
+    report_progress(97, "결과 패키징", "미리보기 데이터 저장")
     archive_path = Path(shutil.make_archive(
         base_name=str(paths.root), format="zip",
         root_dir=paths.root.parent, base_dir=paths.root.name,
     ))
+    report_progress(100, "처리 완료", f"문서 {len(documents)}건 · 청크 {len(chunks)}개")
     return {
         "paths": paths,
         "manifest_df": manifest_df,
diff --git a/Development/ec2_deploy/kdic_deploy_assets/kdic_postgres_store.py b/Development/ec2_deploy/kdic_deploy_assets/kdic_postgres_store.py
index 9a9b752..00ba91b 100644
--- a/Development/ec2_deploy/kdic_deploy_assets/kdic_postgres_store.py
+++ b/Development/ec2_deploy/kdic_deploy_assets/kdic_postgres_store.py
@@ -443,15 +443,18 @@ class PostgresSuggestionAnswerCache:
         with _cursor() as cur:
             for display_order, row in enumerate(records, start=1):
                 suggestion_id = _clean_text(row.get("suggestion_id")).upper()
+                business_key = _clean_text(row.get("business_key")) or _clean_text(
+                    row.get("business")
+                )
                 business = _clean_text(row.get("business"))
                 keyword = _clean_text(row.get("label"))
                 question = _clean_text(row.get("query"))
                 cur.execute(
                     "INSERT INTO suggestion_catalog ("
-                    "suggestion_id, business, keyword, canonical_question, display_order) "
-                    "VALUES (%s, %s, %s, %s, %s) "
+                    "suggestion_id, business_key, business, keyword, canonical_question, display_order) "
+                    "VALUES (%s, %s, %s, %s, %s, %s) "
                     "ON CONFLICT (suggestion_id) DO NOTHING",
-                    (suggestion_id, business, keyword, question, display_order),
+                    (suggestion_id, business_key, business, keyword, question, display_order),
                 )
                 # One-time migration path: if this catalog item does not yet
                 # have an active answer, adopt its newest validated exact-query
@@ -467,6 +470,282 @@ class PostgresSuggestionAnswerCache:
                     (suggestion_id, business, question, suggestion_id),
                 )
 
+    def list_catalog(self, include_disabled: bool = True) -> list[dict[str, Any]]:
+        where = "" if include_disabled else "WHERE catalog.is_enabled = true"
+        with _cursor() as cur:
+            cur.execute(
+                "SELECT catalog.suggestion_id, catalog.business_key, catalog.business, "
+                "catalog.keyword, catalog.canonical_question, catalog.display_order, "
+                "catalog.is_enabled, catalog.is_locked, catalog.active_cache_key, "
+                "catalog.row_version, catalog.created_by, catalog.updated_by, "
+                "extract(epoch from catalog.created_at) AS created_at, "
+                "extract(epoch from catalog.updated_at) AS updated_at, "
+                "answer.hit_count, extract(epoch from answer.last_hit_at) AS last_hit_at, "
+                "answer.public_result, answer.pipeline_name, answer.runtime_revision "
+                "FROM suggestion_catalog AS catalog LEFT JOIN suggestion_answer_cache AS answer "
+                "ON answer.cache_key = catalog.active_cache_key "
+                f"{where} ORDER BY catalog.display_order, catalog.created_at"
+            )
+            rows = cur.fetchall()
+        return [dict(row) for row in rows]
+
+    def snapshot_catalog(self) -> list[dict[str, Any]]:
+        """Capture only the mutable catalog state needed for rollback.
+
+        Answer versions remain immutable history.  Restoring a snapshot moves
+        each catalog pointer back to the answer that was active at that time.
+        """
+
+        rows = self.list_catalog(include_disabled=True)
+        keys = (
+            "suggestion_id",
+            "business_key",
+            "business",
+            "keyword",
+            "canonical_question",
+            "display_order",
+            "is_enabled",
+            "is_locked",
+            "active_cache_key",
+            "row_version",
+            "created_by",
+            "updated_by",
+        )
+        return [{key: row.get(key) for key in keys} for row in rows]
+
+    def restore_catalog(self, snapshot: list[dict[str, Any]], actor: str = "ROLLBACK") -> None:
+        records = [dict(row) for row in snapshot]
+        with _cursor() as cur:
+            # The catalog is small and answer rows do not depend on it.  A
+            # replace avoids transient unique display-order conflicts while
+            # restoring an older ordering.
+            cur.execute("DELETE FROM suggestion_catalog")
+            for row in records:
+                cur.execute(
+                    "INSERT INTO suggestion_catalog (suggestion_id, business_key, business, keyword, "
+                    "canonical_question, display_order, is_enabled, is_locked, active_cache_key, "
+                    "row_version, created_by, updated_by) VALUES "
+                    "(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
+                    (
+                        _clean_text(row.get("suggestion_id")).upper(),
+                        _clean_text(row.get("business_key")),
+                        _clean_text(row.get("business")),
+                        _clean_text(row.get("keyword")),
+                        _clean_text(row.get("canonical_question")),
+                        int(row.get("display_order") or 1),
+                        bool(row.get("is_enabled", True)),
+                        bool(row.get("is_locked", False)),
+                        row.get("active_cache_key"),
+                        max(1, int(row.get("row_version") or 1)),
+                        _clean_text(row.get("created_by")) or "SYSTEM",
+                        _clean_text(actor) or "ROLLBACK",
+                    ),
+                )
+
+    def create_suggestion(
+        self,
+        *,
+        suggestion_id: str,
+        business_key: str,
+        business: str,
+        keyword: str,
+        canonical_question: str,
+        display_order: int | None = None,
+        actor: str = "ADMIN",
+    ) -> dict[str, Any]:
+        clean_id = _clean_text(suggestion_id).upper()
+        with _cursor() as cur:
+            order = int(display_order or 0)
+            if order <= 0:
+                cur.execute("SELECT coalesce(max(display_order), 0) + 1 AS n FROM suggestion_catalog")
+                order = int(cur.fetchone()["n"])
+            cur.execute(
+                "INSERT INTO suggestion_catalog (suggestion_id, business_key, business, keyword, "
+                "canonical_question, display_order, is_enabled, is_locked, created_by, updated_by) "
+                "VALUES (%s, %s, %s, %s, %s, %s, true, false, %s, %s) RETURNING suggestion_id",
+                (
+                    clean_id,
+                    _clean_text(business_key),
+                    _clean_text(business),
+                    _clean_text(keyword),
+                    _clean_text(canonical_question),
+                    order,
+                    _clean_text(actor) or "ADMIN",
+                    _clean_text(actor) or "ADMIN",
+                ),
+            )
+        return next(row for row in self.list_catalog() if row["suggestion_id"] == clean_id)
+
+    def update_suggestion(
+        self,
+        suggestion_id: str,
+        *,
+        business_key: str,
+        business: str,
+        keyword: str,
+        canonical_question: str,
+        display_order: int,
+        row_version: int,
+        actor: str = "ADMIN",
+    ) -> dict[str, Any]:
+        clean_id = _clean_text(suggestion_id).upper()
+        with _cursor() as cur:
+            cur.execute(
+                "SELECT display_order FROM suggestion_catalog WHERE suggestion_id=%s",
+                (clean_id,),
+            )
+            current = cur.fetchone()
+            if current is None:
+                raise KeyError(suggestion_id)
+            current_order = int(current["display_order"])
+            requested_order = int(display_order)
+            if requested_order != current_order:
+                cur.execute(
+                    "SELECT suggestion_id FROM suggestion_catalog WHERE display_order=%s "
+                    "AND suggestion_id<>%s",
+                    (requested_order, clean_id),
+                )
+                occupied = cur.fetchone()
+                if occupied is not None:
+                    cur.execute(
+                        "SELECT coalesce(max(display_order), 0) + 1 AS n FROM suggestion_catalog"
+                    )
+                    temporary_order = int(cur.fetchone()["n"])
+                    cur.execute(
+                        "UPDATE suggestion_catalog SET display_order=%s WHERE suggestion_id=%s",
+                        (temporary_order, clean_id),
+                    )
+                    cur.execute(
+                        "UPDATE suggestion_catalog SET display_order=%s WHERE suggestion_id=%s",
+                        (current_order, occupied["suggestion_id"]),
+                    )
+            cur.execute(
+                "UPDATE suggestion_catalog SET business_key=%s, business=%s, keyword=%s, "
+                "canonical_question=%s, display_order=%s, "
+                "active_cache_key = CASE WHEN canonical_question = %s AND business = %s "
+                "THEN active_cache_key ELSE NULL END, updated_by=%s, updated_at=now(), "
+                "row_version=row_version+1 WHERE suggestion_id=%s AND row_version=%s "
+                "RETURNING suggestion_id",
+                (
+                    _clean_text(business_key),
+                    _clean_text(business),
+                    _clean_text(keyword),
+                    _clean_text(canonical_question),
+                    requested_order,
+                    _clean_text(canonical_question),
+                    _clean_text(business),
+                    _clean_text(actor) or "ADMIN",
+                    clean_id,
+                    int(row_version),
+                ),
+            )
+            if cur.fetchone() is None:
+                raise RuntimeError("다른 관리자가 먼저 수정했습니다. 목록을 새로고침해 주세요.")
+        return next(row for row in self.list_catalog() if row["suggestion_id"] == clean_id)
+
+    def set_enabled(self, suggestion_id: str, enabled: bool, actor: str = "ADMIN") -> None:
+        with _cursor() as cur:
+            cur.execute(
+                "UPDATE suggestion_catalog SET is_enabled=%s, updated_by=%s, updated_at=now(), "
+                "row_version=row_version+1 WHERE suggestion_id=%s RETURNING suggestion_id",
+                (bool(enabled), _clean_text(actor) or "ADMIN", _clean_text(suggestion_id).upper()),
+            )
+            if cur.fetchone() is None:
+                raise KeyError(suggestion_id)
+
+    def clear_active(self, suggestion_id: str, actor: str = "ADMIN") -> None:
+        with _cursor() as cur:
+            cur.execute(
+                "UPDATE suggestion_catalog SET active_cache_key=NULL, updated_by=%s, "
+                "updated_at=now(), row_version=row_version+1 WHERE suggestion_id=%s "
+                "RETURNING suggestion_id",
+                (_clean_text(actor) or "ADMIN", _clean_text(suggestion_id).upper()),
+            )
+            if cur.fetchone() is None:
+                raise KeyError(suggestion_id)
+
+    def versions(self, suggestion_id: str) -> list[dict[str, Any]]:
+        with _cursor() as cur:
+            cur.execute(
+                "SELECT answer.cache_key, answer.pipeline_name, answer.runtime_revision, "
+                "answer.hit_count, answer.public_result, "
+                "extract(epoch from answer.created_at) AS created_at, "
+                "extract(epoch from answer.last_hit_at) AS last_hit_at, "
+                "(catalog.active_cache_key = answer.cache_key) AS is_active "
+                "FROM suggestion_answer_cache AS answer JOIN suggestion_catalog AS catalog "
+                "ON catalog.suggestion_id=answer.suggestion_id "
+                "WHERE answer.suggestion_id=%s AND answer.validation_status='VALIDATED' "
+                "ORDER BY answer.created_at DESC LIMIT 30",
+                (_clean_text(suggestion_id).upper(),),
+            )
+            return [dict(row) for row in cur.fetchall()]
+
+    def save_manual_answer(
+        self,
+        suggestion_id: str,
+        *,
+        answer: str,
+        sources: list[dict[str, str]],
+        actor: str = "ADMIN",
+    ) -> str:
+        clean_id = _clean_text(suggestion_id).upper()
+        clean_answer = str(answer or "").strip()
+        with _cursor() as cur:
+            cur.execute(
+                "SELECT catalog.*, active.public_result, active.raw_result, active.basis_result "
+                "FROM suggestion_catalog AS catalog LEFT JOIN suggestion_answer_cache AS active "
+                "ON active.cache_key=catalog.active_cache_key WHERE catalog.suggestion_id=%s",
+                (clean_id,),
+            )
+            catalog = cur.fetchone()
+            if catalog is None:
+                raise KeyError(suggestion_id)
+            public_result = dict(catalog.get("public_result") or {})
+            raw_result = dict(catalog.get("raw_result") or {})
+            basis_result = dict(catalog.get("basis_result") or {})
+            clean_sources = [
+                {"title": _clean_text(row.get("title") or "공식 안내"), "url": _clean_text(row.get("url"))}
+                for row in sources
+                if _clean_text(row.get("url"))
+            ]
+            required = {
+                "route": "RETRIEVE",
+                "answer": clean_answer,
+                "businesses": [_clean_text(catalog["business"])],
+                "sources": clean_sources,
+                "official_sources": clean_sources,
+                "coverage_status": "SUFFICIENT",
+                "validation_passed": True,
+            }
+            public_result.update(required)
+            raw_result.update(required)
+            raw_payload = dict(raw_result.get("payload") or {})
+            raw_payload.update({"answer": clean_answer, "coverage_status": "SUFFICIENT", "validation_passed": True})
+            raw_result["payload"] = raw_payload
+            cache_key = f"admin:{clean_id}:{uuid.uuid4().hex}"
+            cur.execute(
+                "INSERT INTO suggestion_answer_cache (cache_key, suggestion_id, business, keyword, "
+                "question, public_result, raw_result, basis_result, pipeline_name, runtime_revision, "
+                "validation_status, expires_at) VALUES (%s,%s,%s,%s,%s,%s::jsonb,%s::jsonb,%s::jsonb,"
+                "'ADMIN_MANUAL','managed-cache-admin-v1','VALIDATED',NULL)",
+                (
+                    cache_key,
+                    clean_id,
+                    _clean_text(catalog["business"]),
+                    _clean_text(catalog["keyword"]),
+                    _clean_text(catalog["canonical_question"]),
+                    json.dumps(public_result, ensure_ascii=False),
+                    json.dumps(raw_result, ensure_ascii=False),
+                    json.dumps(basis_result, ensure_ascii=False),
+                ),
+            )
+            cur.execute(
+                "UPDATE suggestion_catalog SET active_cache_key=%s, updated_by=%s, updated_at=now(), "
+                "row_version=row_version+1 WHERE suggestion_id=%s",
+                (cache_key, _clean_text(actor) or "ADMIN", clean_id),
+            )
+        return cache_key
+
     def get_active(self, suggestion_id: str) -> CachedAnswerBundle | None:
         with _cursor() as cur:
             cur.execute(
diff --git a/Development/ec2_deploy/requirements.txt b/Development/ec2_deploy/requirements.txt
index d55d247..e315595 100644
--- a/Development/ec2_deploy/requirements.txt
+++ b/Development/ec2_deploy/requirements.txt
@@ -13,4 +13,5 @@ sentence-transformers>=2.6
 ipywidgets>=8.1
 ipython>=8.20
 tqdm>=4.66
-beautifulsoup4>=4.12
+beautifulsoup4>=4.12
+lxml>=5.2
